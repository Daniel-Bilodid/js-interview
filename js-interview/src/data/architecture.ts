import type { Topic } from '../types'

/*
 * Архітектурні питання (middle/senior): монорепи, shared-пакети,
 * мікрофронтенди, архітектура додатків, стейт-менеджери.
 * Формат той самий, що і в інших вкладках.
 */

export const architectureTopics: Topic[] = [
  {
    id: 'frontend-architecture',
    title: 'Як ти будуєш архітектуру фронтенд-додатка',
    description:
      'Питання про те, як організувати код, щоб він масштабувався. Гарна структура відповіді. 1) Шари: UI (компоненти) → логіка (хуки, стор, сервіси) → дані (API-клієнт); залежності йдуть лише в один бік, UI не знає про транспорт. 2) Код організовано за фічами/доменами, а не за типом файлів: усе про «кошик» лежить разом. Популярний стандарт — Feature-Sliced Design (FSD) із шарами app → pages → widgets → features → entities → shared. 3) Головне правило-обмеження: імпортувати можна лише «вниз» по шарах (feature бере з entities/shared, але не навпаки й не з сусідньої feature) — це руйнує клубок залежностей і закріплюється ESLint-ом. 4) Кожен модуль має публічний API (index.ts) — внутрішності закриті. 5) Стан ділиться на серверний (React Query) і клієнтський (стор) і живе якнайближче до місця використання. Коротка відповідь: «шари з односпрямованими залежностями, код за фічами (FSD), імпорти лише вниз, публічні API модулів, серверний стан окремо від клієнтського».',
    codeExamples: [
      {
        label: 'Feature-Sliced Design — шари',
        code: `src/
  app/        # ініціалізація, провайдери, роутинг
  pages/      # сторінки — збирають віджети
  widgets/    # великі блоки (Header, ProductList)
  features/   # дії користувача (add-to-cart, auth)
  entities/   # бізнес-сутності (product, user)
  shared/     # ui-кіт, хелпери, api — не знає про домен

// правило: імпортувати можна лише з шарів НИЖЧЕ свого`,
      },
      {
        label: 'Публічний API модуля',
        code: `// features/add-to-cart/index.ts — єдина точка входу
export { AddToCartButton } from "./ui/AddToCartButton";
export { useAddToCart } from "./model/useAddToCart";

// ❌ import { x } from "features/add-to-cart/model/internal"
// ✅ import { useAddToCart } from "features/add-to-cart"`,
      },
    ],
    gotchas: [
      'Структура «за типом» (components/, hooks/, utils/) працює лише в малих проєктах — зміна однієї фічі розкидана по всіх папках.',
      'Правила імпортів без автоматики не живуть — закріплюйте їх ESLint-ом (eslint-plugin-boundaries, steiger для FSD).',
      'shared, який знає про бізнес-домен, — перша ознака, що архітектура «потекла».',
      'Серверні дані в Redux/Zustand замість React Query — ручне дублювання кешу, лоадингів та інвалідації.',
      'Сильна відповідь визнає компроміс: для MVP повний FSD — overhead; архітектура має відповідати розміру команди й проєкту.',
    ],
  },
  {
    id: 'feature-sliced-design',
    title: 'Що таке Feature-Sliced Design і яких правил ти в ньому дотримуєшся?',
    definition:
      'Feature-Sliced Design — це методологія організації фронтенд-коду, у якій проєкт ділиться на шари, шари на слайси за предметною областю, а слайси на сегменти за технічною роллю. Шарів шість: app, pages, widgets, features, entities і shared, і вони впорядковані за рівнем відповідальності. Головне правило одне: модуль може імпортувати лише з шарів, розташованих нижче за нього, і ніколи з сусіднього слайса того самого шару. Кожен слайс має публічний API у вигляді index-файлу, і все, що не експортоване звідти, вважається внутрішнім. Разом ці правила гарантують, що граф залежностей лишається ациклічним, а фічу можна видалити разом з її папкою.',
    why: 'Структура за типом файлів, де окремо лежать усі компоненти, усі хуки й усі утиліти, перестає працювати вже на середньому проєкті: зміна однієї фічі зачіпає пʼять папок, а видалити її безпечно неможливо, бо невідомо, хто ще імпортує її частини. FSD дає формальні правила, які закріплюються лінтером і не залежать від того, наскільки акуратна конкретна людина.',
    simple:
      'Уяви будинок, де кожен поверх може спиратись лише на нижчі, а мешканці одного поверху не ходять одне до одного через стіну. Нижній шар shared — це фундамент: кнопки, хелпери, налаштований клієнт API, і він нічого не знає про бізнес. Вище entities — бізнес-сутності: товар, користувач, замовлення, їхні типи й картки. Ще вище features — дії користувача: додати в кошик, залогінитись, застосувати фільтр. Далі widgets — великі самостійні блоки, зібрані з фіч і сутностей, наприклад шапка або картка товару з кнопками. Потім pages — сторінки, що компонують віджети, і нарешті app — провайдери, роутинг, глобальні стилі. Правила, яких я тримаюсь на практиці, такі. Перше: імпорт лише вниз, тому feature може взяти entity, а entity ніколи не бере feature. Друге: жодного імпорту з сусіднього слайса того самого шару, бо саме це створює клубок звʼязків, який потім неможливо розплутати. Третє: усе тільки через публічний index, ніяких імпортів у нутрощі чужого слайса. Четверте: shared лишається без знань про домен, і щойно там зʼявляється щось про замовлення, шар протік. Пʼяте: правила перевіряє не людина, а лінтер — steiger або eslint-plugin-boundaries. І чесний нюанс для співбесіди: на маленькому проєкті повний FSD — надмірність, тому часто беруть спрощений варіант із трьох шарів і доростають до повного, коли команда збільшується.',
    related: [
      {
        title: 'Слайси й сегменти всередині шару',
        text: 'Слайс — це вертикальна нарізка за предметною областю, наприклад product або auth; саме слайси роблять код видаляним разом із папкою. Сегмент — технічна нарізка всередині слайса: ui для компонентів, model для стану й логіки, api для запитів, lib для допоміжного коду. Шари app і shared слайсів не мають, бо ділити їх за доменом немає сенсу.',
      },
      {
        title: 'Чому заборонені горизонтальні імпорти',
        text: 'Імпорт із сусіднього слайса того самого шару — найчастіше порушення, бо технічно він виглядає нешкідливо. Саме він створює цикли й робить неможливим видалення фічі, адже невідомо, хто ще на неї спирається. Дозволений вихід — підняти спільне на шар нижче, в entities або shared, або скомпонувати обидві фічі на рівні widget чи page, де їм і місце.',
      },
      {
        title: 'Автоматична перевірка правил',
        text: 'Без автоматики методологія живе кілька місяців. steiger — офіційний лінтер FSD, який перевіряє структуру шарів і публічні API; eslint-plugin-boundaries дозволяє описати ті самі правила як звичайні правила ESLint і зробити їх помилкою збірки, а не порадою на ревʼю коду.',
      },
      {
        title: 'Альтернативи і компроміси',
        text: 'FSD не єдиний варіант: є простий поділ за фічами без формальних шарів, є модульна структура за доменами в стилі DDD, є атомарний дизайн для UI-шару. Сильна відповідь на співбесіді називає компроміс: FSD дає передбачуваність ціною багатьох папок і церемонії, тому для прототипу чи команди з двох людей він частіше заважає, ніж допомагає.',
      },
    ],
    codeExamples: [
      {
        label: 'Шари, слайси й сегменти',
        code: `src/
  app/                    # провайдери, роутер, глобальні стилі
  pages/
    product/              # слайс сторінки
  widgets/
    product-card/
  features/
    add-to-cart/
      ui/                 # сегменти
      model/
      api/
      index.ts            # публічний API слайса
  entities/
    product/
  shared/
    ui/  api/  lib/       # без знань про домен`,
      },
      {
        label: 'Дозволені й заборонені імпорти',
        code: `// ✅ feature бере entity та shared — це шари нижче
import { ProductCard } from "entities/product";
import { Button } from "shared/ui";

// ❌ entity тягне feature — імпорт угору
import { AddToCart } from "features/add-to-cart";

// ❌ сусідній слайс того самого шару
import { useAuthForm } from "features/auth/model/useAuthForm";

// ❌ обхід публічного API
import { Button } from "shared/ui/Button/Button";`,
      },
    ],
    seniorNotes: [
      'Шар app у FSD не є місцем для бізнес-логіки: там живуть лише провайдери, роутер, ініціалізація стору й глобальні стилі. Поява там компонента з доменною логікою — ознака, що слайс не знайшов свого шару.',
      'Слайси шару entities можуть мати виняток із заборони горизонтальних імпортів через так званий cross-import у сегменті @x, бо звʼязки між сутностями на кшталт замовлення і товару природні; для features такого винятку немає.',
      'Публічний API через index-файл конфліктує з tree shaking, якщо індекс реекспортує все підряд: збирач не завжди може викинути невикористане, і розмір бандла росте. Для великих слайсів роблять кілька точок входу замість одного індексу.',
      'steiger перевіряє структуру, але не ловить логічних помилок на кшталт сутності, що знає про формат конкретного екрана; архітектурні правила все одно потребують ревʼю, лінтер лише знімає механічну частину.',
      'Міграцію на FSD роблять шарами знизу вгору, починаючи з shared і entities, а не перенесенням усього проєкту одним рухом: інакше злиття гілок стає неможливим на тижні.',
    ],
  },

  {
    id: 'features-communication',
    title: 'Чи можуть фічі спілкуватись і як передати дані між ними без стору й контексту?',
    definition:
      'Прямі імпорти між фічами заборонені саме тому, що вони створюють цикли й роблять фічі невидаляними. Коли двом фічам треба взаємодіяти, є три законні шляхи. Перший і основний — підняти взаємодію на рівень вище: сторінка або віджет, що рендерить обидві фічі, тримає спільний стан і передає його пропсами й колбеками, тобто класичний lifting state up. Другий — спілкуватись через спільний нижній шар: обидві фічі працюють з однією сутністю або з одним кешем запитів, і зміна однієї автоматично видно іншій після інвалідації. Третій — обмін подіями через шину, коли відправник не має знати про отримувача взагалі, а контекст і глобальний стор беруться останніми, бо це найважчий інструмент.',
    why: 'Якщо фічі спілкуються напряму, зникає головна властивість модульної архітектури: можливість видалити або замінити фічу, не чіпаючи решту. Замість дерева залежностей утворюється граф із циклами, у якому будь-яка зміна тягне за собою невідомий наслідок в іншому кінці застосунку.',
    simple:
      'Почни з питання, хто з них головний. Найчастіше виявляється, що ніхто: є спільний батько, який рендерить обидві фічі, і саме він має володіти спільним станом. Фільтр і список товарів не мають знати одне про одного — обидва отримують значення й колбек від сторінки. Це і є lifting state up, і на співбесіді це очікувана перша відповідь. Другий інструмент — композиція через children і слоти: батько передає готовий елемент усередину, тому компонент-контейнер працює з невідомим йому вмістом і не потребує імпорту. Третій — рендер-проп або компаунд-компоненти: батько тримає стан, а дочірні частини отримують його через функцію чи неявний контекст самого компонента, залишаючись гнучкими. Четвертий шлях — не передавати дані взагалі, а спиратись на спільне джерело: якщо обидві фічі читають одні дані через кеш запитів, достатньо інвалідувати ключ, і друга фіча оновиться сама, бо вона підписана на той самий запит. Пʼятий, найрідший — шина подій або callback-реєстр: фіча кидає подію, а хто на неї відреагує, її не обходить. Він доречний для наскрізних речей на кшталт тостів чи аналітики, але зловживання ним перетворює потік даних на невідстежуваний. Порядок вибору простий: пропси, композиція, спільний серверний кеш, потім контекст, і лише в кінці глобальний стор чи події.',
    related: [
      {
        title: 'Lifting state up і місце володіння станом',
        text: 'Стан має жити в найближчому спільному предку тих компонентів, які його читають або змінюють. Підйом вище за потрібне робить ререндер ширшим і тягне пропси крізь проміжні рівні; тримання нижче за потрібне змушує синхронізувати дві копії. Це рішення і є головною архітектурною роботою при звʼязуванні фіч.',
      },
      {
        title: 'Композиція через children замість пропсів',
        text: 'Передача готового елемента через children або іменований слот прибирає prop drilling без контексту: проміжний компонент не знає ні про дані, ні про тип вмісту, він лише відводить йому місце. Це ж рішення знімає більшість випадків, де інтуїтивно хочеться контексту, і додатково зменшує кількість зайвих ререндерів.',
      },
      {
        title: 'Спільний серверний кеш як канал звʼязку',
        text: 'Коли обидві фічі працюють з тими самими даними сервера, найпростіший звʼязок — не передавати нічого. Одна робить мутацію та інвалідує ключ, друга підписана на той самий ключ і оновлюється сама. У FSD такий спільний доступ цілком законний, бо йде через нижній шар, а не горизонтально.',
      },
      {
        title: 'Шина подій і її ціна',
        text: 'Event bus чи pub-sub повністю розвʼязує відправника й отримувача, але саме тому робить потік даних невидимим: за кодом неможливо зрозуміти, хто відреагує на подію. Прийнятна сфера застосування — наскрізні механізми на кшталт сповіщень, логування й аналітики; для бізнес-логіки це майже завжди ознака того, що структуру обрано невдало.',
      },
    ],
    codeExamples: [
      {
        label: 'Спільний стан у батька замість імпорту між фічами',
        code: `// ❌ фільтр напряму імпортує стор списку — горизонтальний звʼязок
import { productListStore } from "features/product-list";

// ✅ сторінка володіє станом, фічі лишаються незалежними
function ProductsPage() {
  const [filters, setFilters] = useState({ status: "all" });
  return (
    <>
      <ProductFilters value={filters} onChange={setFilters} />
      <ProductList filters={filters} />
    </>
  );
}`,
      },
      {
        label: 'Композиція і спільний кеш запитів',
        code: `// композиція: контейнер не знає, що всередині
<Panel header={<ProductFilters />}>
  <ProductList />
</Panel>

// спільний кеш: звʼязок без передавання даних
function AddToCart({ id }) {
  const qc = useQueryClient();
  const { mutate } = useMutation({
    mutationFn: () => api.addToCart(id),
    onSuccess: () => qc.invalidateQueries({ queryKey: ["cart"] }),
  });
  return <button onClick={() => mutate()}>У кошик</button>;
}

// інша фіча просто читає той самий ключ і оновиться сама
function CartBadge() {
  const { data } = useQuery({ queryKey: ["cart"], queryFn: api.getCart });
  return <span>{data?.count ?? 0}</span>;
}`,
      },
    ],
    seniorNotes: [
      'Контекст не є засобом оптимізації: будь-яка зміна значення провайдера ререндерить усіх споживачів незалежно від того, яке поле вони читають. Тому передача через children або розбиття на кілька дрібних провайдерів часто дає кращий результат, ніж один великий контекст.',
      'Глобальний стор потрібен не лише для шерингу стану: він дає ще збереження між розмонтуваннями, єдине місце для devtools і time-travel, а також доступ до стану з коду поза React на кшталт перехоплювача запитів.',
      'У FSD дозволеним місцем для звʼязування двох фіч є widget або page: саме тому відповідь підняти взаємодію на шар вище формально узгоджується з методологією, а не є обхідним шляхом.',
      'Шина подій ускладнює типізацію і тестування: подія не має сигнатури, яку перевірить компілятор, тому перейменування поля виявляється лише в рантаймі. Мінімальний захист — типізований реєстр подій із union-типом імен і корисних навантажень.',
      'Рендер-проп і компаунд-компоненти розвʼязують ту саму задачу різною ціною: перший гнучкіший і явніший, другий читабельніший у JSX, але приховує неявний контекст усередині, тому його межі застосування важче побачити з коду використання.',
    ],
  },
  {
    id: 'state-managers',
    title: 'Стейт-менеджери: чим відрізняються і куди їх ставити',
    description:
      'Стейт-менеджер — інструмент для стану, потрібного багатьом компонентам одразу. Порівняння, яке очікують: Redux (Toolkit) — один глобальний стор, зміни лише через actions/reducers; максимум передбачуваності й devtools (time-travel), але найбільше коду — для великих команд. Zustand — мінімалістичний стор-хук без провайдерів, селективні підписки з коробки — сучасний дефолт для клієнтського стану. MobX — реактивність через мутації observable-обʼєктів: коду мало, але потік змін менш явний. Jotai — атомарний підхід: стан складається з дрібних атомів, ререндериться лише підписник атома. Context — НЕ стейт-менеджер, а механізм передачі значення: будь-яка зміна ререндерить усіх споживачів. React Query/SWR — окремий клас для СЕРВЕРНОГО стану: кеш запитів, інвалідація, рефетч. Куди що ставити: дані з API → React Query; локальний стан → useState; спільний клієнтський (тема, кошик, фільтри) → Zustand/RTK; форми → react-hook-form. Коротка відповідь: «спершу ділю стан на серверний і клієнтський: серверний — React Query, глобальний клієнтський — Zustand або RTK, локальний — useState».',
    codeExamples: [
      {
        label: 'Zustand vs Redux Toolkit',
        code: `// Zustand — стор за пʼять рядків, без провайдера
const useCart = create((set) => ({
  items: [],
  add: (item) => set((s) => ({ items: [...s.items, item] })),
}));
const items = useCart((s) => s.items); // підписка ЛИШЕ на items

// Redux Toolkit — slice
const cart = createSlice({
  name: "cart",
  initialState: [],
  reducers: { add: (state, a) => { state.push(a.payload); } },
});`,
      },
      {
        label: 'Поділ стану на практиці',
        code: `// серверний — React Query (кеш, рефетч, інвалідація)
const { data } = useQuery({ queryKey: ["products"], queryFn: fetchProducts });

// клієнтський глобальний — Zustand
const theme = useSettings((s) => s.theme);

// локальний — useState поруч із компонентом
const [open, setOpen] = useState(false);`,
      },
    ],
    gotchas: [
      'Перше, що хочуть почути, — поділ стану на серверний і клієнтський: половина «глобального стану» насправді кеш API.',
      'Context — не заміна стейт-менеджеру: немає селективних підписок, зміна value ререндерить усіх споживачів.',
      'Redux без Toolkit сьогодні — антипатерн: RTK прибрав бойлерплейт і вбудував Immer.',
      'Zustand працює й поза React-деревом (читати/писати стор у сокет-хендлері) — часта причина обрати його замість Context.',
      'Класти все підряд у глобальний стор — антипатерн: локальне має жити в useState поруч із компонентом.',
    ],
  },
  {
    id: 'monorepo',
    title: 'Монорепозиторій: плюси й мінуси',
    description:
      'Монорепозиторій (monorepo) — один git-репозиторій, у якому живуть кілька застосунків і пакетів: web, мобільний додаток, спільні бібліотеки. Плюси, які варто назвати: спільний код без публікації в npm (shared-пакети підключаються напряму через workspaces); атомарні зміни — правка API і всіх його споживачів в одному PR; єдині версії залежностей, лінтер, CI; простіший рефакторинг, бо видно всіх споживачів коду; один клон — увесь проєкт. Мінуси: репозиторій росте, CI без кешування сповільнюється, потрібні інструменти й дисципліна меж між пакетами. Інструменти: pnpm/npm workspaces (звʼязують локальні пакети), Turborepo і Nx (кеш збірок і запуск тільки того, що змінилося). Коротка відповідь: «монорепа дає спільний код, атомарні зміни й одну інфраструктуру; ціна — складніший тулінг і CI».',
    codeExamples: [
      {
        label: 'Типова структура',
        code: `apps/
  web/          # Next.js
  mobile/       # React Native
packages/
  ui/           # спільні компоненти
  api-client/   # типізований клієнт API
  config/       # спільні eslint/tsconfig`,
      },
      {
        label: 'pnpm workspaces',
        code: `# pnpm-workspace.yaml
packages:
  - "apps/*"
  - "packages/*"

// apps/web/package.json — локальний пакет без npm
{ "dependencies": { "@acme/ui": "workspace:*" } }`,
      },
    ],
    gotchas: [
      'Монорепа ≠ моноліт: код лежить разом, але застосунки деплояться незалежно.',
      'Без Turborepo/Nx CI збирає все на кожен коміт — час росте з кожним пакетом; кеш і affected-запуски обовʼязкові.',
      'React Native чутливий до симлінків у node_modules — потрібен налаштований metro (watchFolders) чи nohoist.',
      'Без чітких меж пакети починають імпортувати одне одного по колу — циклічні залежності.',
      'Полірепо доречніше, коли команди повністю незалежні й код не перетинається.',
    ],
  },
  {
    id: 'shared-packages',
    title: 'Shared-пакети: як оформлювати спільний код (web + RN)',
    description:
      'Shared-пакет — спільний код (UI-кіт, API-клієнт, утиліти, типи), винесений в окремий пакет монорепи, щоб ним користувалися кілька застосунків. Як оформлюється: окрема папка в packages/ зі своїм package.json (імʼя виду @company/ui), точкою входу (main/exports) і своїми залежностями; застосунки підключають його через workspace:* без публікації в npm. Ключові правила: react і react-native — у peerDependencies (інакше буде дві копії React і зламаються хуки); пакет самодостатній і не знає про застосунки — залежності йдуть лише «вниз»; назовні — лише публічний API через index.ts. Нюанс web + React Native: спільними роблять логіку, хуки й типи, а платформо-залежний UI — через розширення файлів Button.tsx / Button.native.tsx: Metro сам візьме .native-версію, web-бандлер — звичайну. Коротка відповідь: «окремий пакет зі своїм package.json, підключення через workspaces, React у peerDependencies, платформні відмінності — через .native.tsx».',
    codeExamples: [
      {
        label: 'package.json shared-пакета',
        code: `// packages/ui/package.json
{
  "name": "@acme/ui",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "peerDependencies": { "react": ">=18" }
}

// apps/mobile/package.json
{ "dependencies": { "@acme/ui": "workspace:*" } }`,
      },
      {
        label: 'Платформні файли для web + RN',
        code: `packages/ui/src/
  Button.tsx         // web (div, className)
  Button.native.tsx  // RN (Pressable, View)
  index.ts           // export { Button } from "./Button"

// Metro (RN) автоматично бере .native.tsx,
// web-бандлер — звичайний .tsx`,
      },
    ],
    gotchas: [
      'React у dependencies (а не peerDependencies) shared-пакета → дві копії React → «Invalid hook call».',
      'Shared-пакет не повинен імпортувати нічого з apps/ — залежності лише в один бік.',
      'Повністю спільний UI для web і RN зазвичай не виживає: спільними лишаються логіка/хуки/типи, вигляд — платформний.',
      'Зміна shared-пакета зачіпає всіх споживачів одразу — потрібні тести самого пакета.',
      'Якщо пакет підключається сирцями (без build), транспіляцію робить кожен застосунок — бандлери мають це вміти.',
    ],
  },
  {
    id: 'monorepo-dependencies',
    title: 'Які проблеми виникають із залежностями в монорепозиторії?',
    definition:
      'Основна проблема — дублювання однієї бібліотеки в різних версіях. Якщо два пакети просять різні версії React, менеджер встановить обидві, і застосунок отримає два екземпляри бібліотеки: хуки падають з помилкою про недійсний виклик, instanceof перестає працювати, а бандл росте вдвічі. Друга проблема — куди класти залежність: спільне в корені зручно, але робить пакети несамодостатніми, а дублювання в кожному пакеті ускладнює оновлення. Третя — межа між dependencies, devDependencies і peerDependencies: бібліотека всередині монорепо має оголошувати React саме як peer, інакше кожен споживач отримає власну копію. Проти цього працюють workspaces, overrides для вирівнювання версій і єдиний lock-файл на репозиторій.',
    why: 'У монорепо десятки пакетів мають узгоджені між собою версії, і одна розбіжність ламає не збірку, а рантайм в одному конкретному застосунку, причому помилка виглядає як баг React чи бандлера. Тому правила розміщення залежностей тут не питання смаку, а умова того, що монорепо взагалі працює.',
    simple:
      'Уяви спільний склад інструментів на кілька бригад. Поки всі беруть ту саму дриль, усе добре; щойно одна бригада принесла свою — на складі дві дрилі, і невідомо, яку візьме наступний. У монорепо так само: pnpm чи yarn workspaces підіймають спільні залежності в корінь і роблять одну копію на всіх, але лише якщо діапазони версій сумісні. Щойно один пакет просить React вісімнадцятої версії, а другий девʼятнадцятої, зʼявляються дві копії, і починаються дивні баги: хуки кидають помилку про два React, контекст із однієї копії не видно в іншій, бібліотеки на кшталт styled-components тримають окремі теми. Правило розподілу просте. Те, що потрібно всім і має бути одним, тримають в корені: TypeScript, ESLint, Prettier, тестовий раннер. Те, що є частиною інтерфейсу пакета й має бути єдиним у рантаймі, оголошують як peerDependencies: React, react-dom, роутер, бібліотека стилів. Те, що пакет використовує всередині й може мати власну версію, лишається у звичайних dependencies. Окрема пастка — фантомні залежності: пакет імпортує щось, чого немає в його package.json, і все працює, бо бібліотека випадково лежить у кореневому node_modules. У pnpm це ламається одразу через строгу структуру, у npm і yarn спливає лише під час публікації пакета назовні. Інструменти, які реально тримають порядок: pnpm з workspaces, syncpack або аналог для вирівнювання версій, і один lock-файл на весь репозиторій.',
    related: [
      {
        title: 'peerDependencies і навіщо вони саме тут',
        text: 'peerDependencies означає, що бібліотека не приносить залежність із собою, а вимагає її від споживача. Для React і будь-чого з глобальним станом це єдиний правильний варіант: дві копії React в одному дереві дають помилку invalid hook call. Разом із peer зазвичай додають ту саму бібліотеку в devDependencies, щоб пакет збирався й тестувався окремо.',
      },
      {
        title: 'Фантомні залежності й строгий node_modules',
        text: 'Плоский node_modules у npm і yarn дозволяє імпортувати пакет, якого немає у власному package.json, бо він опинився в корені як залежність сусіда. Код працює, доки сусід не оновиться й не прибере цю залежність. pnpm будує строге дерево із симлінками і робить такий імпорт помилкою одразу, тому міграція на pnpm зазвичай і виявляє всі фантоми.',
      },
      {
        title: 'Вирівнювання версій і overrides',
        text: 'Коли дві залежності тягнуть різні версії третьої, їх примусово зводять до однієї через resolutions у yarn або overrides у npm і pnpm. Це працює, але фактично обіцяє сумісність за авторів бібліотеки, тому кожен запис має супроводжуватись причиною; syncpack допомагає тримати діапазони в різних package.json однаковими.',
      },
      {
        title: 'Кеш збірки й залежності між пакетами',
        text: 'Turborepo і Nx будують граф залежностей між пакетами й кешують результати задач за хешем входу, тому зміна в одному пакеті перезбирає лише те, що від нього залежить. Це працює лише коли залежності оголошені чесно: фантомний імпорт не потрапляє в граф, і кеш віддає застарілий результат — ще одна причина строгої структури.',
      },
    ],
    codeExamples: [
      {
        label: 'Правильний розподіл залежностей у пакеті UI kit',
        code: `// packages/ui/package.json
{
  "name": "@acme/ui",
  "peerDependencies": {          // ✅ єдиний React у рантаймі
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {           // для локальної збірки й тестів
    "react": "^19.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {              // внутрішнє, може мати власну версію
    "clsx": "^2.1.0"
  }
}`,
      },
      {
        label: 'Симптоми двох копій і примусове вирівнювання',
        code: `// симптом у консолі: у дереві два React
// Warning: Invalid hook call. You might have more than one copy of React

# побачити всі встановлені версії
pnpm why react

// примусове вирівнювання в кореневому package.json
{
  "pnpm": { "overrides": { "react": "19.0.0" } }
}`,
      },
    ],
    seniorNotes: [
      'Помилка Invalid hook call у монорепо майже ніколи не про неправильний виклик хука: у девʼяти випадках з десяти це дві копії React у дереві, які видно командою pnpm why react або npm ls react.',
      'pnpm економить місце жорсткими посиланнями на глобальне сховище і будує строге дерево, тому саме він ловить фантомні залежності; ціна — бібліотеки, що покладаються на плоский node_modules, інколи потребують налаштування node-linker або shamefully-hoist.',
      'Один lock-файл на монорепо обовʼязковий: окремі lock-файли в пакетах дають різні версії однієї бібліотеки в різних застосунках і роблять відтворюваність збірки неможливою.',
      'Залежність між внутрішніми пакетами оголошують через протокол workspace, наприклад workspace:* у pnpm: під час публікації він автоматично замінюється на реальну версію, а локально завжди вказує на код у репозиторії, а не на реєстр.',
      'Turborepo і Nx кешують задачі за хешем входів, тому незадекларована залежність між пакетами дає найнебезпечнішу форму помилки — успішну збірку із застарілим артефактом замість явного падіння.',
    ],
  },

  {
    id: 'ui-kit-build',
    title: 'Як зібрати UI kit разом зі стилями і як його потім підключати?',
    definition:
      'Збірка UI kit складається з трьох частин: JavaScript, типи і стилі. Код збирають у ESM з сучасним синтаксисом, зберігаючи розбиття по файлах, щоб працював tree shaking; типи генерують окремим проходом через declaration. Зі стилями є три робочі підходи. Перший і найпередбачуваніший — зібрати один CSS-файл, який споживач імпортує одним рядком; другий — CSS Modules зі згенерованими класами, коли потрібна ізоляція імен; третій — нульовий рантайм на кшталт vanilla-extract або Tailwind із пресетом, коли стилі мають типізуватись і теми мінятись без JavaScript. CSS-in-JS із рантаймом теж можливий, але додає вагу й ускладнює серверний рендеринг.',
    why: 'Компонент без стилів не є готовим компонентом, тому питання не в тому, як зібрати React-код, а в тому, як доставити разом із ним CSS, не змушуючи кожен застосунок налаштовувати ту саму збірку і не ламаючи tree shaking. Помилка тут проявляється як тридцятикілобайтний CSS у застосунку, що використовує дві кнопки.',
    simple:
      'Розділи задачу на три питання: що збирається, як стилі потрапляють до споживача і що застосунок має зробити, щоб їх підключити. Збирається бібліотека, а не застосунок: формат ESM, зовнішні залежності не запікаються всередину, React оголошується як peer, і код не бандлиться в один файл, а лишається розбитим — інакше імпорт однієї кнопки притягне весь кіт. У package.json описують поле exports з окремими точками для коду, типів і стилів, додають types і ставлять sideEffects, щоб збирач знав, що можна викинути. Зі стилями найпростіший і найнадійніший варіант — зібрати їх в один файл dist/style.css і сказати в документації імпортувати його один раз у точці входу застосунку. Мінус у тому, що CSS приїде повністю, навіть якщо використано три компоненти. Якщо важливий саме розмір, беруть CSS Modules або vanilla-extract: тоді стилі імпортуються з самого компонента, і збирач споживача сам викине невикористане. Тему й налаштування виносять у CSS-змінні на корені, бо це єдиний спосіб дозволити застосунку перефарбувати кіт без форку. Окремо: не публікуй нескомпільований TypeScript і не покладайся на те, що споживач сам транспілює твій код — це ламається в кожному другому проєкті з іншим налаштуванням збірки. І завжди перевіряй результат збірки чужими очима: publint і are the types wrong ловлять помилки в exports і типах швидше, ніж перший споживач.',
    related: [
      {
        title: 'Поле exports і кілька точок входу',
        text: 'Сучасний package.json описує підшляхи через exports: окремо корінь із компонентами, окремо ./styles.css, окремо типи. Це і документує публічний API, і закриває доступ до внутрішніх файлів. Без exports споживачі починають імпортувати з dist напряму, і будь-яка зміна структури папок стає ламкою зміною.',
      },
      {
        title: 'sideEffects і tree shaking',
        text: 'Прапорець sideEffects у package.json каже збирачу, що модулі бібліотеки не мають побічних ефектів і невикористане можна викинути. Файли CSS обовʼязково перелічують як такі, що ефекти мають, інакше стилі зникнуть із продакшн-збірки. Значення false без винятку для CSS — класична причина бібліотеки, яка в проді раптом без стилів.',
      },
      {
        title: 'Теми через CSS-змінні',
        text: 'Кастомні властивості CSS дозволяють застосунку перевизначити кольори, відступи й радіуси, не чіпаючи код бібліотеки і не збираючи її заново. Це працює однаково для світлої й темної теми, не потребує JavaScript і не ламає серверний рендеринг, на відміну від тем, що передаються через провайдер у рантаймі.',
      },
      {
        title: 'UI kit у монорепо проти опублікованого пакета',
        text: 'Усередині монорепо кіт часто підключають як вихідний код через workspace без збірки: швидше в розробці й немає кроку публікації. Але щойно кіт споживають ззовні, потрібні збірка, версіювання і changelog. Проміжний варіант це publishConfig із різними шляхами для локальної розробки і для реєстру.',
      },
    ],
    codeExamples: [
      {
        label: 'package.json бібліотеки компонентів',
        code: `{
  "name": "@acme/ui",
  "type": "module",
  "sideEffects": ["*.css"],          // ✅ CSS не можна викидати
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./styles.css": "./dist/style.css"
  },
  "peerDependencies": { "react": "^19.0.0" }
}

// у застосунку — один раз у точці входу
import "@acme/ui/styles.css";
import { Button } from "@acme/ui";`,
      },
      {
        label: 'Vite у режимі бібліотеки і теми на змінних',
        code: `// vite.config.ts
export default defineConfig({
  build: {
    lib: { entry: "src/index.ts", formats: ["es"] },
    rollupOptions: {
      external: ["react", "react-dom", "react/jsx-runtime"], // ❌ не запікати
      output: { preserveModules: true },  // ✅ tree shaking у споживача
    },
  },
});

/* тема перевизначається застосунком без форку кіта */
:root { --ui-color-primary: #2563eb; --ui-radius: 8px; }
.button { background: var(--ui-color-primary); border-radius: var(--ui-radius); }`,
      },
    ],
    seniorNotes: [
      'Бандлінг бібліотеки в один файл убиває tree shaking у споживача: імпорт однієї кнопки притягує весь кіт. Режим preserveModules у Rollup або збірка через tsc зберігають структуру файлів і дозволяють збирачу застосунку викинути невикористане.',
      'React і react-dom мають бути в external і в peerDependencies одночасно: перше не дає запекти їх у бандл, друге вимагає їх від споживача — без пари цих налаштувань у застосунку зʼявляються дві копії React.',
      'Прапорець sideEffects: false без винятку для CSS призводить до бібліотеки без стилів у продакшн-збірці: у розробці все працює, бо там викидання модулів вимкнене.',
      'Інструменти publint і are-the-types-wrong перевіряють коректність exports, типів і форматів пакета до публікації й ловлять найчастіші помилки на кшталт типів, невидимих у режимі node16.',
      'Tailwind у бібліотеці вимагає рішення наперед: або кіт постачає готовий CSS із власними класами, або публікує пресет, і тоді застосунок зобовʼязаний додати шляхи пакета в content, інакше класи вичистяться під час продакшн-збірки.',
    ],
  },
  {
    id: 'microfrontends',
    title: 'Мікрофронтенди: що це і як працюють',
    description:
      'Мікрофронтенди — підхід, коли великий фронтенд розбивають на частини, які розробляються й ДЕПЛОЯТЬСЯ незалежними командами, а в браузері збираються в один застосунок — аналогія мікросервісів для UI. Як працює технічно: є «оболонка» (shell/host), яка в рантаймі вантажить окремі частини (remotes). Способи інтеграції: Module Federation (Webpack 5/Vite) — застосунки обмінюються модулями в рантаймі, спільні бібліотеки (React) шаряться, щоб не вантажитись двічі; single-spa — роутер, що монтує/демонтує окремі застосунки; iframe — максимальна ізоляція, але погана інтеграція; серверна композиція (SSR). Навіщо: незалежні релізи багатьох команд, можливість різних стеків і поступової міграції легасі. Ціна: складна інфраструктура, дублювання залежностей, окремі проблеми спільного стану й дизайн-системи. Коротка відповідь: «незалежно деплойовані частини UI, які shell збирає в рантаймі; стандарт де-факто — Module Federation».',
    codeExamples: [
      {
        label: 'Module Federation (ідея)',
        code: `// remote (команда «Кошик») — віддає модуль назовні
new ModuleFederationPlugin({
  name: "cart",
  filename: "remoteEntry.js",
  exposes: { "./Cart": "./src/Cart" },
  shared: { react: { singleton: true } }, // одна копія React
});

// host (оболонка) — підключає в рантаймі
new ModuleFederationPlugin({
  remotes: { cart: "cart@https://cart.acme.com/remoteEntry.js" },
});
const Cart = React.lazy(() => import("cart/Cart"));`,
      },
    ],
    gotchas: [
      'Мікрофронтенди розвʼязують ОРГАНІЗАЦІЙНУ проблему (десятки команд); для однієї команди це чистий overhead.',
      'shared: { react: { singleton: true } } обовʼязковий — дві копії React ламають хуки й контекст.',
      'Спільний стан тримають мінімальним: URL, CustomEvent або маленький shared-стор — а не спільний Redux на всіх.',
      'Незалежні деплої означають: у проді зустрінуться версії, які разом не тестувалися — потрібні контракти й e2e.',
      'Без спільної дизайн-системи (окремий пакет) UI різних команд «розповзається».',
    ],
  },
]

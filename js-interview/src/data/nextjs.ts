import type { Topic } from '../types'

/*
 * ЯК ДОДАТИ НОВУ ТЕМУ NEXT.JS 👇
 * Скопіюйте блок нижче в кінець масиву (перед закривною дужкою ]).
 * Порядок тем задається масивом NEXTJS_ORDER у data/index.ts.
 *
 * {
 *   id: 'next-unikalnyi-id',
 *   title: 'Питання, як його ставить інтервʼюер',
 *
 *   // 🎯 показується ЗАВЖДИ, навіть у режимі «Повторення»
 *   definition:
 *     'Ідеальне визначення, яке мав би дати senior. 2-4 речення, готові до озвучення вголос: спершу суть одним реченням, далі механізм, далі наслідок.',
 *
 *   // ❓ показується тільки в режимі «З нуля»
 *   why:
 *     'Навіщо це взагалі існує і яку проблему вирішує. Це вхід у тему для того, хто бачить її вперше.',
 *
 *   // 💡 показується в режимах «Розібратись» і «З нуля»
 *   simple:
 *     'Пояснення простими словами — глибоко, але без складних слів. Пишіть суцільним текстом: сайт сам розібʼє на абзаци й підсвітить код у тексті.',
 *
 *   // 🔗 показується тільки в режимі «З нуля»
 *   related: [
 *     { title: 'Супутня тема', text: 'Коротке пояснення того, без чого основна тема не розкривається.' },
 *   ],
 *
 *   // 💻 показується в режимах «Розібратись» і «З нуля»
 *   codeExamples: [
 *     { label: 'Що показує приклад', code: `const x = 1;` },
 *   ],
 *
 *   // 🎖 показується ЗАВЖДИ
 *   seniorNotes: [
 *     'Нюанс, який відрізняє senior від middle.',
 *   ],
 * },
 */

export const nextjsTopics: Topic[] = [
  {
    id: 'next-what-is-and-why',
    title:
      'Що таке Next.js і які архітектурні проблеми він вирішує порівняно зі звичайним React SPA?',
    definition:
      'Next.js — це фреймворк поверх React, який додає файлову маршрутизацію, серверний рендеринг, статичну генерацію сторінок і збірку в єдиний оптимізований пакет із коробки, замість того щоб команда збирала цю інфраструктуру сама через react-router, webpack і власний сервер. Звичайний React SPA рендерить порожній HTML і будує весь інтерфейс у браузері через JavaScript, тому перше відображення повільне, а пошукові боти бачать майже порожню сторінку. Next.js вирішує це, віддаючи готовий HTML із сервера ще до того, як JavaScript довантажиться й виконає гідратацію.',
    why: 'До появи таких фреймворків команди самостійно збирали код-спліттинг, SSR-сервер на Express і кешування сторінок, повторюючи одну й ту саму інфраструктуру в кожному новому проєкті. Пошукова оптимізація й швидкість першого відображення вимагали серверного рендерингу, а сам React його не надавав.',
    simple:
      'Уяви два способи подати гостю обід: у SPA офіціант приносить порожню тарілку й рецепт, а гість сам готує страву на столі — довго і дивно виглядає, поки готування не завершиться. Next.js натомість готує страву на кухні заздалегідь і приносить уже готову тарілку, а рецепт лишає поруч на випадок, якщо гість захоче щось змінити. Технічно це означає, що компонент виконується на сервері під час обробки запиту, результат перетворюється на готовий HTML, і саме цей HTML браузер показує миттєво. Далі JavaScript довантажується у фоні й гідратує розмітку — навішує обробники подій на вже намальовані елементи, не перемальовуючи їх заново. Маршрутизація теж вбудована: файл у папці app чи pages стає адресою сайту без окремого налаштування react-router. Розбиття коду на чанки за сторінками й оптимізація зображень чи шрифтів також відбуваються автоматично під час збірки. Найчастіша помилка новачків — думати, що Next.js замінює React: насправді це надбудова, яка керує тим, коли і де виконується той самий React-код.',
    related: [
      {
        title: 'Гідратація',
        text: 'Процес, коли клієнтський JavaScript навішує обробники подій на вже готову серверну розмітку HTML, а не малює інтерфейс з нуля; без цього незрозуміло, чому Next.js швидший за SPA при першому завантаженні, але однаково потребує JavaScript для інтерактивності.',
      },
      {
        title: 'SSR проти SSG',
        text: 'Next.js дозволяє окремо для кожної сторінки обрати, чи рендерити HTML на кожен запит, чи один раз під час збірки; це ключова відмінність від фреймворків, де весь сайт рендериться одним і тим самим способом.',
      },
      {
        title: 'Vite та Create React App',
        text: 'Інструменти для збірки клієнтського SPA без серверного рендерингу з коробки; порівняння з ними показує, що саме Next.js додає зверху — маршрутизацію, SSR і роботу з даними на сервері.',
      },
    ],
    codeExamples: [
      {
        label: 'Дані готові ще до першого рендера',
        code: `// app/products/page.tsx — Server Component за замовчуванням
export default async function ProductsPage() {
  // ❌ у SPA: useEffect + fetch, порожній екран до відповіді сервера
  // ✅ у Next.js: fetch виконується на сервері до відправки HTML браузеру
  const res = await fetch('https://api.shop.com/products')
  const products = await res.json()

  return <ProductList items={products} />
}`,
      },
      {
        label: 'Файл стає маршрутом без react-router',
        code: `// app/blog/[slug]/page.tsx
// URL /blog/hello-world автоматично рендерить цей файл,
// параметр slug приходить готовим у props без ручного налаштування роутера
export default function PostPage({ params }: { params: { slug: string } }) {
  return <Article slug={params.slug} />
}`,
      },
    ],
    seniorNotes: [
      'Next.js побудований поверх React, тому нові можливості самого React — наприклад Server Components — спершу зʼявляються саме тут, а не в Vite чи Create React App.',
      'SEO-перевага стосується не всього застосунку, а конкретно SSR- і SSG-сторінок: сторінка, що повністю залежить від клієнтського fetch у Client Component, для пошукового бота виглядає так само порожньою, як SPA.',
      'React-документація прибрала Create React App зі списку рекомендованих способів почати новий проєкт, пропонуючи натомість фреймворки на кшталт Next.js чи інструменти на кшталт Vite.',
      'Файлова маршрутизація не означає відсутність конфігурації: динамічні сегменти на кшталт [id], паралельні маршрути й перехоплення маршрутів — це все ще окреме API, яке треба знати.',
      'Гідратація вимагає, щоб серверний і клієнтський рендер дали однаковий HTML; розбіжність, наприклад через виклик Date.now() прямо в розмітці, кидає попередження про hydration mismatch у консолі.',
    ],
  },

  {
    id: 'next-app-vs-pages-router',
    title: 'Які ключові відмінності між App Router та Pages Router?',
    definition:
      'App Router — це маршрутизація на основі каталогу app, де кожен маршрут описується файлом page.tsx у своїй папці, а компоненти за замовчуванням є серверними, тобто React Server Components. Pages Router — це попередня система на основі каталогу pages, де кожен файл одразу є клієнтським компонентом, а дані завантажуються через окремі функції на кшталт getServerSideProps чи getStaticProps. Головна відмінність не в розташуванні файлів, а в моделі рендерингу: App Router дозволяє змішувати серверні й клієнтські компоненти в одному дереві, а Pages Router рендерить кожну сторінку одним і тим самим способом цілком.',
    why: 'Pages Router рендерив кожну сторінку єдиним блоком і відправляв у браузер весь її JavaScript цілком, навіть якщо інтерактивною була лише одна кнопка. App Router зʼявився, щоб дозволити серверу робити важку роботу з даними без відправки зайвого JavaScript клієнту.',
    simple:
      'Уяви редакцію журналу: у Pages Router кожна стаття — це суцільний текст, який редактор пише і повністю передає читачеві разом з усіма підготовчими матеріалами. В App Router статтю можна розбити на частини: основний текст редактор готує на місці й одразу показує читачеві, а інтерактивні вставки на кшталт коментарів чи голосувань позначені окремо і довантажуються вже в браузері. Технічно кожен компонент у папці app за замовчуванням виконується на сервері, не потрапляє в клієнтський бандл і не має доступу до useState чи onClick. Щойно компоненту потрібна інтерактивність, на початку файлу ставлять директиву use client, і саме з цього місця вниз по дереву компоненти стають клієнтськими та потрапляють у JavaScript-бандл. У Pages Router такого розділення немає: getServerSideProps виконується на сервері, а сам компонент сторінки завжди рендериться в браузері, тому дані підвантажуються окремо від самого компонента. Це й пояснює, чому перехід з Pages на App Router — не перейменування папок, а переосмислення того, який код узагалі має опинитися в браузері.',
    related: [
      {
        title: 'React Server Components',
        text: 'Компоненти, що виконуються тільки на сервері, ніколи не потрапляють у клієнтський бандл і не можуть використовувати хуки на кшталт useState; саме вони — фундамент, на якому побудований App Router.',
      },
      {
        title: 'Директива use client',
        text: 'Позначка на початку файлу, яка перетворює компонент і все, що він імпортує далі по дереву, на клієнтський код; без неї useState чи onClick у файлі App Router викличуть помилку збірки.',
      },
      {
        title: 'getStaticPaths і generateStaticParams',
        text: 'Старий і новий спосіб заздалегідь перелічити динамічні маршрути для статичної генерації; перехід між ними — типова заминка під час міграції з Pages на App Router.',
      },
    ],
    codeExamples: [
      {
        label: 'Той самий маршрут у двох моделях',
        code: `// Pages Router: pages/products/[id].tsx
export async function getServerSideProps({ params }) {
  const product = await getProduct(params.id)
  return { props: { product } } // дані окремо від компонента
}
export default function ProductPage({ product }) {
  return <Details product={product} />
}

// App Router: app/products/[id]/page.tsx
export default async function ProductPage({ params }) {
  const product = await getProduct(params.id) // дані прямо в компоненті
  return <Details product={product} />
}`,
      },
      {
        label: 'Межа між серверним і клієнтським кодом',
        code: `// app/cart/CartButton.tsx
'use client' // ❌ без цього рядка useState впаде з помилкою збірки

import { useState } from 'react'

export function CartButton() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(count + 1)}>{count}</button>
}`,
      },
    ],
    seniorNotes: [
      'Каталог app зʼявився в Next.js 13 як експериментальна можливість і став стабільним у версії 13.4; Server Actions стабілізувались пізніше, у Next.js 14.',
      'Pages Router офіційно не застарілий і підтримується разом з App Router у тому самому проєкті одночасно — обидва каталоги можуть співіснувати під час поступової міграції.',
      'У Pages Router спільний layout для всіх сторінок описувався компонентом _app.tsx, який перемонтовувався на кожен перехід; layout.tsx в App Router лишається змонтованим між переходами й не втрачає свій внутрішній стан.',
      'generateStaticParams замінює getStaticPaths і не потребує окремого fallback-прапорця: поведінка для маршрутів, не переданих у список, керується через dynamicParams у тому самому файлі.',
      'Middleware працює однаково в обох моделях маршрутизації, бо виконується на рівні Edge Runtime ще до того, як Next.js визначив, App Router чи Pages Router оброблятиме конкретний запит.',
    ],
  },

  {
    id: 'next-build',
    title: 'Що відбувається під час виконання next build?',
    definition:
      'Команда next build компілює TypeScript і JSX у продакшн-бандли, розбиті окремими чанками для кожного маршруту, і одночасно аналізує кожну сторінку, щоб визначити, можна відрендерити її один раз під час збірки, чи вона потребує серверного рендерингу на кожен запит. Статичні сторінки одразу виконуються й зберігаються як готовий HTML у папці .next, а динамічні лишаються функціями, які виконаються пізніше, коли прийде реальний запит. Паралельно Next.js перевіряє типи через tsc і формує build-маніфест із мапою маршрутів на файли, який потім читає сервер під час старту.',
    why: 'Без окремого кроку збірки Next.js довелося б вирішувати на кожен запит, статична сторінка перед ним чи динамічна, і компілювати код на льоту, що робило б продакшн повільним і непередбачуваним. next build переносить цю роботу наперед, тому сервер під час запитів лише віддає готові результати чи виконує заздалегідь підготовлені функції.',
    simple:
      'Уяви, що ресторан готує частину страв заздалегідь вранці, а частину — лише на замовлення ввечері, і вивішує на кухні список: яка страва вже в холодильнику готова, а яка готується на місці. next build робить те саме для сторінок. Спочатку компілятор перетворює весь код на JavaScript, придатний для Node.js і браузера, і розбиває його на окремі файли за маршрутами, щоб браузер не завантажував код сторінок, які користувач не відкривав. Далі для кожного маршруту Next.js статично аналізує код: якщо сторінка не читає cookies, headers чи інші дані конкретного запиту, вона позначається як статична і одразу рендериться в готовий HTML-файл прямо під час збірки. Якщо сторінка звертається до даних запиту, вона лишається серверною функцією, яка виконається лише тоді, коли прийде реальний запит користувача. Наприкінці в консоль виводиться таблиця маршрутів із позначками типу рендерингу для кожного і розміром JavaScript-бандла, за яким видно, які сторінки варто полегшити. Результат усього процесу складається в папку .next, яку потім запускає next start, або яку платформа деплою розкладає по окремих серверних функціях і CDN.',
    related: [
      {
        title: 'Статичний проти динамічного рендерингу',
        text: 'Рішення, ухвалене автоматично для кожного маршруту під час збірки на основі того, чи звертається код до даних конкретного запиту; саме воно визначає, потрапить сторінка в готовий HTML чи лишиться серверною функцією.',
      },
      {
        title: 'Incremental Static Regeneration',
        text: 'Механізм, який дозволяє статичній сторінці оновлюватися після збірки за розкладом чи на вимогу, не вимагаючи повного next build заново для всього сайту.',
      },
      {
        title: "output: 'standalone'",
        text: 'Налаштування в next.config.js, яке під час збірки збирає мінімальний серверний бандл із лише потрібними залежностями — зручно для контейнерів Docker, де зайві node_modules збільшують образ.',
      },
    ],
    codeExamples: [
      {
        label: 'Сторінка примусово стає динамічною',
        code: `// app/dashboard/page.tsx
// ❌ якби Next.js не бачив звернення до cookies, сторінку могло б
// помилково позначити статичною і закешувати для всіх користувачів
import { cookies } from 'next/headers'

export default async function DashboardPage() {
  const session = cookies().get('session')
  const data = await getUserData(session?.value)
  return <Dashboard data={data} />
}`,
      },
      {
        label: 'Явна відмова від статичної генерації',
        code: `// app/live-prices/page.tsx
export const dynamic = 'force-dynamic' // ✅ сторінка виконується на кожен запит

export default async function LivePrices() {
  const prices = await fetchLatestPrices()
  return <PriceTable prices={prices} />
}`,
      },
    ],
    seniorNotes: [
      'Виклик cookies() чи headers() усередині Server Component автоматично переводить увесь маршрут у динамічний режим, навіть якщо решта коду сторінки не залежить від запиту.',
      "Опція output: 'standalone' у next.config.js аналізує реальні залежності через трасування файлів і копіює в результат лише ті node_modules, які справді використовуються під час виконання.",
      'Якщо typescript.ignoreBuildErrors не увімкнено в конфігурації, next build завершується з помилкою при першій-ліпшій помилці типів, навіть якщо застосунок технічно міг би запуститися.',
      'Для динамічного маршруту в App Router без output: "export" відсутність generateStaticParams не ламає збірку — сторінка просто лишається повністю серверною і рендериться на кожен запит.',
      'Розмір JavaScript-бандла в таблиці виводу build показує окремо First Load JS для кожного маршруту, тому саме цю таблицю дивляться першою під час пошуку причини роздутого бандла.',
    ],
  },

  {
    id: 'next-execution-phases',
    title:
      'Які частини Next.js виконуються під час build time, request time та в браузері?',
    definition:
      'У Next.js той самий код може виконуватися в різний час залежно від типу маршруту. Під час build time виконуються статичні Server Components і generateStaticParams для сторінок без динамічних даних запиту — їхній результат зберігається як готовий HTML. Під час request time на сервері виконуються middleware, динамічні Server Components, які читають cookies чи headers, і Route Handlers — усе, що неможливо порахувати заздалегідь. У браузері виконуються лише Client Components: код із директивою use client, хуки на кшталт useState і обробники подій, які на сервері взагалі не існують.',
    why: 'Плутанина між цими трьома фазами — головна причина помилок на кшталт window is not defined чи спроби прочитати cookies у клієнтському компоненті: код, написаний для одного середовища, просто не має там потрібного йому оточення.',
    simple:
      'Уяви завод із трьома цехами, які працюють у різний час доби. Нічна зміна, це build time, виготовляє деталі наперед і складає їх на склад — відбувається один раз, коли запускають next build, і стосується лише сторінок, яким не потрібні свіжі дані на кожне замовлення. Ранкова зміна, request time, працює на сервері щоразу, коли надходить реальне замовлення: вона читає cookies, заголовки, параметри конкретного запиту й нічого не може порахувати заздалегідь, бо цих даних просто не існувало до запиту. Вечірня зміна, браузер, отримує вже зібраний виріб і докручує до нього рухомі частини: клієнтські компоненти виконуються лише в браузері користувача й мають доступ до window, document і подій кліку, яких на сервері фізично немає. Одна й та сама функція не може одночасно належати двом цехам: якщо серверний компонент намагається викликати useState, збірка впаде з помилкою, а клієнтський компонент не має прямого доступу до серверних функцій на кшталт cookies. Route Handler у файлі route.ts — окремий випадок серверного коду, що виконується на request time завжди, незалежно від того, статична решта сайту чи ні. Розуміння цього поділу і є відповіддю на питання, де саме виконається конкретний рядок коду, — питання, яке в звичайному React SPA взагалі не мало сенсу, бо там усе виконувалося тільки в браузері.',
    related: [
      {
        title: 'generateStaticParams',
        text: 'Функція, яка під час build time повертає список значень динамічного сегмента маршруту, щоб Next.js заздалегідь згенерував HTML для кожного з них замість того, щоб рендерити їх під час запиту.',
      },
      {
        title: 'Route Handlers',
        text: 'Файли route.ts у каталозі app, що описують серверні API-ендпоінти; вони завжди виконуються на request time, навіть якщо решта сайту повністю статична.',
      },
      {
        title: 'Динамічні функції cookies() і headers()',
        text: 'Виклик будь-якої з них усередині Server Component автоматично переводить маршрут у динамічний режим, бо ці дані фізично існують лише в момент запиту, а не під час збірки.',
      },
    ],
    codeExamples: [
      {
        label: 'Три фази в межах одного маршруту',
        code: `// app/articles/[slug]/page.tsx
// 1. build time: список slug для попередньої генерації
export async function generateStaticParams() {
  const articles = await getArticleSlugs()
  return articles.map((slug) => ({ slug }))
}

// 2. request time на сервері (лише для slug поза списком вище)
export default async function ArticlePage({ params }) {
  const article = await getArticle(params.slug)
  return <ArticleBody article={article} />
}

// 3. у браузері: окремий Client Component усередині дерева
// 'use client'
// export function LikeButton() { const [liked, setLiked] = useState(false); ... }`,
      },
      {
        label: 'Код, що зʼявляється в неправильній фазі',
        code: `// ❌ window у Server Component — там немає браузерного оточення
export default function Banner() {
  const width = window.innerWidth // впаде на сервері
  return <div>{width}</div>
}

// ✅ доступ до window переносять у Client Component з useEffect
'use client'
import { useEffect, useState } from 'react'
export function Banner() {
  const [width, setWidth] = useState(0)
  useEffect(() => setWidth(window.innerWidth), [])
  return <div>{width}</div>
}`,
      },
    ],
    seniorNotes: [
      'У Next.js 13 і 14 fetch без явних опцій кешувався за замовчуванням; у Next.js 15 поведінку змінили на протилежну — без cache: "force-cache" запит за замовчуванням не кешується.',
      'Змінні оточення без префікса NEXT_PUBLIC_ доступні лише на build time і request time на сервері: при спробі використати їх у Client Component їхнє значення буде undefined у зібраному бандлі.',
      'Client Component попри назву все одно один раз рендериться на сервері для початкового HTML, якщо його не імпортовано з опцією ssr: false; "лише в браузері" стосується подальших ререндерів і подій, а не першого відображення.',
      'Route Handler з методом GET без звернень до динамічних даних кешується так само, як статична сторінка, доки в ньому явно не встановлено export const dynamic = "force-dynamic".',
      'Middleware виконується на Edge Runtime з урізаним набором Node.js API: у ньому недоступні прямі звернення до файлової системи чи більшість нативних модулів Node.',
    ],
  },

  {
    id: 'next-request-lifecycle',
    title:
      'Поясни повний життєвий цикл HTTP-запиту в Next.js: від браузера до готового інтерактивного UI',
    definition:
      'Запит спершу проходить через middleware, яке може переписати чи заблокувати маршрут ще до рендерингу сторінки. Далі Next.js визначає потрібний сегмент за структурою папки app, виконує серверні компоненти цього дерева — включно з layout і page — і звертається до джерел даних через fetch із власним кешем. Результат рендериться в HTML і потоково, через Suspense, відправляється браузеру разом із серіалізованим деревом компонентів. Браузер спершу показує статичний HTML, а щойно довантажиться клієнтський бандл — гідратує лише ті частини дерева, що позначені як Client Component.',
    why: 'Розуміння цього циклу пояснює, чому одні частини сторінки зʼявляються миттєво, а інші — із затримкою, і де шукати причину повільного першого байта відповіді чи зайвого JavaScript у бандлі. Без нього оптимізація зводиться до навмання розставлених use client замість усвідомленого рішення, що саме має виконуватися на сервері, а що — в браузері.',
    simple:
      'Уяви естафету з чотирьох етапів, де кожен наступний бігун стартує, щойно отримає естафетну паличку від попереднього, не чекаючи, поки той добіжить до фінішу. Перший етап, middleware, виконується на межі мережі ще до того, як Next.js почав щось рендерити, і може одразу відправити редирект чи 404, не витрачаючи час на решту роботи. Другий етап: сервер підбирає потрібний layout і page за структурою папок і виконує їхній код; якщо компонент асинхронний і робить await fetch, Next.js не блокує всю сторінку, а обгортає цю частину в Suspense і продовжує рендерити решту дерева. Третій етап: HTML тече до браузера частинами — спочатку видно оболонку сторінки, а секції, що чекають на дані, заповнюються, щойно їхній fetch завершиться, без перезавантаження сторінки. Разом із HTML браузер отримує серіалізоване дерево компонентів — опис того, які з них клієнтські і якими пропсами їх ініціалізувати. Четвертий етап настає, коли довантажується JavaScript-бандл: React не перемальовує вже показаний HTML, а лише навішує обробники подій на елементи, позначені як Client Component, — саме це і є гідратація. Найчастіша помилка — думати, що вся сторінка чекає на найповільніший запит: насправді межі Suspense дозволяють швидким частинам зʼявитися одразу, а повільним підʼїхати трохи пізніше.',
    related: [
      {
        title: 'Streaming SSR і Suspense',
        text: 'Механізм, що дозволяє відправляти HTML частинами замість очікування на весь рендер сторінки цілком; кожна межа Suspense стає окремим фрагментом, який зʼявляється у браузері, щойно готовий.',
      },
      {
        title: 'Middleware та Edge Runtime',
        text: 'Код, що виконується на межі мережі, географічно ближче до користувача, ще до того, як запит дійшов до основного сервера; звідси його обмежений набір доступних Node.js API.',
      },
      {
        title: 'Гідратація',
        text: 'Завершальний крок циклу, коли клієнтський React навішує обробники подій на вже готову розмітку, а не перемальовує сторінку з нуля; без гідратації клієнтські компоненти лишились би неінтерактивними.',
      },
    ],
    codeExamples: [
      {
        label: 'Middleware як перший етап циклу',
        code: `// middleware.ts
import { NextResponse } from 'next/server'

export function middleware(request) {
  const isLoggedIn = request.cookies.has('session')
  // ❌ без цієї перевірки запит дійде аж до рендерингу сторінки й лише
  // тоді зʼясується, що користувач не авторизований
  if (!isLoggedIn && request.nextUrl.pathname.startsWith('/account')) {
    return NextResponse.redirect(new URL('/login', request.url))
  }
  return NextResponse.next()
}`,
      },
      {
        label: 'Suspense розбиває рендер на потокові фрагменти',
        code: `// app/dashboard/page.tsx
import { Suspense } from 'react'

export default function DashboardPage() {
  return (
    <div>
      <Header /> {/* рендериться й показується одразу */}
      <Suspense fallback={<Spinner />}>
        {/* цей фрагмент долітає в HTML окремо, коли дані готові */}
        <SlowRevenueChart />
      </Suspense>
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Файл loading.tsx у сегменті маршруту автоматично створює межу Suspense навколо всього page.tsx цього сегмента — окремо обгортати компонент вручну не потрібно.',
      'Час до першого байта відповіді залежить від найповільнішого fetch поза межею Suspense: усе, що не обгорнуте в Suspense, блокує відправку початкового HTML.',
      'Послідовні await у різних компонентах без Promise.all створюють request waterfall — кожен наступний fetch стартує лише після завершення попереднього, хоча міг би виконуватися паралельно.',
      'RSC-корисне навантаження, яке приходить разом з HTML, це не той самий JSON, що повертає звичайний REST-ендпоінт: це власний серіалізований формат React для опису дерева серверних компонентів.',
      'Middleware виконується для кожного запиту, що підпадає під його matcher, включно із запитами на статичні файли, якщо matcher не звужено явно, — це поширена причина непомітного сповільнення всього сайту.',
    ],
  },

  {
    id: 'next-large-app-architecture',
    title: 'Як організувати архітектуру великого Next.js-застосунку з кількома командами?',
    definition:
      'Великий Next.js-застосунок з кількома командами найчастіше організовують як монорепозиторій, де сам Next.js-застосунок — тонкий шар маршрутизації й композиції, а бізнес-логіка й дизайн-система винесені в окремі пакети. Команди володіють не окремими сторінками, а пакетами: наприклад пакетом каталогу товарів чи пакетом оплати, і підключають їх у потрібні маршрути через звичайний імпорт. Спільні речі — UI-кіт, конфігурація лінтера, типи API — живуть в одному місці, щоб зміна не розповзалася копіями по кожній команді окремо.',
    why: 'Без чіткого поділу великий каталог app перетворюється на спільний простір, де компоненти різних команд переплутуються й будь-яка зміна в одному розділі випадково ламає інший. Межа на рівні окремих пакетів дає командам змогу деплоїти й тестувати свою частину незалежно від інших.',
    simple:
      'Уяви торговий центр, де кожен орендар має власну крамницю зі своїм персоналом, а спільні речі — освітлення, охорону, вхідні двері — обслуговує адміністрація центру, а не кожен магазин окремо. У Next.js-монорепозиторії роль адміністрації виконує кореневий застосунок: він відповідає лише за маршрутизацію, layout і збірку, а логіку конкретного розділу — каталог товарів, кошик, профіль користувача — пишуть окремі команди у власних пакетах, наприклад packages/catalog чи packages/checkout. Сторінка в каталозі app стає тонким файлом, який імпортує компонент із потрібного пакета і майже не містить власної логіки. Спільні речі — дизайн-система, типи, клієнт для звернень до бекенду — виносять в окремі пакети на кшталт packages/ui чи packages/api-client, щоб їх не копіювали в кожній команді по-своєму. Для цього використовують інструменти монорепозиторію на кшталт Turborepo чи Nx, які вміють кешувати збірку лише змінених пакетів, а не перезбирати весь застосунок через правку в одному компоненті. Межі власності описують у CODEOWNERS, щоб кожна зміна в пакеті проходила ревʼю саме тієї команди, яка ним володіє. Найскладніша частина тут не технічна, а організаційна: визначити межі пакетів так, щоб вони збігалися з межами відповідальності команд, а не з випадковим поділом файлів.',
    related: [
      {
        title: 'Монорепозиторій і Turborepo',
        text: 'Інструмент, що керує залежностями між пакетами одного репозиторію й кешує результати збірки та тестів по кожному пакету окремо, щоб зміна в одній команді не перезапускала все з нуля.',
      },
      {
        title: 'Мікрофронтенди',
        text: 'Альтернативний підхід, коли команди деплоять свої частини інтерфейсу повністю незалежно одна від одної, а не збирають спільний застосунок разом; обирають його, коли монорепозиторію з пакетами вже недостатньо.',
      },
      {
        title: 'transpilePackages',
        text: 'Налаштування в next.config.js, без якого Next.js не компілює вихідний TypeScript чи ESM-код внутрішніх пакетів робочого простору, а очікує вже зібраний JavaScript.',
      },
    ],
    codeExamples: [
      {
        label: 'Сторінка як тонкий шар над пакетом команди',
        code: `// apps/web/app/checkout/page.tsx
import { CheckoutFlow } from '@shop/checkout' // пакет команди Checkout

export default function CheckoutPage() {
  // сторінка не містить бізнес-логіки — лише підключає пакет
  return <CheckoutFlow />
}`,
      },
      {
        label: 'Структура монорепозиторію',
        code: `apps/
  web/                 # сам Next.js-застосунок: маршрутизація й layout
    app/
packages/
  catalog/             # команда Catalog: логіка й компоненти каталогу
  checkout/            # команда Checkout: логіка й компоненти оплати
  ui/                  # спільна дизайн-система
  api-client/          # спільний типізований клієнт до бекенду`,
      },
    ],
    seniorNotes: [
      'Turborepo підтримує remote caching: результат збірки, зроблений на одній CI-машині, можуть повторно використати інші машини й розробники, не перезбираючи незмінені пакети.',
      'next.config.js потребує явного transpilePackages зі списком внутрішніх пакетів, якщо ці пакети публікуються як вихідний TypeScript- чи ESM-код без попередньої компіляції.',
      'eslint-plugin-boundaries дозволяє заборонити пакету однієї команди імпортувати внутрішні файли пакета іншої команди напряму, змушуючи ходити лише через публічний вхідний файл пакета.',
      'Якщо командам потрібні справді незалежні деплої без спільної збірки, замість монорепозиторію використовують Module Federation через спільнотні плагіни для Next.js, а не вбудовані засоби фреймворка.',
      'Vercel підтримує монорепозиторії нативно: для кожного застосунку в apps/ налаштовують окремий проєкт із власним Root Directory, і зміна в одному застосунку не тригерить деплой інших.',
    ],
  },

  {
    id: 'next-bff',
    title: 'Які переваги та недоліки використання Next.js як Backend for Frontend?',
    definition:
      'Next.js може виконувати роль BFF через Route Handlers і Server Actions: сторінка звертається не напряму до мікросервісів, а до свого ж серверного шару в тому самому застосунку, який агрегує дані, ховає внутрішні API-ключі й адаптує відповідь під конкретний екран. Перевага в тому, що фронтенд і його BFF розробляються, тестуються й деплояться разом, без окремого сервісу й окремої команди. Недолік — цей шар виконується в тому самому процесі, що й рендеринг сторінок, тому важке навантаження на BFF-ендпоінти може впливати на швидкість самого сайту.',
    why: 'Без BFF клієнт або звертається напряму до кількох мікросервісів і сам збирає дані з них, розкриваючи їхні адреси й формати в браузері, або команда підтримує окремий BFF-сервіс, що подвоює розгортання й код. Вбудований BFF-шар у Next.js закриває цю прогалину без окремого сервісу.',
    simple:
      'Уяви офіціанта в ресторані, який приймає одне замовлення від гостя, а сам ходить на кухню, до бару й до комори, щоб зібрати все потрібне, а потім приносить готову тарілку. Роль офіціанта в Next.js виконує Route Handler чи Server Action: браузер робить один запит до свого ж сервера, а той усередині звертається до кількох мікросервісів, обʼєднує відповіді і повертає рівно ту форму даних, яка потрібна конкретному екрану. Це знімає з клієнта потребу знати адреси внутрішніх сервісів, зберігати секретні токени чи робити кілька запитів послідовно з ризиком request waterfall. Оскільки BFF-код лежить у тому самому репозиторії й на тому самому деплої, що й UI, зміну форми даних для нового екрана роблять в одному пул-реквесті, а не координують два окремі сервіси й два реліз-цикли. Зворотний бік — BFF-логіка ділить ресурси процесу з рендерингом сторінок: важкий агрегаційний запит чи повільний зовнішній сервіс можуть підняти час відповіді для всього сервера, а не лише для цього ендпоінта, якщо навантаження не винести на окремий рантайм. Ще один компроміс — такий BFF привʼязаний до життєвого циклу фронтенд-деплою: якщо кілька застосунків на різних фреймворках потребують той самий агрегований API, доводиться або дублювати логіку, або таки виносити її в окремий сервіс.',
    related: [
      {
        title: 'Route Handlers',
        text: 'Файли route.ts у каталозі app, що описують серверні ендпоінти з повним контролем над методом, заголовками й статусом відповіді; саме вони найчастіше виконують роль BFF-шару.',
      },
      {
        title: 'Server Actions',
        text: 'Серверні функції, які клієнтський компонент може викликати напряму, наче звичайну функцію, без ручного написання fetch і ендпоінта; підходять для мутацій даних, а не для довільної агрегації відповідей.',
      },
      {
        title: 'Архітектура мікросервісів',
        text: 'Модель, де бізнес-логіка розбита на незалежні сервіси з власними базами даних; BFF існує саме для того, щоб фронтенд не звертався до кожного з них окремо і не знав про їхню внутрішню структуру.',
      },
    ],
    codeExamples: [
      {
        label: 'Route Handler агрегує кілька сервісів в один виклик',
        code: `// app/api/dashboard-summary/route.ts
export async function GET() {
  // ❌ без BFF: браузер робив би три окремі запити до трьох сервісів
  const [orders, invoices, user] = await Promise.all([
    fetch('https://orders.internal/api').then((r) => r.json()),
    fetch('https://billing.internal/api').then((r) => r.json()),
    fetch('https://users.internal/api').then((r) => r.json()),
  ])

  // ✅ клієнт отримує один запит і рівно ту форму даних, яка йому потрібна
  return Response.json({ orders, invoices, userName: user.name })
}`,
      },
      {
        label: 'Server Action ховає секретний ключ від клієнта',
        code: `// app/actions.ts
'use server'

export async function createOrder(formData: FormData) {
  // секретний ключ ніколи не потрапляє в клієнтський бандл
  const res = await fetch('https://orders.internal/api', {
    method: 'POST',
    headers: { Authorization: \`Bearer \${process.env.ORDERS_API_KEY}\` },
    body: formData,
  })
  return res.json()
}`,
      },
    ],
    seniorNotes: [
      'Server Actions за замовчуванням перевіряють заголовок Origin і відхиляють виклик із чужого домену — вбудований захист від CSRF, який не потрібно писати вручну.',
      'Route Handler, розгорнутий на Vercel, за замовчуванням працює як окрема serverless-функція з власним холодним стартом, якщо в ньому явно не вказано export const runtime = "edge".',
      'Секрети, використані лише у Route Handler чи Server Action без директиви use client, ніколи не потрапляють у клієнтський бандл на відміну від випадкового імпорту такого модуля в Client Component.',
      'Важка агрегація в Route Handler без кешування напряму збільшує Time To First Byte сторінки, що його викликає, бо запит до BFF блокує рендер, доки не отримає відповідь від усіх залежних сервісів.',
      'Якщо BFF-логіку потрібно повторно використати в мобільному застосунку чи іншому фронтенді, Route Handlers Next.js для цього не підходять напряму — вони приватні для цього конкретного застосунку.',
    ],
  },

  {
    id: 'next-when-not-to-use',
    title: 'У яких випадках Next.js буде поганим вибором для нового проєкту?',
    definition:
      'Next.js — гірший вибір, коли застосунок не потребує SEO чи серверного рендерингу і є суто інтерактивним інструментом за авторизацією: дашборд, адмінка, редактор однаково рендеряться лише в браузері, а серверний шар Next.js додає складність без користі. Він також не підходить, коли команда вже має готовий бекенд і потребує лише простого статичного сайту: важча збірка Next.js не виправдана перед звичайним SPA на Vite чи статичним генератором. Нарешті, Next.js погано пасує проєктам, яким потрібен довгоживучий сервер із власним станом у памʼяті, бо серверна модель Next.js розрахована на короткоживучі безстанові функції.',
    why: 'Додаткова складність Next.js — серверний рантайм, кешування fetch, правила поділу на Server і Client Components — коштує реальних годин розробки й дебагу. Якщо застосунку не потрібен жоден із наслідків цієї складності, вона стає чистим накладним видатком, а не перевагою.',
    simple:
      'Уяви, що купуєш вантажівку, щоб їздити на роботу за два квартали: вона впорається із завданням, але носитиме з собою вантажопідйомність, витрату пального й складність паркування, які насправді не потрібні. Так само Next.js несе із собою серверний рантайм, правила кешування fetch, поділ на Server і Client Components — усе це виправдане, коли є реальна потреба в SEO, швидкому першому відображенні чи серверній агрегації даних. Якщо застосунок ховається за логіном і працює як внутрішній інструмент, пошукові боти його ніколи не побачать, а перше відображення не критичне, бо користувач і так чекає на завантаження свого робочого простору. У такому разі простий SPA на Vite з react-router буде легшим у налаштуванні, швидшим для локальної розробки і не вимагатиме розуміння того, який код виконується на сервері, а який у браузері. Якщо ж проєкт — простий маркетинговий сайт без інтерактивності, повноцінний Next.js теж надлишковий: статичний генератор чи навіть чистий HTML дадуть той самий результат дешевше і простіше. Ще один сигнал — потреба в довгоживучому зʼєднанні чи стані сервера, наприклад вебсокет-сервер із власною чергою в памʼяті: серверна модель Next.js розрахована на короткі безстанові виклики, і для таких задач природніше підходить окремий Node.js- чи Go-сервіс, а Next.js лишити тільки для вебінтерфейсу над ним.',
    related: [
      {
        title: 'Vite та SPA без SSR',
        text: 'Простіша альтернатива для застосунків без потреби в SEO чи серверному рендерингу: швидший локальний старт, менше конфігурації, немає поділу коду на серверний і клієнтський.',
      },
      {
        title: 'Внутрішні інструменти за авторизацією',
        text: 'Дашборди й адмінки, недоступні пошуковим ботам за визначенням; переваги серверного рендерингу для SEO для них не мають сенсу, а частина складності Next.js лишається невикористаною.',
      },
      {
        title: 'Довгоживучі сервери й вебсокети',
        text: 'Задачі, де потрібен процес із постійним станом у памʼяті чи тривалим зʼєднанням; serverless-модель, на яку розрахований Next.js на типових платформах деплою, для них погано підходить.',
      },
    ],
    codeExamples: [
      {
        label: 'Коли Next.js зводиться до SPA, але з накладними витратами',
        code: `// Внутрішня адмінка за логіном:
// force-dynamic вимикає кешування й статичну генерацію на кожній сторінці —
// у результаті Next.js поводиться як звичайний SPA, але з накладними
// витратами серверного рантайму на кожен запит
export const dynamic = 'force-dynamic'

export default async function AdminDashboard() {
  const data = await fetchInternalData() // виконується на кожен запит
  return <Dashboard data={data} />
}

// ✅ тут простіше: SPA на Vite з react-query, без серверного шару взагалі`,
      },
    ],
    seniorNotes: [
      'Route Handlers на Vercel деплояться як serverless-функції з холодним стартом; для внутрішнього API з постійним навантаженням довгоживучий Node.js-сервер на кшталт Fastify без холодних стартів часто передбачуваніший.',
      'generateStaticParams і кешування fetch не дають переваги для сторінок за автентифікацією, бо виклик cookies() автоматично переводить маршрут у динамічний режим на кожен запит.',
      'Статичні генератори для суто контентних сайтів без інтерактивності виводять менше JavaScript у браузер, бо не підключають React-рантайм на кожну сторінку за замовчуванням.',
      'WebSocket-сервер із довгоживучим станом у памʼяті погано лягає на serverless-модель типової платформи деплою Next.js, де інстанс функції можуть завершити між запитами.',
      'Команда без досвіду в React чи TypeScript витрачає час на вивчення різниці між Server і Client Components ще до першої фічі, тоді як звичний SPA цього кроку не вимагає.',
    ],
  },

  {
    id: 'next-ssr-ssg-isr-csr',
    title: 'Яка різниця між SSR, SSG, ISR та CSR?',
    definition:
      'SSR рендерить HTML на сервері окремо для кожного запиту, тому дані завжди свіжі, але кожен візит платить часом виконання рендера. SSG рендерить HTML один раз під час збірки і віддає той самий готовий файл усім користувачам — найшвидше, але дані застарівають до наступної збірки. ISR — це SSG з періодом ревалідації: сторінка лишається статичною між оновленнями, а Next.js регенерує її у фоні через заданий інтервал без повного перезапуску білду. CSR не рендерить розмітку на сервері взагалі: браузер отримує майже порожній HTML і будує весь інтерфейс через JavaScript після виконання fetch-запитів.',
    why: 'Різні сторінки одного застосунку мають різні вимоги: блог-стаття майже не змінюється і виграє від SSG, панель курсів валют має бути свіжою на кожен запит і потребує SSR, а каталог товарів з тисячами позицій не можна перебудовувати повністю через кожну зміну ціни — саме для нього придумали ISR.',
    simple:
      'Уяви чотири способи донести новини до читача. SSG — це друкована газета: номер зверстали один раз вранці, і всі читачі протягом дня отримують той самий текст, надрукований заздалегідь. SSR — це пряма радіотрансляція: диктор говорить наживо щоразу, коли ти вмикаєш приймач, тому інформація актуальна, але хтось мусить озвучувати її щоразу заново. ISR — це та сама газета, але редакція передруковує окремі шпальти раз на годину: більшість дня читачі бачать той самий заздалегідь надрукований номер, а після спливання таймера наступний відвідувач отримує вже оновлену версію, поки Next.js генерує її у фоні. CSR — це коли читачеві дають чисті аркуші й диктофонний запис, а він сам переписує новини на папір, перш ніж зможе їх прочитати: інтерфейс збирається просто в тебе на очах, у браузері, після завантаження JavaScript. Технічно різниця зводиться до того, коли саме виконується рендер: під час збірки — для SSG й ISR, під час кожного запиту на сервері — для SSR, і в браузері після завантаження сторінки — для CSR. Вибір між ними — це компроміс між свіжістю даних, навантаженням на сервер і швидкістю першого відображення, а не питання, який спосіб «кращий» узагалі.',
    related: [
      {
        title: 'Static vs Dynamic Rendering в App Router',
        text: 'Офіційна термінологія App Router, де SSG й ISR обʼєднані поняттям статичного рендерингу, а SSR — динамічного; розуміння старих абревіатур допомагає читати нову документацію.',
      },
      {
        title: 'Time to First Byte',
        text: 'Час до отримання першого байта відповіді напряму залежить від стратегії рендерингу: у SSG й ISR він мінімальний, бо файл уже готовий, а в SSR включає час виконання рендера на сервері.',
      },
      {
        title: 'revalidate і on-demand ревалідація',
        text: 'Механізм, що визначає, як часто ISR-сторінка оновлюється у фоні, і дозволяє примусово оновити конкретну сторінку одразу після зміни даних, не чекаючи спливання таймера.',
      },
    ],
    codeExamples: [
      {
        label: 'SSG, SSR та ISR — той самий fetch, різні опції',
        code: `// SSG: без опцій кешу — Next.js рендерить один раз під час збірки
const staticData = await fetch('https://api.example.com/data')

// SSR: кешування вимкнене повністю — рендер на кожен запит
const freshData = await fetch('https://api.example.com/data', {
  cache: 'no-store',
})

// ISR: кеш з інтервалом ревалідації в секундах
const isrData = await fetch('https://api.example.com/data', {
  next: { revalidate: 60 },
})`,
      },
      {
        label: 'CSR: інтерфейс збирається в браузері',
        code: `// app/widget/ClientOnlyWidget.tsx
'use client'
import { useEffect, useState } from 'react'

export function ClientOnlyWidget() {
  const [data, setData] = useState(null)

  useEffect(() => {
    // ❌ порожній екран, доки не прийде відповідь від сервера
    fetch('/api/data')
      .then((r) => r.json())
      .then(setData)
  }, [])

  return data ? <View data={data} /> : <Spinner />
}`,
      },
    ],
    seniorNotes: [
      'В App Router режим вибирається автоматично на основі коду сторінки, на відміну від Pages Router, де розробник явно експортував саме getStaticProps чи саме getServerSideProps.',
      'ISR працює за моделлю stale-while-revalidate: перший відвідувач після спливання інтервалу все одно отримує стару сторінку, поки нова генерується у фоні, і лише наступні відвідувачі бачать оновлену версію.',
      'Значення revalidate для ISR задається під час збірки чи в коді сторінки як стале число секунд — його не можна обчислити динамічно з даних конкретного запиту.',
      'revalidatePath і revalidateTag дозволяють примусово оновити ISR-сторінку одразу, наприклад за вебхуком з CMS, не чекаючи спливання інтервалу revalidate.',
      'На Vercel статичні SSG- та ISR-сторінки віддаються напряму з CDN без виклику серверної функції при кожному попаданні в кеш, тоді як SSR-сторінка викликає функцію на кожен запит без винятку.',
    ],
  },

  {
    id: 'next-prerendering-vs-dynamic',
    title: 'Чим відрізняється prerendering від dynamic rendering в App Router?',
    definition:
      'Prerendering, або статичний рендеринг, — це коли Next.js рендерить сторінку в HTML заздалегідь, один раз під час next build чи у фоні для ISR, і потім віддає той самий результат усім, доки він не застаріє. Dynamic rendering — це коли сторінка рендериться на сервері окремо для кожного вхідного запиту, бо вона читає дані, які існують тільки в момент запиту: cookies, headers, searchParams чи fetch без кешування. Обидва режими застосовуються не до всього застосунку, а до кожного маршруту окремо, і Next.js вибирає між ними автоматично на основі того, який код виконує сторінка.',
    why: 'Без чіткого розуміння цього поділу розробник не може передбачити, чи зміна в коді випадково не перевела швидку статичну сторінку в повільнішу динамічну, яка тепер виконується на сервері для кожного відвідувача окремо.',
    simple:
      'Уяви кухню ресторану напередодні великого свята. Prerendering — це коли кухар готує тацю канапе заздалегідь, ще до відкриття залу, і просто виносить її, щойно прийде перший гість: усі гості отримують ту саму тацю. Dynamic rendering — це коли гість замовляє стейк особисто для себе, і кухар готує його з нуля саме на це замовлення, бо заздалегідь приготований стейк для конкретної людини не має сенсу. У Next.js за замовчуванням кожен маршрут вважається кандидатом на prerendering, доки в його коді не зʼявиться щось, що можна дізнатися лише в момент запиту, — виклик cookies(), headers(), читання searchParams чи fetch з опцією cache: no-store. Щойно таке зʼявляється, весь маршрут переходить у dynamic rendering, навіть якщо решта коду на сторінці не залежить від запиту взагалі. Це відрізняється від Pages Router, де режим обирали явно, експортуючи саме getStaticProps чи саме getServerSideProps: в App Router режим — наслідок того, який код розробник написав, а не окреме рішення, яке приймають одразу і явно.',
    related: [
      {
        title: 'Динамічні API cookies() і headers()',
        text: 'Функції, виклик яких усередині Server Component автоматично переводить маршрут у dynamic rendering, бо їхні дані існують лише в момент конкретного запиту, а не під час збірки.',
      },
      {
        title: 'next build',
        text: 'Команда, під час якої Next.js аналізує кожен маршрут і вирішує, prerendering він чи dynamic rendering; результат цього аналізу видно в таблиці маршрутів у консолі.',
      },
      {
        title: 'Streaming і Suspense',
        text: 'Механізм, що дозволяє віддавати частини dynamic-маршруту потоково, щойно вони готові, замість того щоб чекати весь рендер сторінки цілком.',
      },
    ],
    codeExamples: [
      {
        label: 'Один і той самий App Router, два різні режими',
        code: `// app/blog/[slug]/page.tsx — prerendering: немає динамічних API
export default async function BlogPost({ params }) {
  const post = await getPost(params.slug) // кешується, рендериться заздалегідь
  return <Article post={post} />
}

// app/account/page.tsx — dynamic rendering: читає cookies()
import { cookies } from 'next/headers'

export default async function AccountPage() {
  const session = (await cookies()).get('session')
  const user = await getUser(session?.value)
  return <Profile user={user} />
}`,
      },
      {
        label: 'Примусовий режим замість автоматичного визначення',
        code: `// app/status/page.tsx
export const dynamic = 'force-static'
// ✅ маршрут лишається статичним, навіть якщо код усередині викликає
// cookies() чи headers() — такі виклики поверне порожні значення

export const dynamic = 'error'
// ❌ навпаки: будь-яка динамічна API усередині маршруту зламає next build`,
      },
    ],
    seniorNotes: [
      'export const dynamic = "force-static" примусово тримає маршрут статичним: виклики cookies() чи headers() усередині нього повертають порожні значення замість реальних даних запиту.',
      'export const dynamic = "error" змушує next build завершитися помилкою, якщо в маршруті випадково зʼявиться динамічна API, — корисно, щоб гарантовано не втратити статичність важливої сторінки.',
      'Читання searchParams у Server Component-сторінці так само форсує dynamic rendering для всього маршруту, бо рядок запиту різний для кожного запиту.',
      'Dynamic rendering не вимикає кешування конкретних fetch-викликів усередині маршруту: окремий запит може мати свій revalidate, навіть якщо сам маршрут рендериться щоразу заново.',
      'У виводі next build статичні маршрути позначаються окремим символом від динамічних у тій самій таблиці, де показано розмір First Load JS кожної сторінки.',
    ],
  },

  {
    id: 'next-cookies-headers-rendering-impact',
    title: 'Як використання cookies() або headers() впливає на рендеринг?',
    definition:
      'Виклик cookies() чи headers() усередині Server Component чи Route Handler читає дані, які існують лише в момент конкретного запиту, тому Next.js не може порахувати результат заздалегідь під час збірки. Через це маршрут, що використовує будь-яку з цих функцій, автоматично переходить у dynamic rendering і виконується на сервері окремо для кожного вхідного запиту. Це стосується всього маршруту, а не лише компонента, що зробив виклик, — тому одна перевірка сесії у глибоко вкладеному компоненті може зробити динамічною всю сторінку.',
    why: 'Розробники часто додають cookies() для дрібної речі — наприклад визначити тему оформлення — не усвідомлюючи, що це вимикає статичну генерацію й кешування CDN для всього маршруту, а не лише для тієї частини інтерфейсу, що справді залежить від cookie.',
    simple:
      'Уяви бібліотекаря, який видає книжки за єдиним каталогом усім відвідувачам, — це швидко, бо каталог підготовлений заздалегідь і однаковий для всіх. Тепер уяви, що бібліотекар починає перевіряти читацький квиток кожного відвідувача особисто, перш ніж щось видати, — тепер він уже не може обслуговувати за заздалегідь підготовленим списком, бо кожна видача залежить від конкретної людини. cookies() і headers() — це і є той читацький квиток: вони існують тільки в момент, коли реальний запит доходить до сервера, тому Next.js фізично не може виконати цей код заздалегідь під час збірки. Щойно такий виклик зʼявляється в дереві маршруту, увесь маршрут переходить у режим, де сервер виконує рендер наново на кожен запит, навіть якщо результат використовується лише для однієї маленької кнопки. У Next.js 15 обидві функції стали асинхронними й повертають проміс замість значення напряму, тому їх тепер викликають через await, а не як синхронну функцію, якою вони були раніше. Практичний спосіб обмежити вплив — винести код, що читає cookies() чи headers(), у максимально глибокий ізольований компонент і обгорнути його межею Suspense: це не поверне маршруту статичність без Partial Prerendering, але хоча б дозволить решті дерева рендеритися й стрімитися, не чекаючи саме цієї перевірки.',
    related: [
      {
        title: 'Асинхронні динамічні API в Next.js 15',
        text: 'Зміна, після якої cookies(), headers() і params повертають проміс замість значення напряму; старий синхронний виклик показує попередження й підлягає заміні через кодмод.',
      },
      {
        title: 'Prerendering проти dynamic rendering',
        text: 'Динамічні API — один з головних тригерів переходу маршруту з prerendering у dynamic rendering; без розуміння цього звʼязку незрозуміло, чому одна перевірка cookie сповільнює всю сторінку.',
      },
      {
        title: 'Partial Prerendering',
        text: 'Механізм, що дозволяє ізолювати код із cookies() чи headers() у динамічному острівці, лишаючи решту сторінки статичною, замість того щоб переводити весь маршрут у dynamic rendering.',
      },
    ],
    codeExamples: [
      {
        label: 'cookies() як асинхронна функція в Next.js 15',
        code: `// app/theme/ThemeBanner.tsx
import { cookies } from 'next/headers'

export default async function ThemeBanner() {
  // ✅ Next.js 15: cookies() повертає проміс, потрібен await
  const theme = (await cookies()).get('theme')?.value ?? 'light'
  return <Banner theme={theme} />
}`,
      },
      {
        label: 'Ізоляція динамічного коду в окремому компоненті',
        code: `// app/page.tsx — решта дерева не залежить від cookies()
import { Suspense } from 'react'
import { ThemeBanner } from './ThemeBanner'

export default function HomePage() {
  return (
    <div>
      <StaticHero /> {/* не читає cookies() чи headers() */}
      <Suspense fallback={null}>
        <ThemeBanner /> {/* єдине місце, що форсує динамічність */}
      </Suspense>
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Next.js 15 зробив cookies(), headers(), draftMode() та params асинхронними; синхронний виклик без await ще працює перехідний період, але показує попередження про застарілість.',
      'cookies() усередині Server Component повертає лише read-only обʼєкт: викликати на ньому set() не можна, зміна cookie можлива тільки в Route Handler чи Server Action.',
      'headers(), викликаний навіть у компоненті, чий fetch має власний revalidate, усе одно переводить весь маршрут у dynamic rendering — кешування конкретного запиту не рятує режим рендерингу сторінки.',
      'Middleware може читати cookies і headers без жодного впливу на dynamic rendering сторінки, бо middleware виконується на кожен запит незалежно від режиму рендерингу маршруту, що йде далі.',
      'Усередині Route Handler ті самі дані доступні напряму через request.cookies і request.headers обʼєкта NextRequest, без імпорту окремих хелперів cookies() і headers() з next/headers.',
    ],
  },

  {
    id: 'next-static-to-dynamic-bailout',
    title: 'Що відбувається, коли сторінка переходить від статичного до динамічного рендерингу?',
    definition:
      'Коли сторінка переходить від статичного до динамічного рендерингу, Next.js перестає генерувати для неї готовий HTML заздалегідь і починає виконувати весь рендер на сервері окремо для кожного запиту. Це стосується всього маршруту цілком: навіть компоненти, які самі по собі не залежать від запиту, тепер виконуються щоразу заново, бо межа статичний-динамічний проходить по маршруту, а не по окремому компоненту. Наслідок — маршрут більше не можна віддавати прямо з CDN-кешу, і кожен візит навантажує серверну функцію, збільшуючи затримку відповіді порівняно з готовим статичним файлом.',
    why: 'Перехід у динамічний режим часто стається непомітно — через один виклик cookies() у глибоко вкладеному компоненті чи через бібліотеку аналітики, яка читає headers усередині себе. Розуміння цього механізму пояснює, чому нібито статична сторінка раптом почала повільніше відповідати в продакшні.',
    simple:
      'Уяви конвеєр на заводі, де одна зіпсована деталь зупиняє не тільки свою станцію, а весь наступний етап складання, бо продукт єдиний і не можна відправити його частинами. Так само й тут: щойно в дереві компонентів одного маршруту зʼявляється виклик динамічної функції — cookies(), headers(), доступ до searchParams чи fetch з cache: no-store — прапорець динамічності піднімається на рівень усього маршруту, а не лише того компонента, що його викликав. Технічно це означає, що Next.js більше не може виконати рендер один раз під час збірки й закешувати результат: замість готового HTML-файлу маршрут стає функцією, яку сервер запускає заново на кожен вхідний запит. Компоненти-сусіди, які самі не читають нічого запитозалежного, від цього не рятуються — вони теж перевиконуються щоразу, просто тому, що є частиною того самого дерева рендерингу. У виводі next build такий маршрут більше не позначається як статичний, а поруч із ним зʼявляється позначка серверної функції замість розміру готового файлу. Практичний наслідок — CDN більше не може віддавати відповідь напряму з краю мережі: кожен запит доїжджає до серверної функції, і час відповіді зростає порівняно з тим, коли сторінка була повністю статичною. Уникнути цього можна, ізолювавши запитозалежний код в окремому глибокому компоненті чи винісши його в Client Component, щоб решта дерева лишалася статичною, а не тягнула за собою весь маршрут.',
    related: [
      {
        title: 'Динамічні API cookies() і headers()',
        text: 'Найчастіша причина переходу маршруту в динамічний режим: будь-який виклик цих функцій усередині дерева компонентів маршруту форсує рендеринг на кожен запит.',
      },
      {
        title: 'Partial Prerendering',
        text: 'Механізм, розроблений саме для цієї проблеми: дозволяє ізолювати динамічний код у Suspense-межі, не забираючи статичність у решти маршруту.',
      },
      {
        title: 'next build',
        text: 'Команда, під час якої видно, які маршрути лишилися статичними, а які перейшли в динамічний режим, — саме за цим виводом шукають несподівані bailout.',
      },
    ],
    codeExamples: [
      {
        label: 'Один виклик робить динамічним увесь маршрут',
        code: `// app/pricing/page.tsx
import { cookies } from 'next/headers'

export default async function PricingPage() {
  const plans = await getPlans() // сам по собі кандидат на статичний рендер

  // ❌ цей єдиний виклик переводить УВЕСЬ маршрут у dynamic rendering,
  // хоча він потрібен лише для банера під заголовком
  const promo = (await cookies()).get('promo')?.value

  return (
    <div>
      <PromoBanner code={promo} />
      <PlansList plans={plans} /> {/* теж почне виконуватись на кожен запит */}
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Bailout діє на рівні маршруту, побудованого з layout і page разом: динамічний виклик у layout переводить у dynamic rendering усі вкладені page, навіть якщо в них самих такого коду немає.',
      'next build не вказує напряму, який саме компонент спричинив перехід у динамічний режим — типовий спосіб знайти причину це пошук викликів cookies(), headers() чи searchParams по дереву маршруту вручну.',
      'Без Partial Prerendering межа Suspense усередині вже динамічного маршруту дає лише стрімінг цього вже динамічного рендера, а не рятує маршрут від самого переходу в динамічний режим.',
      'Перенесення коду з cookies() чи headers() у Client Component, що робить власний запит до Route Handler після гідратації, повністю усуває bailout, бо сервер під час рендеру маршруту цей код більше не виконує.',
      'Динамічний маршрут на Vercel завжди викликає серверну функцію заново, тому middleware з тяжкою логікою перед таким маршрутом додає затримку до кожного без винятку запиту, а не лише до першого.',
    ],
  },

  {
    id: 'next-server-rendering-vs-rsc',
    title: 'Чим Server Rendering відрізняється від React Server Components?',
    definition:
      'Server Rendering — це процес перетворення дерева React-компонентів на HTML-рядок на сервері; його робили ще до React Server Components, наприклад через renderToString для звичайних компонентів у Pages Router. React Server Components — це окремий тип компонента, який виконується виключно на сервері, ніколи не потрапляє в клієнтський JavaScript-бандл і не може мати власний стан чи обробники подій. Головна відмінність: Server Rendering — це про те, де відбувається перший рендер компонента, який усе одно потім гідратується й може повторно рендеритися в браузері, а RSC — про компонент, який у браузері взагалі ніколи не виконується.',
    why: 'До RSC весь JavaScript компонента однаково потрапляв у клієнтський бандл, навіть якщо він рендерився на сервері для першого відображення, — сервер лише пришвидшував перше малювання, але не зменшував розмір JavaScript, який мав завантажити браузер. RSC вирішує саме це: код, що ніколи не має сенсу виконувати в браузері, туди просто не потрапляє.',
    simple:
      'Уяви театральну виставу, яку показують по телебаченню наживо. Server Rendering — це запис тієї самої вистави на плівку, яку глядач потім переглядає вдома: результат перенесли на носій заздалегідь, але сценарій і все потрібне, щоб зіграти цю виставу ще раз, глядачеві однаково передають повністю. React Server Components — це коли частину вистави взагалі не записують і не передають глядачеві: оповідач за кадром розповідає передісторію один раз, глядач чує лише підсумок, а весь текст оповідача лишається за лаштунками назавжди. Технічно старий Server Rendering, renderToString у Pages Router, перетворював компонент на HTML-рядок, але сам компонент і його код усе одно опинялися в клієнтському бандлі, бо React мав змогу повторно відрендерити його в браузері при наступному оновленні стану. RSC-компонент так само рендериться на сервері в щось схоже на HTML, але жодного JavaScript цього компонента браузер узагалі не отримує — оновити його можна лише повторним запитом до сервера, а не локальним ререндером у браузері. Через це в App Router компонент за замовчуванням саме RSC, а не просто «серверно відрендерений компонент», який потім усе одно живе в браузері: щоб отримати старий стиль поведінки, потрібна явна директива use client. Плутанина між цими двома поняттями — типова помилка на співбесіді: «сервер рендерить компонент» не означає автоматично «це Server Component» у сенсі RSC.',
    related: [
      {
        title: 'Директива use client',
        text: 'Позначка, яка перетворює компонент і все, що він імпортує далі, на клієнтський код, що потрапляє в бандл і гідратується; без неї компонент лишається RSC і не має доступу до useState.',
      },
      {
        title: 'Гідратація',
        text: 'Крок, що стосується лише Client Component: React навішує обробники подій на вже готову розмітку; RSC-компоненти цей крок узагалі не проходять, бо їхній код у браузер не потрапляє.',
      },
      {
        title: 'renderToString і застарілі API рендерингу',
        text: 'Низькорівнева функція, якою React перетворював дерево компонентів на HTML-рядок до появи App Router; вона й досі лежить в основі механізму Server Rendering, але нічого не знає про RSC.',
      },
    ],
    codeExamples: [
      {
        label: 'RSC ніколи не потрапляє в бандл, Client Component — потрапляє',
        code: `// app/profile/page.tsx — RSC: цей код і db НІКОЛИ не потрапляють у браузер
import { db } from '@/lib/db'

export default async function ProfilePage({ params }) {
  const user = await db.user.findUnique({ where: { id: params.id } })
  return <ProfileView user={user} />
}

// app/profile/ProfileView.tsx — Client Component: рендериться на сервері
// один раз для початкового HTML, а потім гідратується й живе в браузері
'use client'
import { useState } from 'react'

export function ProfileView({ user }) {
  const [editing, setEditing] = useState(false)
  return editing ? <EditForm user={user} /> : <ViewOnly user={user} />
}`,
      },
    ],
    seniorNotes: [
      'Імпорт серверного модуля, що використовує Node.js API, усередині Client Component ламає збірку; пакет server-only навмисно кидає помилку при спробі імпортувати такий код із клієнтського файлу.',
      'RSC-компонент може бути напряму async-функцією з await усередині тіла, а Client Component так не може — його функція рендеру не дозволяє бути async.',
      'Пропси, що передаються від Server Component до Client Component, мають бути серіалізовними: передати функцію чи інстанс класу як пропс не можна, React кине помилку під час рендера.',
      'router.refresh() на клієнті повторно запитує RSC-корисне навантаження й оновлює дерево серверних компонентів без повного перезавантаження сторінки й без втрати стану Client Component.',
      'Server Rendering як техніка існує і в Remix, і в старому Pages Router без жодного поняття RSC — тому сама по собі фраза «рендериться на сервері» не є доказом, що компонент є Server Component.',
    ],
  },

  {
    id: 'next-partial-prerendering',
    title: 'Що таке Partial Prerendering (PPR) і яку проблему воно вирішує?',
    definition:
      'Partial Prerendering, PPR — це режим рендерингу, у якому одна сторінка одночасно містить статично згенеровану оболонку й динамічні острівці всередині неї, а обидві частини повертаються клієнту в межах однієї HTTP-відповіді. Статична частина рендериться заздалегідь, як при SSG, і віддається миттєво з кешу, а динамічні частини, обгорнуті межею Suspense, дорендерюються на сервері на льоту і дотягуються у потоці тієї самої відповіді. PPR вирішує проблему бінарного вибору «вся сторінка статична або вся сторінка динамічна»: раніше один виклик cookies() у дрібному компоненті переводив геть увесь маршрут у повільніший динамічний режим.',
    why: 'До PPR розробники або жертвували швидкістю статичної частини заради динамічної, змушуючи весь маршрут виконуватися на сервері щоразу, або виносили персоналізацію в окремий клієнтський запит після завантаження сторінки, що додавало видиму затримку. PPR дозволяє отримати миттєвий статичний каркас і персоналізацію в одній відповіді без цього компромісу.',
    simple:
      'Уяви листівку, яку друкарня друкує заздалегідь великим тиражем, але лишає на ній порожнє віконце для рукописного підпису, який ставлять уже в момент відправки конкретному отримувачу. Більшість листівки — картинка, текст привітання — однакова для всіх і друкується заздалегідь: це статична частина. Підпис пишуть окремо для кожного отримувача в останній момент: це динамічна частина, але вона займає лише маленьке віконце, а не всю листівку. PPR працює так само: Next.js аналізує сторінку й визначає межі Suspense як кордон між статичним і динамічним, рендерить статичну оболонку заздалегідь під час збірки, а вміст усередині Suspense-меж лишає порожнім до моменту реального запиту. Коли приходить запит, сервер миттєво віддає готову статичну оболонку і одразу ж, у тій самій відповіді, починає дорендерювати динамічні острівці, стрімлячи їх у ці порожні місця, щойно вони готові. Для користувача це виглядає як миттєве перше відображення сторінки, після якого персоналізовані блоки на кшталт кошика чи рекомендацій доповнюються буквально за частки секунди. Технічно PPR — це не нова окрема технологія, а комбінація вже наявних механізмів Next.js: статичного рендерингу, Suspense і потокової передачі HTML, просто застосованих одночасно в межах одного маршруту, а не як взаємовиключний вибір.',
    related: [
      {
        title: 'Streaming SSR і Suspense',
        text: 'Базовий механізм, на якому побудований PPR: без потокової передачі HTML і меж Suspense не було б способу домалювати динамічну частину в уже відправлену статичну відповідь.',
      },
      {
        title: 'Динамічні API cookies() і headers()',
        text: 'Виклики, що визначають, яка саме частина дерева компонентів стане динамічним острівцем у PPR-маршруті, а яка лишиться в статичній оболонці.',
      },
      {
        title: 'Комбінування статики й персоналізації',
        text: 'Практична техніка застосування PPR на реальній сторінці — куди саме ставити межі Suspense і як поводитися, якщо PPR ще недоступний на проєкті.',
      },
    ],
    codeExamples: [
      {
        label: 'Статична оболонка й динамічний острівець в одному маршруті',
        code: `// app/product/[id]/page.tsx
export const experimental_ppr = true // вмикає PPR для цього маршруту

export default function ProductPage({ params }) {
  return (
    <div>
      <ProductInfo id={params.id} /> {/* статична частина: та сама для всіх */}
      <Suspense fallback={<CartSkeleton />}>
        {/* динамічний острівець: рендериться окремо на кожен запит */}
        <PersonalizedCart />
      </Suspense>
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'PPR вмикають інкрементально на рівні окремого маршруту через export const experimental_ppr = true після ввімкнення experimental.ppr у next.config.js — не для всього застосунку одразу.',
      'Компонент усередині Suspense-межі має справді бути динамічним, наприклад викликати cookies(); якщо він статичний, Next.js може відрендерити його разом з оболонкою, а не як окремий потоковий острівець.',
      'PPR повертає одну HTTP-відповідь, що містить одразу готові байти й потоковий продовження, тому вимагає хостингу зі стрімінговими відповідями, а не платформи, здатної віддавати лише статичні файли.',
      'PPR не прибирає вартість виконання динамічної частини — вона все одно рендериться на кожен запит; PPR прибирає лише те, що ця динамічна частина блокує показ статичної.',
      'Без PPR межа Suspense у динамічному маршруті все одно дає стрімінг, але лише в межах уже динамічного рендерингу всієї сторінки; унікальність PPR саме в поєднанні статичної оболонки з динамічним потоком в одній відповіді.',
    ],
  },

  {
    id: 'next-static-plus-personalized-content',
    title: 'Як поєднати статичний контент і персоналізовані дані на одній сторінці?',
    definition:
      'Щоб поєднати статичний контент і персоналізовані дані на одній сторінці, персоналізовану частину виносять в окремий компонент, обгортають межею Suspense і, за можливості, вмикають Partial Prerendering для цього маршруту. Статична частина дерева рендериться заздалегідь і віддається миттєво з кешу, а компонент усередині Suspense виконується окремо для кожного запиту й дорендерюється в потоці тієї самої відповіді. Якщо PPR недоступний, альтернатива — рендерити персоналізовану частину як Client Component, що робить власний fetch у браузері вже після того, як статична оболонка показалася користувачу.',
    why: 'Наївний підхід — обгорнути всю сторінку в динамічний рендеринг через один виклик cookies() заради дрібної персоналізації — губить усі переваги кешування статичної частини заради маленького шматка інтерфейсу, який реально залежить від користувача.',
    simple:
      'Уяви газетний кіоск, де більшість шпальт друкує великий друкарський верстат заздалегідь, а невеличкий стікер зі знижкою для постійного клієнта наклеюють на кожен примірник окремо вже під час видачі. Основний текст газети — заголовки, статті, реклама — однаковий для всіх читачів, тому його вигідно надрукувати одним великим тиражем наперед: це статична частина сторінки, наприклад опис товару чи блок з відгуками. Персональний стікер — кошик покупця, рекомендації на основі історії переглядів, привітання на імʼя — потребує знати, хто саме зараз перед тобою, тому його неможливо надрукувати заздалегідь для всіх одразу. У коді це виглядає як окремий компонент, що читає cookies() чи звертається до бази даних користувача, обгорнутий у Suspense з fallback-заглушкою на час, поки персональні дані ще завантажуються. Якщо на проєкті ввімкнено Partial Prerendering, Next.js сам розподіляє сторінку на статичну оболонку й динамічні острівці за цими Suspense-межами, і обидві частини йдуть в одній HTTP-відповіді. Якщо PPR ще не використовують, той самий результат отримують вручну: персональний блок роблять Client Component, який після гідратації сторінки сам робить fetch до свого Route Handler і підвантажує дані окремим запитом, не блокуючи показ статичної частини. В обох варіантах головний принцип той самий: межа між статичним і динамічним проводиться по конкретному компоненту, а не по всій сторінці цілком.',
    related: [
      {
        title: 'Partial Prerendering',
        text: 'Вбудований механізм Next.js, що автоматизує саме цю техніку: статична оболонка й динамічні Suspense-острівці в одній відповіді без ручного поділу на серверний і клієнтський запит.',
      },
      {
        title: 'Suspense і межі стрімінгу',
        text: 'Базовий React-механізм, яким користується як ручний підхід через Client Component, так і автоматичний підхід через PPR, щоб показати статичну частину, не чекаючи персоналізовану.',
      },
      {
        title: 'Client Component і власний fetch',
        text: 'Резервний спосіб персоналізації без PPR: компонент з use client робить запит до свого Route Handler у браузері після гідратації, не впливаючи на режим рендерингу решти сторінки.',
      },
    ],
    codeExamples: [
      {
        label: 'Ручний поділ без PPR: статична сторінка плюс клієнтський острівець',
        code: `// app/product/[id]/page.tsx — без PPR
export default function ProductPage({ params }) {
  return (
    <div>
      <StaticProductInfo id={params.id} /> {/* спільне для всіх, кешується */}
      <RecommendedForYou />
    </div>
  )
}

// app/product/[id]/RecommendedForYou.tsx
'use client'
import { useEffect, useState } from 'react'

export function RecommendedForYou() {
  const [items, setItems] = useState(null)

  useEffect(() => {
    fetch('/api/recommendations')
      .then((r) => r.json())
      .then(setItems)
  }, [])

  // ✅ статична частина сторінки вже показана користувачу до цього моменту
  return items ? <List items={items} /> : <RecommendationsSkeleton />
}`,
      },
    ],
    seniorNotes: [
      'Без PPR межа Suspense навколо динамічного Server Component усе одно змушує весь маршрут рендеритися динамічно; Suspense тут лише дає стрімінг, а не звільняє від переходу в dynamic rendering.',
      'Підхід із Client Component повністю уникає dynamic-bailout маршруту, бо Server Component-дерево взагалі не викликає жодної динамічної API — персоналізований запит іде вже після гідратації в браузері.',
      'Клієнтський fetch для персоналізації живе поза системою кешування fetch на сервері Next.js, тому для нього окремо налаштовують власне кешування, наприклад через SWR чи React Query.',
      'Edge Middleware, що читає cookie й підставляє значення в заголовок запиту, не рятує компонент, який далі сам викликає headers() чи cookies() напряму, — той виклик однаково форсує dynamic rendering.',
      'Комбінований підхід із PPR доступний лише на платформах, що підтримують потокові HTTP-відповіді; без такої підтримки Suspense-межа мовчки перетворюється на звичайне блокувальне очікування.',
    ],
  },

  {
    id: 'next-rendering-strategy-choice',
    title: 'Як визначити оптимальну стратегію рендерингу для конкретної сторінки?',
    definition:
      'Стратегію рендерингу обирають за трьома питаннями: як часто змінюються дані, наскільки критична свіжість для конкретного користувача і скільки різних версій сторінки взагалі існує. Якщо дані спільні для всіх і змінюються рідко — підходить SSG чи ISR з відповідним інтервалом ревалідації. Якщо дані унікальні для кожного запиту чи користувача — потрібен серверний рендеринг на кожен запит. Якщо частина даних персональна, а решта спільна, найкраще поєднати статичний каркас із динамічними острівцями через Suspense чи Partial Prerendering, а не рендерити всю сторінку одним способом.',
    why: 'Вибір стратегії рендерингу для кожної сторінки окремо, а не для всього застосунку відразу, дозволяє платити за динамічність лише там, де вона справді потрібна, а решту сайту віддавати миттєво з кешу чи CDN.',
    simple:
      'Уяви лікаря, який перед призначенням лікування спершу ставить кілька уточнювальних питань, а не виписує один і той самий рецепт усім пацієнтам. Перше питання для сторінки: чи дані однакові для всіх відвідувачів? Якщо так — і сторінка список товарів чи стаття блогу — це кандидат на SSG, бо немає сенсу рендерити той самий HTML повторно для кожного відвідувача. Друге питання: як часто змінюються ці дані? Якщо рідко, але не ніколи, наприклад ціни оновлюються раз на годину, це ISR з ревалідацією, що дозволяє не передзбирати весь сайт заново через кожну зміну. Третє питання: чи дані унікальні саме для цього користувача чи цього запиту — кошик, персональні рекомендації, сторінка після логіну? Тоді потрібен серверний рендеринг на кожен запит, бо жодна версія HTML, згенерована наперед, не підійде для іншого користувача. Якщо ж на сторінці є і спільний контент, і персональний одночасно, наприклад стаття блогу зі спільним текстом і панеллю «рекомендовано для тебе», правильна відповідь не «динамічний рендеринг для всього», а розбити сторінку межами Suspense: статичну частину віддати одразу з кешу, а динамічний острівець довантажити окремо. Найчастіша помилка — обирати динамічний рендеринг за замовчуванням для перестрахування: це прибирає всі переваги кешування там, де дані насправді однакові для всіх.',
    related: [
      {
        title: 'SSR, SSG, ISR та CSR',
        text: 'Чотири базові стратегії, між якими й відбувається вибір; розуміння різниці між ними — передумова для того, щоб узагалі ставити правильні питання під час вибору.',
      },
      {
        title: 'Partial Prerendering',
        text: 'Варіант для сторінок, де відповідь на питання вибору стратегії не однозначна «статично» чи «динамічно», а «і те, і те одночасно» для різних частин однієї сторінки.',
      },
      {
        title: 'revalidate і on-demand ревалідація',
        text: 'Інструмент тонкого налаштування ISR-стратегії: дозволяє підібрати інтервал оновлення під реальну частоту зміни даних, а не просто обрати між «завжди свіже» і «завжди старе».',
      },
    ],
    codeExamples: [
      {
        label: 'Три сторінки — три різні відповіді на одне й те саме питання',
        code: `// app/blog/[slug]/page.tsx — дані спільні, змінюються рідко → SSG
export default async function BlogPost({ params }) {
  return <Article post={await getPost(params.slug)} />
}

// app/products/[id]/page.tsx — дані спільні, оновлюються регулярно → ISR
export const revalidate = 3600 // раз на годину
export default async function ProductPage({ params }) {
  return <Product data={await getProduct(params.id)} />
}

// app/dashboard/page.tsx — дані унікальні для користувача → dynamic rendering
import { cookies } from 'next/headers'
export default async function Dashboard() {
  const session = (await cookies()).get('session')
  return <Widgets data={await getUserData(session?.value)} />
}`,
      },
    ],
    seniorNotes: [
      'Статичний рендеринг — режим за замовчуванням для будь-якого маршруту App Router; маршрут стає динамічним лише як наслідок конкретного коду, а не через окреме явне рішення розробника.',
      'export const revalidate, заданий на рівні маршруту, застосовується до всього сегмента цілком; окремий fetch з власним next.revalidate може задати менше значення, і саме менше з двох візьме гору.',
      'Читання searchParams як пропа Page-компонента форсує dynamic rendering для всієї сторінки, бо рядок запиту відрізняється для кожного відвідувача, навіть якщо решта даних сторінки спільна.',
      'ISR на Vercel регенерує сторінку у фоновій функції, не блокуючи запит, що спричинив оновлення, — це принципово відрізняється від SSR, де кожен запит чекає на завершення рендера напряму.',
      'Вибір динамічного рендерингу для сторінки, яка могла б бути статичною, повністю прибирає можливість віддавати відповідь із CDN: кожен запит без винятку доходить до серверної функції.',
    ],
  },

  {
    id: 'next-server-vs-client-component',
    title: 'У чому принципова різниця між Server Component та Client Component?',
    definition:
      'Server Component виконується лише на сервері: його код, залежності й будь-які секрети ніколи не потрапляють у клієнтський JavaScript-бандл, а результат браузер отримує вже у вигляді розмітки та серіалізованого опису дерева. Client Component рендериться на сервері лише один раз для початкового HTML, а потім гідратується в браузері й може повторно ререндеритися локально, реагуючи на useState, події чи контекст. Ключова відмінність не в тому, де компонент рендериться, а в тому, чи існує його код у браузері взагалі: Server Component там не існує ніколи, а Client Component існує завжди, навіть якщо перший показ стався на сервері.',
    why: 'React довгий час пропонував лише один тип компонента, тому будь-який код — навіть той, що просто форматує дату чи звертається до бази даних, — однаково потрапляв у клієнтський бандл і збільшував розмір JavaScript. Поділ на два типи дозволяє тримати в браузері лише той код, якому справді потрібна інтерактивність.',
    simple:
      'Уяви кухню ресторану, відокремлену від залу скляною стіною. Server Component — це кухар за стіною: гість ніколи не бачить, як він готує, не знає його рецептів і навіть не підозрює, які інгредієнти лежать у нього в холодильнику, — гість отримує лише готову страву. Client Component — це офіціант у залі: він теж зʼявляється вперше з кухні, несучи вже готову страву, але після цього лишається в зоні видимості гостя, реагує на його прохання чи змінює замовлення, тобто далі живе й діє прямо в залі. Технічно Server Component виконується виключно на сервері під час формування відповіді і ніколи не потрапляє в JavaScript, який завантажує браузер, тому не може використовувати useState, useEffect чи onClick — у нього просто немає механізму для повторного виконання в браузері. Client Component, навпаки, обовʼязково має JavaScript-версію в бандлі: сервер лише малює його початковий HTML для швидкого першого відображення, а сама логіка компонента так само працює в браузері після гідратації. Найважливіший практичний наслідок: Server Component може напряму звертатися до бази даних чи використовувати секретні ключі, бо цей код фізично недосяжний для користувача, а Client Component так робити не може — усе написане в ньому будь-хто побачить у вихідному коді бандла.',
    related: [
      {
        title: 'Директива use client',
        text: 'Позначка, що визначає, з якого файлу компонент і все, що з нього імпортується далі, стають Client Component; без неї файл у каталозі app за замовчуванням лишається Server Component.',
      },
      {
        title: 'React Server Components',
        text: 'Технологія, що стоїть за самим існуванням Server Component як окремого типу; без неї в React був би лише один вид компонента для будь-якого середовища виконання.',
      },
      {
        title: 'Серіалізація пропсів на межі Server і Client Components',
        text: 'Обмеження на те, які саме дані можна передати з Server у Client Component; напряму пояснює, чому не можна просто передати функцію чи інстанс класу як проп через цю межу.',
      },
    ],
    codeExamples: [
      {
        label: 'Прямий доступ до бази даних лише в Server Component',
        code: `// app/orders/page.tsx — Server Component
import { db } from '@/lib/db'
import { OrderActions } from './OrderActions'

export default async function OrdersPage() {
  const orders = await db.order.findMany() // прямий доступ до БД, безпечно
  return <OrderActions orders={orders} />
}

// app/orders/OrderActions.tsx — Client Component
'use client'
import { useState } from 'react'

export function OrderActions({ orders }) {
  const [selected, setSelected] = useState(null)
  return <OrderList orders={orders} onSelect={setSelected} selected={selected} />
}`,
      },
    ],
    seniorNotes: [
      'Спроба використати useState чи useEffect усередині Server Component кидає помилку прямо під час збірки, а не попередження під час виконання.',
      'Client Component усе одно виконується на сервері один раз для початкового HTML, якщо його явно не імпортовано через dynamic() з опцією ssr: false.',
      'Server Component може імпортувати й рендерити Client Component напряму, але не навпаки: Client Component не може імпортувати файл Server Component і відрендерити його з власними пропсами.',
      'Тип компонента визначається на рівні файлу, а не проєкту загалом: наявність чи відсутність use client на початку конкретного файлу і є межею між двома типами.',
      'Змінна оточення без префікса NEXT_PUBLIC_ читається в Server Component без проблем, але всередині коду Client Component її значення буде undefined уже після збірки.',
    ],
  },

  {
    id: 'next-use-client-module-graph',
    title: 'Що означає директива use client і як вона впливає на module graph?',
    definition:
      'Директива use client, записана першим рядком файлу, повідомляє збірнику Next.js, що цей файл і все, що з нього імпортується вниз по дереву, має потрапити в клієнтський JavaScript-бандл. Технічно вона не робить компонент клієнтським сама по собі — вона проводить межу в module graph, позначаючи вхідну точку, з якої починається підграф модулів, призначених для браузера. Усе, що імпортоване з цього файлу далі — хуки, допоміжні функції, інші компоненти без власної директиви — автоматично потрапляє в той самий клієнтський підграф, навіть якщо в них немає use client.',
    why: 'Без цієї директиви збірник не мав би способу дізнатися, з якого саме модуля починається код, призначений для браузера, і був би змушений або включати весь код у клієнтський бандл про всяк випадок, або взагалі не дозволяти інтерактивність у App Router.',
    simple:
      'Уяви величезний склад із товарами, розділений на дві зони: закриту складську частину і торговий зал, куди пускають покупців. use client — це двері між ними: усе, що лежить за цими дверима в бік залу, автоматично вважається доступним покупцям, навіть якщо на самому товарі не написано «для продажу» окремо. Так само з module graph: Next.js будує граф залежностей усього застосунку, і щойно він натрапляє на файл із use client, усе, що цей файл імпортує далі — інші компоненти, утиліти, навіть цілі бібліотеки — потрапляє в клієнтську частину графа, бо крокує через ці двері в бік браузера. Важливо, що директива діє на файл як вхідну точку, а не позначає окремо кожен компонент усередині: якщо файл із use client імпортує ще один компонент без цієї директиви, той компонент усе одно опиняється в клієнтському бандлі, просто тому що потрапив у підграф за дверима. У зворотний бік директива не працює: Server Component може імпортувати Client Component, і це не перетворює сам Server Component на клієнтський, бо межа проходить лише в напрямку від use client вниз по дереву імпортів. На практиці це означає, що один use client на верхньому рівні великого дерева компонентів може випадково затягти в бандл значно більше коду, ніж здається на перший погляд, якщо не стежити, звідки саме починається ця межа.',
    related: [
      {
        title: 'Server Component проти Client Component',
        text: 'Базове розрізнення двох типів компонентів, яке module graph реалізує технічно: файл із use client і все, що він тягне за собою, стає клієнтським підграфом саме цього розрізнення.',
      },
      {
        title: 'Розмір JavaScript bundle',
        text: 'Пряме продовження теми: те, наскільки високо в дереві компонентів поставлена директива, безпосередньо визначає, скільки коду опиняється в клієнтському підграфі й потрапляє в бандл браузера.',
      },
      {
        title: 'Гідратація',
        text: 'Крок, що застосовується саме до клієнтського підграфа, побудованого директивою use client; Server Component, що лишився поза цим підграфом, гідратацію взагалі не проходить.',
      },
    ],
    codeExamples: [
      {
        label: 'Директива тягне за собою залежності без власного use client',
        code: `// app/widgets/Counter.tsx
'use client' // межа module graph починається тут

import { useState } from 'react'
import { formatCount } from './formatCount'
// ⬆ потрапляє в клієнтський бандл, хоча в самому formatCount.ts
// немає жодної згадки use client

export function Counter() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(count + 1)}>{formatCount(count)}</button>
}`,
      },
    ],
    seniorNotes: [
      'use client має бути найпершим рядком файлу, до будь-яких import; якщо перед нею є інший код чи коментар певного типу, Next.js може її проігнорувати.',
      'Файл із use client, що імпортує велику стороню бібліотеку, тягне цю бібліотеку цілком у клієнтський бандл, навіть якщо в самому файлі використовується лише невелика її частина.',
      'use client у барел-файлі, що реекспортує багато компонентів, позначає клієнтським увесь цей барел і все, що через нього реекспортується, навіть компоненти, які самі по собі не використовують клієнтські хуки.',
      'Повідомлення Next.js у dev-режимі про потребу useState у компоненті, що не має use client, вказує саме на відсутність цієї директиви як кореневу причину помилки збірки.',
      'Той самий файл компонента може одночасно потрапити і в серверний, і в клієнтський бандл, якщо його імпортують з обох боків межі в різних частинах застосунку.',
    ],
  },

  {
    id: 'next-use-client-still-ssr',
    title: 'Чи означає директива use client, що компонент ніколи не рендериться на сервері?',
    definition:
      'Ні, use client не означає, що компонент ніколи не виконується на сервері: Next.js все одно рендерить Client Component на сервері один раз, щоб згенерувати початковий HTML для швидкого першого відображення. Директива лише означає, що JavaScript цього компонента додатково потрапляє в клієнтський бандл і компонент гідратується та може повторно рендеритися в браузері. Різниця з Server Component не в тому, виконується компонент на сервері чи ні, а в тому, чи існує його код у браузері після першого завантаження.',
    why: 'Плутанина тут типова, бо назва «use client» звучить як «рендерити тільки в клієнті», хоча насправді директива керує тим, куди потрапляє JavaScript, а не тим, де відбувається перший рендер.',
    simple:
      'Уяви актора, який спершу читає свою репліку на репетиції в порожньому залі, а потім виходить і повторює ту саму репліку вже перед глядачами. Client Component робить те саме: спершу він репетирує на сервері, формуючи готовий HTML, який браузер покаже миттєво, а потім, коли довантажиться JavaScript, той самий компонент виходить на сцену ще раз у браузері — гідратується й далі реагує на кліки та зміни стану вже локально. Якщо прибрати першу репетицію взагалі, тобто явно вимкнути серверний рендер через dynamic-імпорт з опцією ssr: false, компонент почне зʼявлятися лише після завантаження JavaScript, а до того на його місці буде порожньо. Це підтверджує, що серверний рендер Client Component не виняток, а типова поведінка за замовчуванням, і use client тут ні при чому: директива стосується лише того, чи включати JavaScript компонента в бандл браузера. Server Component — це той, що взагалі не має версії для браузера, а Client Component — це той, що має обидві версії одночасно: серверну для першого HTML і клієнтську для подальшої інтерактивності. Плутати «рендериться на сервері» з «є Server Component» — саме та помилка, яку перевіряють на співбесіді цим питанням.',
    related: [
      {
        title: 'Server Rendering проти React Server Components',
        text: 'Та сама плутанина розглянута ширше: «рендериться на сервері» описує техніку, застосовну і до Client Component, а не лише синонім до Server Component.',
      },
      {
        title: 'Гідратація',
        text: 'Процес, що застосовується саме через те, що Client Component уже мав серверний HTML, і React у браузері має що гідратувати, а не малювати з нуля.',
      },
      {
        title: 'dynamic import з опцією ssr: false',
        text: 'Явний і єдиний спосіб дійсно прибрати серверний рендер для конкретного Client Component, коли він залежить від window чи бібліотеки без підтримки серверного виконання.',
      },
    ],
    codeExamples: [
      {
        label: 'Типова поведінка проти явного винятку',
        code: `// За замовчуванням Client Component рендериться на сервері один раз
'use client'
export function Clock() {
  const [time, setTime] = useState(() => new Date())
  return <span>{time.toLocaleTimeString()}</span>
}

// ❌ якщо компонент має рендеритись ЛИШЕ в браузері (наприклад, залежить
// від window чи бібліотеки без підтримки серверного рендера),
// серверний рендер вимикають явно
const ClientOnlyMap = dynamic(() => import('./Map'), { ssr: false })`,
      },
    ],
    seniorNotes: [
      'dynamic(() => import(...), { ssr: false }) — це явний і єдиний вбудований спосіб дійсно прибрати серверний рендер для Client Component, а не сама директива use client.',
      'Client Component, що звертається до window чи document напряму без перевірки чи useEffect, падає під час серверного рендера, бо цей серверний прохід усе одно відбувається за замовчуванням.',
      'Гідратація спирається на припущення, що серверний HTML Client Component і його перший клієнтський рендер збігаються; dynamic з ssr: false цю умову просто прибирає, бо серверного рендера немає взагалі.',
      'Server Component ніколи не має клієнтської версії коду в принципі, тоді як Client Component завжди має обидві версії одночасно — серверну для першого HTML і клієнтську для подальшої роботи.',
    ],
  },

  {
    id: 'next-rsc-payload',
    title: 'Що таке RSC Payload, яку інформацію він містить і навіщо потрібен?',
    definition:
      'RSC Payload — це спеціальний серіалізований формат, яким React описує дерево серверних компонентів: він містить не готовий HTML, а компактний опис того, які компоненти відрендерились, якими даними їх наповнити і де в дереві починаються межі Client Component. Він передається браузеру разом із початковим HTML під час першого завантаження, а також окремо — під час навігації між сторінками чи виклику router.refresh(), коли новий HTML взагалі не потрібен. Навіщо він потрібен: саме RSC Payload дозволяє React у браузері доповнити чи оновити дерево компонентів, не перевиконуючи Server Component і не перезавантажуючи сторінку цілком.',
    why: 'Без окремого формату опису дерева React довелося б або відправляти повний HTML на кожну навігацію, витрачаючи трафік на розмітку, яка й так не змінилася, або взагалі не мати способу оновити лише частину сторінки, керовану сервером, без перезавантаження всього документа.',
    simple:
      'Уяви, що архітектор передає будівельникам не готовий будинок, а креслення: на ньому позначено, де стіни, де вікна, а де залишені порожні прорізи для дверей, які встановить уже окрема бригада. RSC Payload — це і є таке креслення дерева компонентів: React серіалізує результат виконання Server Components у компактний формат, що описує структуру дерева, значення пропсів і місця, де починаються Client Component — ті самі прорізи для дверей, які браузер заповнить власним JavaScript. Під час першого завантаження сторінки цей payload іде разом із готовим HTML: HTML дає миттєве візуальне відображення, а payload дає React у браузері інструкцію, як зібрати це саме дерево в памʼяті, щоб потім керувати ним. Під час подальшої навігації всередині застосунку, коли користувач переходить на інший маршрут через Link, сервер може віддати лише новий RSC Payload без нового HTML-документа — браузер сам домальовує різницю, і сторінка оновлюється без класичного перезавантаження. Те саме відбувається, коли викликають router.refresh(): React заново запитує RSC Payload для поточного маршруту, щоб перевиконати Server Components і підхопити свіжі дані, не втрачаючи стан Client Component на сторінці. Формат RSC Payload не JSON у звичайному розумінні: він підтримує потокову передачу частинами й посилання на ще не завершені проміси, що й дозволяє йому працювати разом зі стрімінгом через Suspense.',
    related: [
      {
        title: 'Streaming SSR і Suspense',
        text: 'Механізм, з яким RSC Payload узгоджений на рівні формату: обидва вміють передавати дані частинами, тому динамічний вміст може дотягуватись у вже показану сторінку поступово.',
      },
      {
        title: 'Гідратація',
        text: 'Процес, що спирається саме на дані з RSC Payload: React знає, які вузли DOM відповідають Client Component і потребують навішування обробників подій, завдяки інформації з payload.',
      },
      {
        title: 'router.refresh()',
        text: 'Клієнтський виклик, що повторно запитує RSC Payload для поточного маршруту без перезавантаження сторінки, — практичний приклад того, навіщо цей формат існує окремо від HTML.',
      },
    ],
    codeExamples: [
      {
        label: 'router.refresh() запитує лише новий RSC Payload',
        code: `// app/dashboard/RefreshButton.tsx
'use client'
import { useRouter } from 'next/navigation'

export function RefreshButton() {
  const router = useRouter()
  // ✅ повторно запитує лише RSC Payload цього маршруту, без перезавантаження
  // сторінки і без втрати стану інших Client Component на ній
  return <button onClick={() => router.refresh()}>Оновити</button>
}`,
      },
    ],
    seniorNotes: [
      'Формат RSC Payload — це власний серіалізований формат React, а не звичайний JSON: він підтримує потокові чанки й посилання на проміси, що резолвляться пізніше.',
      'Prefetch посилання Link, коли воно зʼявляється в полі зору, завантажує RSC Payload цього маршруту заздалегідь, тому клієнтська навігація виглядає миттєвою вже під час першого кліку.',
      'router.refresh() оновлює дерево, зливаючи новий RSC Payload з поточним, а не перемонтовуючи компоненти заново, тому локальний стан useState у Client Component на сторінці зберігається.',
      'Пропси Client Component, серіалізовані в RSC Payload, підпорядковані тим самим обмеженням серіалізації, що й будь-яка передача даних через межу Server і Client Component: функції в них не помістяться.',
      'У мережевій вкладці браузера запит за RSC Payload видно окремо від запиту HTML-документа — це окрема відповідь зі своїм форматом, яку Next.js генерує спеціально для клієнтської навігації.',
    ],
  },

  {
    id: 'next-first-load-html-rsc-js',
    title:
      'Опиши, як HTML, RSC Payload та JavaScript взаємодіють під час першого завантаження сторінки',
    definition:
      'Під час першого завантаження сервер одночасно готує й відправляє HTML і RSC Payload в одній потоковій відповіді: HTML дає браузеру що показати миттєво, а RSC Payload — інструкцію, з яких компонентів складається дерево і де в ньому починаються межі Client Component. Браузер спершу малює отриманий HTML без жодного JavaScript — сторінка вже видима, хоча ще не інтерактивна. Паралельно довантажується JavaScript-бандл: React використовує вже наявний RSC Payload, щоб зрозуміти структуру дерева, а сам бандл — щоб отримати код Client Component, і гідратує саме ці компоненти поверх уже намальованого HTML, не перемальовуючи його.',
    why: 'Ці три частини — HTML, RSC Payload і JavaScript — навмисно розвʼязані одна від одної й довантажуються незалежно, тому користувач бачить контент раніше, ніж застосунок стає повністю інтерактивним, а не чекає одну монолітну відповідь, що містить усе одразу.',
    simple:
      'Уяви, що тобі одночасно надсилають фотографію кімнати, план меблювання з підписами й самі меблі окремою вантажівкою. Фотографія — це HTML: побачивши її, ти вже розумієш, як виглядає кімната, і можеш почати нею милуватися, навіть якщо меблі ще в дорозі. План з підписами — це RSC Payload: він описує, які саме меблі де стоять і які з них рухомі, тобто мають ручки й колеса, а які просто частина стін. Вантажівка з рухомими меблями — це JavaScript-бандл: щойно вона приїжджає, робітники звіряються з планом і встановлюють ручки та коліщатка саме на ті предмети, що позначені як рухомі, не чіпаючи решту кімнати. У браузері це відбувається так: спершу приходить потокова HTML-відповідь, і сторінка стає видимою практично миттєво, хоча жоден клік поки що нічого не робить. Услід за HTML чи разом із ним приходить RSC Payload, за яким React будує внутрішнє уявлення дерева компонентів, знаючи наперед, де саме в цьому дереві розташовані Client Component. Коли довантажується JavaScript-бандл із кодом самих Client Component, React проходить по HTML і навішує обробники подій рівно на ті вузли, що описані в payload як клієнтські, — це і є гідратація, останній із трьох етапів. Якщо будь-яка з трьох частин запізнюється, сторінка не ламається: вона просто лишається видимою, але не до кінця інтерактивною, доки не прибуде відсутня частина.',
    related: [
      {
        title: 'RSC Payload',
        text: 'Другий з трьох елементів цієї взаємодії — формат, що описує дерево компонентів і межі Client Component, без якого React у браузері не знав би, що саме гідратувати.',
      },
      {
        title: 'Гідратація',
        text: 'Завершальний етап описаної взаємодії: момент, коли JavaScript-бандл і RSC Payload разом дозволяють React навісити обробники подій на вже показаний HTML.',
      },
      {
        title: 'Streaming SSR і Suspense',
        text: 'Механізм, що дозволяє всім трьом частинам не чекати одна одну повністю: повільний фрагмент дерева може дотягуватися пізніше, не блокуючи показ решти сторінки.',
      },
    ],
    codeExamples: [
      {
        label: 'Три частини в межах одного дерева компонентів',
        code: `// app/page.tsx
export default async function HomePage() {
  const data = await getData() // виконується на сервері до відправки HTML

  return (
    <div>
      <Hero data={data} /> {/* частина HTML, видима одразу */}
      <InteractiveWidget />
      {/* 1. HTML для InteractiveWidget теж прийшов одразу (SSR-версія) */}
      {/* 2. RSC Payload позначив цей вузол як межу Client Component    */}
      {/* 3. після завантаження бандла відбувається гідратація         */}
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Клік по ще не гідратованому елементу не втрачається: React 18+ ставить подію в чергу через selective hydration і застосовує її, щойно код саме цього фрагмента гідратується.',
      'App Router здатний віддати HTML і RSC Payload в одному потоковому HTTP-зʼєднанні, а не обовʼязково двома окремими round trip навіть при першому завантаженні.',
      'Якщо JavaScript узагалі не завантажиться, наприклад через блокувальник реклами, HTML лишається видимим і читабельним, але жодна інтерактивність не зʼявиться — це базовий рівень деградації.',
      'Серверний HTML Client Component і його перший клієнтський рендер мають повністю збігатися, інакше React логує hydration mismatch і може відкинути та перемалювати цей фрагмент DOM.',
      'Попередньо завантажений через Link у полі зору маршрут уже має закешований на клієнті RSC Payload до кліку, тому подальша навігація пропускає крок очікування на цю частину відповіді.',
    ],
  },

  {
    id: 'next-hydration-selective-mismatch',
    title: 'Що таке hydration, selective hydration та hydration mismatch?',
    definition:
      'Hydration — це процес, коли React у браузері навішує обробники подій на вже готову серверну розмітку, не перемальовуючи її заново, перетворюючи статичний HTML на інтерактивний інтерфейс. Selective hydration — це вдосконалення цього процесу в React 18 і новіших версіях, коли гідратація різних частин дерева відбувається незалежно й у пріоритетному порядку: частина, на яку користувач клацнув чи яка зʼявилась у полі зору, гідратується першою, не чекаючи решту сторінки. Hydration mismatch — це помилка, коли розмітка, згенерована на сервері, і розмітка, яку той самий компонент видає під час першого рендера в браузері, відрізняються, через що React не може безпечно повторно використати вже намальований HTML.',
    why: 'Без гідратації сервер мусив би або взагалі не давати інтерактивності, або дублювати весь рендер повністю в браузері, ігноруючи вже готовий HTML; без selective hydration весь застосунок чекав би на найповільніший фрагмент JavaScript, перш ніж стати інтерактивним хоч десь.',
    simple:
      'Уяви велику виставу, де на сцені вже стоять статичні декорації, а актори мають зайняти свої місця й почати рухатися. Просте hydration — це коли всі актори виходять на сцену одночасно, рівно за одним сигналом, і лише тоді вистава оживає повністю. Selective hydration — це коли режисер дозволяє актору, чия репліка потрібна найближчим часом, вийти на сцену першим і почати діяти негайно, не чекаючи, поки решта акторів теж будуть готові. У React це виглядає так: якщо користувач клацає на ще не гідратований Client Component, React ставить цю подію в чергу і, щойно JavaScript цього конкретного компонента довантажиться, гідратує саме його з підвищеним пріоритетом, а не чекає завершення гідратації всієї сторінки по порядку. Hydration mismatch — це вже не про швидкість, а про коректність: якщо серверний HTML для компонента виглядає інакше, ніж те, що той самий компонент рендерить при першому виконанні в браузері, React не може довірити вже намальованій розмітці й або показує попередження в консолі, або перемальовує розбіжний фрагмент заново. Класична причина розбіжності — код, що залежить від часу, випадкових чисел чи браузерних API під час першого рендера, наприклад new Date() чи Math.random() прямо в JSX, бо сервер і браузер виконують цей код у різний момент і отримують різні значення.',
    related: [
      {
        title: 'RSC Payload',
        text: 'Джерело інформації, за яким React у браузері знає, які саме вузли DOM відповідають Client Component і потребують гідратації, а не лише статичного відображення.',
      },
      {
        title: 'Діагностика hydration errors',
        text: 'Практичне продовження цієї теми: як саме знайти й виправити конкретну причину розбіжності, коли hydration mismatch уже стався в реальному застосунку.',
      },
      {
        title: 'suppressHydrationWarning',
        text: 'Проп React, що глушить попередження про розбіжність для конкретного DOM-елемента, коли розбіжність очікувана й свідома, наприклад для часового штампа чи theme-атрибута.',
      },
    ],
    codeExamples: [
      {
        label: 'Типова причина mismatch і правильний фікс',
        code: `// ❌ значення відрізняється між сервером і браузером
export function Timestamp() {
  return <span>{new Date().toLocaleTimeString()}</span> // hydration mismatch
}

// ✅ значення обчислюється лише після монтування в браузері
'use client'
import { useEffect, useState } from 'react'

export function Timestamp() {
  const [time, setTime] = useState<string | null>(null)
  useEffect(() => setTime(new Date().toLocaleTimeString()), [])
  return <span>{time ?? '—'}</span>
}`,
      },
    ],
    seniorNotes: [
      'suppressHydrationWarning глушить попередження лише для текстового вмісту конкретного елемента, на якому він стоїть, а не для його дочірніх елементів чи всього піддерева.',
      'Браузерні розширення, що вставляють атрибути в DOM до гідратації, наприклад менеджери паролів чи Grammarly, — поширена причина hydration mismatch, ніяк не повʼязана з кодом самого застосунку.',
      'Selective hydration у React 18 спирається саме на межі Suspense як одиниці незалежної гідратації: без Suspense навколо повільного компонента його гідратація все одно блокує звичайний порядок згори вниз.',
      'Next.js офіційно рекомендує suppressHydrationWarning саме для атрибутів тегів html чи body, куди сторонній скрипт теми може вставити клас чи стиль ще до гідратації React.',
      'Після mismatch React відкидає серверну розмітку розбіжного піддерева і перемальовує його заново в браузері з нуля, що може викликати помітний спалах іншого вмісту.',
    ],
  },

  {
    id: 'next-hydration-errors-diagnosis',
    title: 'Чому можуть виникати hydration errors і як ви їх діагностуєте?',
    definition:
      'Hydration errors виникають, коли компонент рендерить різний результат на сервері й під час першого виконання в браузері: причиною найчастіше є код, що залежить від часу, випадковості, локалі користувача чи браузерних API, недоступних на сервері. Діагностують їх за повідомленням React у консолі браузера, яке в React 18 і новіших версіях показує саме місце розбіжності — конкретний DOM-вузол і атрибут чи текст, що не збігається. Другий типовий інструмент діагностики — порівняння HTML, отриманого від сервера, з рендером у браузері вручну: перегляд вихідного коду сторінки проти того, що показує React DevTools після гідратації.',
    why: 'Hydration error, залишена без уваги, не завжди ламає застосунок видимо: React може мовчки замінити розбіжний фрагмент, і баг проявиться лише як дивний блимаючий контент чи втрачений стан, який складно повʼязати із самою гідратацією без цілеспрямованої діагностики.',
    simple:
      'Уяви коректора, який звіряє два примірники одного тексту — чернетку, надруковану вранці, і чистовик, надрукований щойно, — і підкреслює кожне слово, де вони розходяться. React під час гідратації робить те саме: порівнює HTML, який прийшов від сервера, із тим, що той самий компонент видав би, якби рендерився в браузері просто зараз. Найчастіші джерела розбіжності — виклики, чутливі до часу виконання: new Date() чи Math.random() прямо в розмітці, перевірки typeof window, які на сервері завжди false, а в браузері true, або дані, що залежать від locale чи часового поясу користувача, які на сервері й у браузері можуть відрізнятися. Ще одна поширена причина — некоректний HTML, наприклад тег p усередині іншого тега p, який браузер автоматично виправляє під час парсингу, тому DOM, отриманий React у браузері, вже відрізняється від того, що сервер надіслав. Для діагностики React 18 і новіші версії показують у консолі не просто загальне повідомлення про помилку, а конкретний DOM-вузол і атрибут, де сталася розбіжність, що значно звужує пошук. Практичний підхід — відкрити вкладку Elements чи переглянути вихідний HTML сторінки й порівняти його з тим, що показує застосунок після повного завантаження: розбіжність зазвичай видно неозброєним оком саме в цьому порівнянні.',
    related: [
      {
        title: 'Hydration mismatch',
        text: 'Сама помилка, причини й діагностику якої розглядає ця тема; без розуміння механізму mismatch діагностика перетворюється на випадковий перебір гіпотез.',
      },
      {
        title: 'Перевірки typeof window',
        text: 'Типове джерело розбіжності: значення завжди відрізняється між сервером, де window немає, і браузером, де він є, тому пряме використання такої перевірки в JSX ризиковане.',
      },
      {
        title: 'suppressHydrationWarning',
        text: 'Інструмент для свідомого приглушення попередження в тих рідкісних випадках, коли розбіжність очікувана й нешкідлива, а не спосіб виправити реальну причину помилки.',
      },
    ],
    codeExamples: [
      {
        label: 'Діагностика через typeof window і виправлення',
        code: `// ❌ на сервері isBrowser завжди false, у браузері — true: mismatch
export function Widget() {
  const isBrowser = typeof window !== 'undefined'
  return <div>{isBrowser ? <RichWidget /> : <SimpleWidget />}</div>
}

// ✅ обидва рендери, і серверний, і перший клієнтський, дають однаковий
// результат; різниця застосовується лише після монтування
'use client'
import { useEffect, useState } from 'react'

export function Widget() {
  const [mounted, setMounted] = useState(false)
  useEffect(() => setMounted(true), [])
  return <div>{mounted ? <RichWidget /> : <SimpleWidget />}</div>
}`,
      },
    ],
    seniorNotes: [
      'React 18 і новіші версії називають у консолі конкретний тег і проп, де сталася розбіжність, тоді як React 17 обмежувався загальним повідомленням без точної локації.',
      'Некоректна вкладеність HTML-тегів, наприклад div усередині p чи a усередині a, викликає hydration errors, бо власний HTML-парсер браузера перебудовує DOM ще до того, як React його побачить.',
      'Сторонні браузерні розширення, що змінюють DOM до гідратації — блокувальники реклами, менеджери паролів, Grammarly — дають попередження, не повʼязані з жодним багом у коді самого застосунку.',
      'Обгортання значення часу чи випадкового числа в стан, що встановлюється всередині useEffect, — стандартний фікс, бо useEffect виконується лише після гідратації на клієнті, ніколи під час серверного проходу.',
      'Оверлей помилок Next.js у dev-режимі привʼязує hydration error напряму до файлу й рядка через стек компонентів React, що пришвидшує пошук порівняно з читанням сирого diff DOM.',
    ],
  },

  {
    id: 'next-server-components-as-children',
    title: 'Як правильно передавати Server Components через children у Client Component?',
    definition:
      'Server Component передають у Client Component не як прямий імпорт усередині клієнтського файлу, а як уже відрендерений елемент через проп children чи інший React-проп: батьківський Server Component рендерить дочірній Server Component і передає результат у Client Component ззовні. Це працює, бо Client Component у такому разі не імпортує й не виконує Server Component самостійно — він лише отримує вже готове React-дерево як значення й вставляє його у своє власне розташування в розмітці. Такий підхід дозволяє огорнути серверний контент клієнтською інтерактивністю, наприклад модальним вікном чи акордеоном, не перетворюючи сам серверний контент на клієнтський код.',
    why: 'Пряма спроба імпортувати Server Component усередині файлу з use client не має сенсу технічно: увесь клієнтський файл і все, що він імпортує, автоматично стає частиною клієнтського підграфа, тому Server Component, імпортований туди напряму, втратив би саме ті серверні можливості, заради яких він Server Component.',
    simple:
      'Уяви подарункову коробку, яку пакує один працівник, а стрічку на неї зверху повʼязує інший. Якщо працівник зі стрічкою спробує сам відкрити коробку і переупакувати вміст, він фактично забере цю роботу собі, а разом з нею й усі обмеження свого робочого місця, де немає доступу до складу з подарунками. Натомість правильний порядок: перший працівник пакує коробку повністю на своєму робочому місці зі складу, а другому просто передає вже готову закриту коробку, щоб той повʼязав стрічку зверху, не заглядаючи всередину. У коді роль коробки грає Server Component, а роль стрічки — Client Component: батьківський Server Component рендерить дочірній Server Component так само, як завжди, а потім передає результат у Client Component через проп children, а не через import у файлі з use client. Client Component при цьому не бачить і не виконує код усередині children — він отримує вже готовий React-елемент і просто вставляє його в потрібне місце своєї розмітки, наприклад усередині модального вікна чи обгортки з анімацією. Такий підхід типово застосовують для layout-патернів: Client Component відповідає за інтерактивну обгортку — відкриття, закриття, анімацію, а весь вміст усередині лишається Server Component і зберігає прямий доступ до бази даних чи файлової системи.',
    related: [
      {
        title: 'Директива use client і module graph',
        text: 'Пояснює, чому прямий import Server Component усередині клієнтського файлу небажаний: він автоматично затягнув би цей компонент у клієнтський підграф разом з усіма його залежностями.',
      },
      {
        title: 'Серіалізація пропсів на межі Server і Client Components',
        text: 'Проп children у цьому патерні — не виняток із загальних правил: він так само має бути результатом рендера, а не функцією чи довільним обʼєктом, переданим напряму.',
      },
      {
        title: 'Композиція компонентів через children',
        text: 'Загальний React-патерн, на якому побудований цей прийом: children як спосіб передати вже готовий вміст усередину компонента-обгортки, застосовний і поза межею Server і Client Components.',
      },
    ],
    codeExamples: [
      {
        label: 'Server Component усередині Client Component-обгортки',
        code: `// app/layout.tsx — Server Component
import { Modal } from './Modal'
import { UserProfile } from './UserProfile' // теж Server Component

export default function Layout() {
  return (
    <Modal>
      {/* ✅ UserProfile рендериться як Server Component і передається
          готовим елементом, а не імпортується всередині Modal */}
      <UserProfile />
    </Modal>
  )
}

// app/Modal.tsx — Client Component
'use client'
import { useState } from 'react'

export function Modal({ children }) {
  const [open, setOpen] = useState(false)
  return (
    <div>
      <button onClick={() => setOpen(true)}>Відкрити</button>
      {open && <div className="modal">{children}</div>}
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Патерн працює, бо children — звичайний проп із уже обчисленим React-елементом, а не функцією чи посиланням на модуль: його значення повністю готове ще до того, як Client Component почне рендеритись.',
      'Той самий прийом застосовний до будь-якого пропа, не лише children: Server Component можна передати як header={<ServerHeader />} з тими самими правилами.',
      'Server Component, переданий через children, лишається повністю серверним: він не додає жодного байта до клієнтського бандла і не отримує пропси, які Client Component-обгортка передає йому пізніше з власного стану.',
      'Пряма спроба імпортувати файл Server Component усередині файлу з use client або падає з помилкою збірки, або перетворює його на клієнтську версію — залежно від того, чи сумісний код цього файлу з клієнтським середовищем.',
    ],
  },

  {
    id: 'next-serialization-boundary-limits',
    title: 'Які обмеження серіалізації існують на межі Server і Client Components?',
    definition:
      'Пропси, що передаються від Server Component до Client Component, мають бути серіалізовними в RSC Payload: підтримуються примітиви, звичайні обʼєкти й масиви, Date, Map, Set і Promise, але не підтримуються функції, класи, Symbol чи циклічні посилання. Спроба передати функцію-колбек чи інстанс довільного класу як проп викликає помилку під час рендера, бо React фізично не може серіалізувати виконуваний код чи довільну поведінку класу в текстовий формат, який розуміє клієнт. У зворотному напрямку, від Client до Server Component, передача пропсів взагалі неможлива напряму: Client Component не може імпортувати й рендерити Server Component із власними пропсами, лише отримати вже готовий елемент через children чи подібний проп.',
    why: 'Без цих обмежень довелося б або серіалізувати довільний виконуваний JavaScript-код у текстовий формат, що небезпечно й нетривіально, або створювати ілюзію, що функція, передана через межу, справді виконається на тому боці, де вона фізично ніколи не існувала.',
    simple:
      'Уяви лист, який пересилають поштою: у ньому можна написати текст, вкласти фотографію чи документ, але не можна вкласти саму людину чи дію, яку хтось має виконати особисто. Межа між Server і Client Component працює так само: React серіалізує пропси в щось на кшталт такого листа, тому в нього поміщаються дані — рядки, числа, обʼєкти, масиви, навіть Date чи Map, але не поміщається функція, бо функція це не дані, а інструкція, привʼязана до конкретного місця виконання в памʼяті сервера. Якщо спробувати передати функцію-колбек із Server Component у Client Component як проп, React кине помилку ще під час рендера, бо не має способу переслати саму функцію через цю межу. Це особливо помітно новачкам, що звикли передавати onClick чи подібні колбеки як звичайні пропси в React: у звичайному дереві клієнтських компонентів це працює, бо все виконується в одному середовищі, а на межі Server і Client Component цих середовищ уже два різні, і функція не може перестрибнути з одного в інше. Зворотний напрямок обмежений ще жорсткіше: Client Component взагалі не може імпортувати файл Server Component і відрендерити його з довільними пропсами всередині себе — єдиний легальний спосіб отримати серверний контент усередині клієнтського дерева це прийняти вже готовий елемент через children чи інший проп, обчислений батьківським Server Component заздалегідь. Практичний вихід для функції, яку нібито треба передати на клієнт, це Server Action: спеціальна серверна функція, яку Next.js серіалізує не як звичайний колбек, а як посилання, що клієнт може викликати мережевим запитом.',
    related: [
      {
        title: 'Server Actions',
        text: 'Єдиний легальний спосіб передати щось схоже на функцію через межу Server і Client Component: серіалізується не сам код, а посилання, яке клієнт викликає мережевим запитом.',
      },
      {
        title: 'RSC Payload',
        text: 'Формат, у межах якого й діють ці обмеження серіалізації; саме структура цього формату визначає, які типи даних можна, а які не можна передати через межу.',
      },
      {
        title: 'Server Components через children',
        text: 'Практичний спосіб обійти обмеження на передачу Server Component у Client Component: не через проп із довільним обʼєктом, а через уже відрендерений елемент.',
      },
    ],
    codeExamples: [
      {
        label: 'Функцію не можна передати, Server Action — можна',
        code: `// ❌ функцію не можна передати як проп із Server у Client Component
export default function Page() {
  const handleClick = () => console.log('клік')
  return <Button onClick={handleClick} /> // помилка серіалізації
}

// ✅ Server Action серіалізується як спеціальне посилання, а не як функція
export default function Page() {
  async function like(id: string) {
    'use server'
    await db.like.create({ data: { id } })
  }
  return <LikeButton onLike={like} /> // це дозволено
}`,
      },
    ],
    seniorNotes: [
      'RSC-серіалізація підтримує Date, Map, Set і Promise, який використовується для стрімінгу через Suspense, але явно відхиляє функції, класи, Symbol і циклічні посилання з помилкою під час рендера.',
      'Функція з директивою use server — єдиний виняток: Next.js серіалізує її як ідентифікатор дії, яку клієнт викликає мережевим запитом, а не як буквальний виконуваний код.',
      'Date, переданий через межу Server і Client Component, доходить до клієнта як повноцінний обʼєкт Date, відновлений під час десеріалізації, без ручного JSON.stringify чи парсингу рядка.',
      'Циклічне посилання в обʼєкті пропсів, наприклад вузол, що посилається сам на свого батька, кидає помилку серіалізації RSC, хоча той самий обʼєкт спокійно існував би в памʼяті між двома звичайними клієнтськими компонентами.',
    ],
  },

  {
    id: 'next-use-client-bundle-size',
    title: 'Як розміщення директиви use client впливає на розмір JavaScript bundle?',
    definition:
      'Розміщення use client визначає, з якого файлу починається клієнтський підграф module graph, а отже й скільки коду туди затягне: чим вище по дереву компонентів поставлена директива, тим більше залежностей — власних компонентів і бібліотек — автоматично потрапляє в клієнтський бандл разом із нею. Якщо директиву поставити на весь великий компонент сторінки заради однієї маленької кнопки всередині, у бандл потрапить увесь код цього компонента й усе, що він імпортує, а не лише логіка кнопки. Правильна практика — опускати use client якомога глибше по дереву, до найменшого листового компонента, якому справді потрібна інтерактивність, лишаючи решту дерева серверною і бандл — меншим.',
    why: 'Кожен зайвий кілобайт у клієнтському бандлі — це додатковий час завантаження й парсингу JavaScript на повільному зʼєднанні чи слабкому пристрої, тому розмір бандла напряму впливає на те, як швидко сторінка стає інтерактивною, а не лише видимою.',
    simple:
      'Уяви, що потрібно перевезти один маленький сувенір із одного міста в інше, але замість окремої посилки цей сувенір кладуть у величезну валізу разом з усім іншим одягом, бо валіза все одно їде тим самим потягом. Так само й з use client: якщо поставити директиву на компонент високо в дереві, у клієнтський бандл поїде не тільки потрібна інтерактивна частина, а й усе, що цей великий компонент імпортує далі, навіть код, що сам по собі міг би спокійно лишитися на сервері. Правильний підхід — виносити інтерактивну частину в окремий, максимально малий компонент-листок і ставити use client саме на нього, а не на весь батьківський компонент, у якому ця кнопка чи форма лише одна маленька деталь серед переважно статичного вмісту. Наприклад, сторінка статті може лишатися повністю Server Component, а use client поставити лише на невеликий компонент лайка, не зачіпаючи ні заголовок, ні текст статті, ні коментарі, якщо вони самі не потребують інтерактивності. Це особливо помітно з важкими бібліотеками: графік, редактор тексту чи бібліотека дат, підключена всередині компонента з use client, повністю потрапляє в клієнтський бандл, тоді як та сама бібліотека, використана лише для форматування даних усередині Server Component, ніколи не покидає сервер і взагалі не впливає на розмір JavaScript, який завантажує браузер. Практичний спосіб перевірити результат — подивитися стовпець First Load JS у таблиці виводу next build для конкретного маршруту до й після перенесення директиви вглиб дерева.',
    related: [
      {
        title: 'Директива use client і module graph',
        text: 'Механізм, що напряму пояснює цей ефект: клієнтський бандл — це підграф, побудований від файлу з use client, тому позиція цієї директиви буквально визначає межі підграфа.',
      },
      {
        title: 'next build',
        text: 'Команда, чия таблиця виводу з колонкою First Load JS — головний практичний інструмент виміряти реальний вплив перенесення use client на розмір бандла конкретного маршруту.',
      },
      {
        title: 'Server Component проти Client Component',
        text: 'Базове розрізнення, наслідком якого і є цей ефект: код, що лишається на боці Server Component, узагалі не враховується в розмір клієнтського JavaScript незалежно від власного розміру.',
      },
    ],
    codeExamples: [
      {
        label: 'use client високо в дереві проти use client на листку',
        code: `// ❌ use client на всій сторінці — вона й усе, що імпортує,
// потрапляє в клієнтський бандл заради однієї кнопки лайка
'use client'
import { HeavyMarkdownRenderer } from 'heavy-markdown-lib'

export default function ArticlePage({ article }) {
  const [liked, setLiked] = useState(false)
  return (
    <div>
      <HeavyMarkdownRenderer content={article.body} />
      <button onClick={() => setLiked(!liked)}>{liked ? '❤️' : '🤍'}</button>
    </div>
  )
}

// ✅ use client лише на маленькому листовому компоненті
export default function ArticlePage({ article }) {
  return (
    <div>
      <HeavyMarkdownRenderer content={article.body} /> {/* лишається на сервері */}
      <LikeButton /> {/* єдина клієнтська частина */}
    </div>
  )
}`,
      },
    ],
    seniorNotes: [
      'Колонка First Load JS у виводі next build для конкретного маршруту — стандартний спосіб виміряти реальний ефект перенесення use client вище чи нижче по дереву компонентів.',
      'Важка бібліотека, імпортована лише всередині Server Component, наприклад для конвертації markdown у HTML, ніколи не потрапляє в браузер, на відміну від тієї самої бібліотеки, підключеної в межах Client Component.',
      'Барел-файл, що реекспортує багато компонентів і сам має use client, мовчки затягує в клієнтський бандл кожен реекспортований компонент, навіть той, що окремо не використовує жодного клієнтського хука.',
      'Перенесення use client на листовий компонент не лише зменшує бандл, а й лишає решту дерева Server Component здатною звертатися до даних напряму, без додаткового клієнтського fetch і зайвого request waterfall.',
    ],
  },

  {
    id: 'next-caching-layers-overview',
    title: 'Які існують рівні кешування в Next.js і за що відповідає кожен із них?',
    definition:
      'У Next.js кешування працює на кількох незалежних рівнях із різною тривалістю життя й місцем зберігання. Request Memoization дедублює однакові fetch-виклики в межах одного рендера на сервері. Data Cache зберігає результати fetch між запитами й навіть між деплоями, а Full Route Cache — готовий HTML і RSC Payload статичних маршрутів на сервері. Router Cache тримає RSC Payload уже відвіданих маршрутів у памʼяті браузера для миттєвої навігації, а в новій моделі Cache Components ці шари обʼєднуються довкола директиви use cache з явним cacheLife і cacheTag.',
    why: 'Без розуміння того, що саме кешується, де й на який термін, розробник не може передбачити, чи зміна в базі даних дійде до користувача одразу, за хвилину чи лише після редеплою, і легко плутає причину, чому сторінка показує застарілі дані.',
    simple:
      'Уяви ланцюжок постачання їжі від ферми до тарілки, де на кожному етапі свій склад із власним терміном зберігання. Request Memoization — це стіл кухаря просто зараз: якщо рецепт двічі просить одну й ту саму нарізану цибулю в межах одного приготування страви, кухар не ріже її вдруге, а бере вже нарізану, але щойно страва готова, цей стіл прибирають і про цибулю забувають. Data Cache — це холодильник ресторану: інгредієнт, покладений туди, лежить довго, переживає навіть зміну кухаря на зміні, тобто новий деплой застосунку, доки хтось спеціально не викине його як застарілий. Full Route Cache — це вже готові страви на роздачі: замість готувати той самий салат для кожного відвідувача заново, кухня один раз готує його наперед і виносить копію кожному, хто замовив те саме. Router Cache — це те, що вже лежить у тебе вдома після попереднього візиту в ресторан: якщо ти йдеш туди повторно найближчим часом, частину даних браузер бере з власної памʼяті, навіть не питаючи сервер. Ці чотири шари існують у класичній моделі Next.js, а нова модель Cache Components переосмислює їх довкола однієї директиви use cache, де замість неявних правил fetch розробник явно позначає, що саме кешувати і на який термін через cacheLife та cacheTag.',
    related: [
      {
        title: 'Request Memoization, Data Cache, Full Route Cache, Router Cache',
        text: 'Детальне порівняння чотирьох шарів попередньої моделі кешування: де саме кожен живе, що саме зберігає і як інвалідація одного впливає чи не впливає на інші.',
      },
      {
        title: 'Cache Components і директива use cache',
        text: 'Новий, явний спосіб керувати кешуванням, що приходить на зміну неявним евристикам fetch і route segment config попередньої моделі.',
      },
      {
        title: 'stale-while-revalidate',
        text: 'Стратегія, що лежить в основі часової ревалідації кількох із цих шарів одразу: застарілий результат віддається миттєво, поки свіжий генерується у фоні.',
      },
    ],
    codeExamples: [
      {
        label: 'Той самий результат у двох моделях кешування',
        code: `// Попередня модель: рівні керуються опціями fetch і route segment config
export const revalidate = 3600 // впливає на Full Route Cache
const res = await fetch(url, { next: { tags: ['posts'] } }) // Data Cache

// Cache Components: та сама поведінка — одна явна директива
async function getPosts() {
  'use cache'
  cacheLife('hours')
  cacheTag('posts')
  return fetch(url).then((r) => r.json())
}`,
      },
    ],
    seniorNotes: [
      'Request Memoization — це можливість React, а не Next.js: вона застосовується лише до GET fetch у межах одного проходу рендеру й скидається одразу після його завершення.',
      'Data Cache переживає навіть новий деплой застосунку, на відміну від Full Route Cache, який скидається автоматично на кожен редеплой.',
      'Router Cache — єдиний із чотирьох шарів, що живе в браузері, а не на сервері, тому інвалідація на сервері не завжди означає миттєву зміну для вже відкритої вкладки користувача.',
      'У Next.js 15 і новіших сторінки з динамічним рендерингом за замовчуванням не потрапляють у Router Cache, на відміну від попередніх версій, де типовий час життя був довшим.',
      'Cache Components, стабільна з Next.js 16 за прапорцем cacheComponents, замінює модель «кешуємо якомога більше за замовчуванням» на протилежну: нічого не кешується, доки не позначено use cache.',
    ],
  },

  {
    id: 'next-old-cache-model-layers',
    title:
      'Чим відрізняються Data Cache, Full Route Cache, Router Cache та request memoization у попередній моделі кешування?',
    definition:
      'Request Memoization дедублює однакові GET fetch-виклики в межах одного серверного рендера й живе рівно до кінця цього рендера. Data Cache зберігає результат fetch-запиту на сервері між різними запитами й навіть між деплоями, доки його явно не інвалідують чи не позначать як no-store. Full Route Cache зберігає вже відрендерений HTML і RSC Payload статичного маршруту на сервері й скидається автоматично на кожен новий деплой, на відміну від Data Cache. Router Cache — єдиний із чотирьох, що живе в браузері, а не на сервері: він тримає RSC Payload відвіданих чи попередньо завантажених маршрутів у памʼяті клієнта для миттєвої навігації.',
    why: 'Ці чотири механізми легко сплутати, бо інвалідація одного не завжди інвалідує інші: скидання Full Route Cache ніяк не впливає на Data Cache, а revalidateTag у Route Handler не одразу оновлює Router Cache в браузері користувача.',
    simple:
      'Різницю між цими чотирма шарами найлегше запамʼятати за двома осями: де живуть дані — на сервері чи в браузері, і що саме вони зберігають — сирі дані чи вже готовий результат рендера. Request Memoization і Data Cache обидва на сервері, але Request Memoization зберігає результат виклику функції лише на час одного рендера, тоді як Data Cache зберігає саме дані fetch-запиту довго, переживаючи навіть перезапуск сервера через новий деплой. Full Route Cache теж на сервері, але зберігає не сирі дані, а вже готовий результат рендеру цілого маршруту — HTML і RSC Payload, тому його інвалідація означає, що маршрут доведеться відрендерити заново, навіть якщо дані для цього рендеру вже лежать у Data Cache. Router Cache єдиний живе в браузері: він зберігає RSC Payload маршрутів, якими користувач уже користувався чи які були попередньо завантажені через Link, і саме тому revalidateTag на сервері не завжди означає, що конкретний браузер конкретного користувача побачить зміну миттєво. Ланцюжок залежності між ними однонаправлений: інвалідація Data Cache тягне за собою інвалідацію Full Route Cache, бо результат рендеру залежав від застарілих даних, але інвалідація Full Route Cache сама по собі не чіпає Data Cache. router.refresh() на клієнті, навпаки, очищає лише Router Cache, роблячи новий запит до сервера, і взагалі не займає Data чи Full Route Cache.',
    related: [
      {
        title: 'Загальний огляд рівнів кешування',
        text: 'Ширша карта всіх чотирьох шарів разом із новою моделлю Cache Components, корисна як вхідна точка перед детальним порівнянням саме цих чотирьох механізмів.',
      },
      {
        title: 'revalidatePath і revalidateTag',
        text: 'Інструменти, якими саме інвалідують Data Cache і Full Route Cache в попередній моделі; розуміння різниці шарів пояснює, чому виклик одного й того самого API в різних місцях діє по-різному.',
      },
      {
        title: 'Cache Components і use cache',
        text: 'Нова модель, що переосмислює ці чотири шари довкола однієї директиви; корисно порівняти, які саме проблеми попередньої моделі вона вирішує.',
      },
    ],
    codeExamples: [
      {
        label: 'Кожен шар торкається окремим API',
        code: `// Data Cache: явно вмикається опцією force-cache чи next.revalidate
await fetch(url, { cache: 'force-cache' })

// Full Route Cache: залежить від того, чи маршрут статичний
export const revalidate = 3600 // скидається разом з Data Cache через годину

// Router Cache: живе в браузері, чиститься так
'use client'
import { useRouter } from 'next/navigation'
const router = useRouter()
router.refresh() // ✅ чистить лише Router Cache, новий запит до сервера`,
      },
    ],
    seniorNotes: [
      'Request Memoization застосовується лише до GET-запитів fetch і лише в межах дерева React-компонентів — вона не діє всередині Route Handlers, бо ті не є частиною цього дерева.',
      'revalidateTag чи revalidatePath, викликані в Server Action, одразу інвалідують і Router Cache відповідного маршруту, а ті самі виклики в Route Handler цього не роблять, бо Route Handler не привʼязаний до конкретного маршруту.',
      'cookies.set() чи cookies.delete() у Server Action автоматично інвалідують Router Cache, навіть без явного виклику revalidatePath чи revalidateTag, щоб маршрути, залежні від автентифікації, не лишалися застарілими.',
      'Full Route Cache очищається на кожен новий деплой, а Data Cache — ні: вона продовжує діяти й після редеплою, доки її не інвалідують явно, що часом дивує команди, що очікують повного скидання кешу на реліз.',
    ],
  },

  {
    id: 'next-fetch-default-behavior-15-16',
    title: 'Яка поведінка fetch за замовчуванням у Next.js 15–16 без Cache Components?',
    definition:
      'У Next.js 15 і 16 без увімкненого cacheComponents fetch-запит без явної опції cache за замовчуванням не кешується — поведінка еквівалентна явному cache: no-store. Це відрізняється від Next.js 13 і 14, де fetch без опцій кешувався за замовчуванням, ніби вказано force-cache. Щоб отримати кешування, потрібно явно передати cache: force-cache чи next.revalidate; без цього кожен виклик іде до джерела даних заново, а маршрут без інших причин лишатися статичним рендериться динамічно.',
    why: 'Ця зміна за замовчуванням у Next.js 15 усунула поширену категорію багів, коли розробник не усвідомлював, що дані кешуються, і бачив застарілу інформацію без жодного явного налаштування кешу у власному коді.',
    simple:
      'Уяви офіціанта, який раніше за замовчуванням запамʼятовував твоє замовлення і наступного разу автоматично приносив те саме, навіть якщо ти хотів замовити щось нове, — доводилось явно попереджати, щоб отримати свіжу страву. У Next.js 13 і 14 fetch поводився саме так: без жодних опцій він за замовчуванням кешувався, ніби ти сам написав cache: force-cache, і багато розробників про це навіть не здогадувалися, доки не натикалися на застарілі дані на проді. У Next.js 15 цю поведінку розвернули на протилежну: тепер офіціант за замовчуванням щоразу перепитує замовлення заново, і лише якщо ти явно попросиш запамʼятати це, результат кешується. Технічно fetch(url) без опцій у Next.js 15 і 16 без cacheComponents еквівалентний fetch(url, { cache: no-store }): дані завжди свіжі, а маршрут без інших причин лишатися статичним рендериться на кожен запит. Щоб повернути кешування для конкретного запиту, його явно позначають через cache: force-cache для безстрокового кешу чи next: revalidate для кешу з періодом ревалідації. Ця зміна за замовчуванням стосується лише fetch: Data Cache як механізм нікуди не подівся, просто тепер розробник має свідомо в нього щось покласти, а не покладатися на неявну поведінку.',
    related: [
      {
        title: 'Data Cache',
        text: 'Механізм, поведінку якого змінює саме ця зміна за замовчуванням: Data Cache як така не зникла, змінилося лише те, чи потрапляє в неї fetch без явних опцій.',
      },
      {
        title: 'fetchCache route segment config',
        text: 'Опція, що дозволяє повернути старе кешування за замовчуванням для всього route segment одразу, не змінюючи кожен окремий виклик fetch у коді.',
      },
      {
        title: 'Cache Components і use cache',
        text: 'Ще один крок у тому самому напрямку: якщо Next.js 15 зробив некешованим fetch за замовчуванням, Cache Components робить некешованим взагалі все, доки не позначено use cache.',
      },
    ],
    codeExamples: [
      {
        label: 'Поведінка за замовчуванням до і після Next.js 15',
        code: `// Next.js 13-14: без опцій fetch кешувався за замовчуванням
const data = await fetch(url) // неявно 'force-cache'

// Next.js 15-16 без cacheComponents: без опцій fetch НЕ кешується
const data2 = await fetch(url) // неявно 'no-store'

// Щоб повернути кешування, потрібно вказати явно
const cached = await fetch(url, { cache: 'force-cache' })
const revalidated = await fetch(url, { next: { revalidate: 3600 } })`,
      },
    ],
    seniorNotes: [
      'Цю зміну за замовчуванням у Next.js 15 задокументовано як поведінкову зміну в гайді з оновлення, разом з аналогічною зміною для GET Route Handlers, які так само стали некешованими за замовчуванням у тому самому релізі.',
      "Route segment config fetchCache зі значенням default-cache дозволяє повернути старе «кешуємо, якщо не сказано інакше» для всього маршруту одразу, не редагуючи кожен окремий виклик fetch.",
      'Навіть за нового некешованого за замовчуванням fetch, Request Memoization усе одно дедублює однакові GET-виклики в межах одного проходу рендеру — просто це дедуплікація без збереження між запитами.',
      'Ця зміна стосується лише самого fetch: функції, обгорнуті в unstable_cache, і явно налаштований Data Cache цією зміною за замовчуванням не зачіпаються.',
    ],
  },

  {
    id: 'next-stale-while-revalidate',
    title: 'Що таке stale-while-revalidate і в яких сценаріях воно корисне?',
    definition:
      'Stale-while-revalidate — це стратегія кешування, за якою застарілий кешований результат одразу віддається користувачу, а оновлення даних відбувається у фоні, і лише наступний запит після завершення оновлення отримує свіжу версію. У Next.js ця стратегія лежить в основі revalidate у fetch, ISR та профілю revalidateTag з аргументом max: жоден користувач не чекає на повільний перерахунок, натомість хтось один отримує трохи застарілі дані, поки оновлення відбувається паралельно. Вона корисна там, де невелика затримка свіжості прийнятна, а миттєва відповідь важливіша за абсолютну точність — списки товарів, стрічки блогу, каталоги, що змінюються нечасто відносно кількості відвідувань.',
    why: 'Альтернатива — синхронно чекати на оновлення даних перед кожною відповіддю — гарантує свіжість, але робить час відповіді залежним від найповільнішого джерела даних щоразу; stale-while-revalidate розвʼязує цей компроміс на користь швидкості там, де невелика затримка прийнятна.',
    simple:
      'Уяви табло з розкладом руху на вокзалі, яке онлайн-система оновлює раз на хвилину. Коли ти дивишся на табло, воно показує дані станом на останнє оновлення, навіть якщо потяг могли затримати секунду тому, але тобі не доводиться чекати, поки система перерахує все заново, лише щоб побачити цифру. Приблизно так само працює stale-while-revalidate: коли кешований запис застаріває за часом revalidate, наступний, хто його запитує, отримує ще стару версію миттєво, а сервер у цей самий момент у фоні йде рахувати нову. Щойно нова версія готова, вона замінює стару в кеші, і всі наступні відвідувачі вже бачать оновлені дані, а той перший відвідувач так ніколи й не помітив затримки, бо для нього все виглядало миттєвим. Ключова відмінність від on-demand інвалідації через updateTag у тому, що тут ніхто спеціально не чекає на свіжі дані: система сама освіжає їх за розкладом, а не за подією. Це добре підходить для даних, де важлива швидкість відповіді, а не ідеальна миттєва точність — каталог товарів, список статей блогу, агреговану статистику, що й так змінюється поступово, а не миттєво в один момент часу.',
    related: [
      {
        title: 'cacheLife: stale, revalidate, expire',
        text: 'Три параметри, якими саме налаштовується ця стратегія в новій моделі кешування: revalidate визначає момент, коли починається фонове оновлення застарілого запису.',
      },
      {
        title: 'revalidateTag з профілем max',
        text: 'Практичний спосіб отримати stale-while-revalidate для on-demand інвалідації, на відміну від updateTag, який навмисно цю стратегію не використовує.',
      },
      {
        title: 'Incremental Static Regeneration',
        text: 'Механізм попередньої моделі кешування, побудований на тому самому принципі: застаріла статична сторінка віддається, поки нова версія генерується у фоні.',
      },
    ],
    codeExamples: [
      {
        label: 'Той самий принцип у двох моделях кешування',
        code: `// Попередня модель: після хвилини перший запит отримує стару відповідь,
// поки нова генерується у фоні; наступні запити вже бачать свіжу
const res = await fetch(url, { next: { revalidate: 60 } })

// Нова модель: явний stale-while-revalidate через профіль 'max'
revalidateTag('products', 'max') // ✅ фонове оновлення, а не жорстке очікування`,
      },
    ],
    seniorNotes: [
      'Термін походить із розширення Cache-Control в HTTP (директива stale-while-revalidate), і кешування Next.js на основі revalidate навмисно повторює ту саму семантику, а не вигадує нову.',
      'Якщо фонове оновлення даних падає, наприклад через недоступність джерела даних, Next.js продовжує віддавати попередню кешовану версію, а не показує помилку користувачу, що спричинив спробу оновлення.',
      'updateTag свідомо НЕ використовує stale-while-revalidate: він існує саме для випадків, коли навіть короткочасний показ застарілих даних після дії самого користувача гірший за трохи повільнішу відповідь.',
      'revalidateTag без явного аргументу профілю кешу відкочується до застарілої поведінки негайного закінчення терміну дії, еквівалентної updateTag, — щоб отримати фонове оновлення, профіль на кшталт max потрібно передати явно.',
    ],
  },

  {
    id: 'next-cache-components-enable',
    title: 'Що змінюється після ввімкнення cacheComponents: true?',
    definition:
      'Увімкнення cacheComponents: true в next.config перемикає застосунок на нову модель кешування, де за замовчуванням нічого не кешується і жоден код не потрапляє в заздалегідь підготовлену статичну оболонку, доки його явно не позначено директивою use cache. Це протилежність попередній моделі, де Next.js евристично намагався кешувати якомога більше, доки розробник явно не опише інакше. Разом з use cache стають доступні cacheLife і cacheTag для явного керування тривалістю життя кешу й тегами інвалідації, а сторінки автоматично отримують Partial Prerendering: статична оболонка рендериться заздалегідь, а некешовані чи динамічні частини стрімляться в межах Suspense.',
    why: 'Стара модель, де кешування вгадувалося неявно за наявністю викликів cookies(), headers() чи опцій fetch, часто давала несподівані результати: одна дрібна зміна могла випадково перевести весь маршрут у динамічний режим. Cache Components прибирає цю неявність, роблячи кешування свідомим рішенням, а не побічним ефектом коду.',
    simple:
      'Уяви, що раніше на кухні діяло правило за замовчуванням «готуй усе наперед, якщо шеф не сказав інакше», а тепер правило змінили на протилежне: «нічого не готуй наперед, доки хтось явно не попросить». Це і є суть переходу на Cache Components: без нього Next.js намагався вгадати, що можна закешувати, аналізуючи код сторінки, а щойно код десь торкався cookies(), headers() чи searchParams, кешування для всього маршруту мовчки вимикалося. З увімкненим cacheComponents жодна функція чи компонент не кешується, доки над ним не стоїть use cache, і так само жодна частина сторінки не потрапляє в заздалегідь підготовлену статичну оболонку без цієї директиви. Замість неявного вибору «статично чи динамічно для всього маршруту» зʼявляється явний вибір на рівні кожної функції чи компонента: use cache з cacheLife визначає, що і на скільки кешувати, а Suspense навколо решти коду позначає, що має довантажуватись у потоці на кожен запит. Результатом автоматично стає Partial Prerendering: Next.js будує статичну оболонку з усього, що передбачувано — кешовані фрагменти, статичні дані, синхронні обчислення, а решту лишає позаду меж Suspense, які дорендерюються на льоту. На практиці це означає, що міграція на cacheComponents часто вимагає пройтися по застосунку і розставити use cache там, де раніше кешування вгадувалося саме по собі, інакше продуктивність може навіть погіршитися, бо за замовчуванням тепер кешується значно менше.',
    related: [
      {
        title: 'Директива use cache',
        text: 'Основний інструмент нової моделі: без явного use cache на функції чи компоненті жоден результат більше не кешується автоматично, як це робилося раніше через евристики fetch.',
      },
      {
        title: 'Partial Prerendering',
        text: 'Автоматичний наслідок увімкнення cacheComponents: статична оболонка й динамічні острівці в одній відповіді, побудовані на основі того, що позначено use cache, а що ні.',
      },
      {
        title: 'Попередня модель кешування',
        text: 'Модель, від якої відмовляються при вмиканні cacheComponents; порівняння з нею допомагає зрозуміти, що саме змінюється в поведінці застосунку.',
      },
    ],
    codeExamples: [
      {
        label: 'Явне кешування замість неявного',
        code: `// next.config.ts
const nextConfig = {
  cacheComponents: true,
}

// До: неявне кешування через route segment config
export const revalidate = 3600

// Після: явне кешування конкретної функції чи компонента
import { cacheLife } from 'next/cache'
async function getData() {
  'use cache'
  cacheLife('hours')
  return fetch(url).then((r) => r.json())
}`,
      },
    ],
    seniorNotes: [
      'cacheComponents став стабільним, невипробувальним прапорцем починаючи з Next.js 16.0.0.',
      'З увімкненим cacheComponents GET Route Handlers підпорядковуються тій самій моделі попереднього рендерингу, що й сторінки, — їм так само потрібен use cache чи Suspense, щоб потрапити в статичну оболонку.',
      'cacheComponents вмикається на рівень усього застосунку одним прапорцем у конфігурації, а не для окремого маршруту, тому міграція великого проєкту зазвичай відбувається поступово за підказками дев-оверлея.',
      'Без жодного виклику use cache застосунок із cacheComponents: true поводиться як повністю динамічний, що рендериться на кожен запит, — і може бути повільнішим за стару модель, доки кешування не додано явно.',
    ],
  },

  {
    id: 'next-use-cache-vs-react-cache',
    title: 'Для чого потрібна директива use cache і чим вона відрізняється від React.cache()?',
    definition:
      'Директива use cache позначає асинхронну функцію, компонент чи весь файл як кешовані: перший виклик із певним набором аргументів виконує тіло функції і зберігає результат, а наступні виклики з тими самими аргументами — в межах одного рендера і між різними запитами — повертають збережений результат без повторного виконання. React.cache(), навпаки, працює лише в межах одного рендера: він дедублює виклики функції під час побудови одного дерева компонентів, а після рендеру його кеш повністю скидається. Ключова відмінність — тривалість життя й область дії: use cache персистентний і має власні cacheLife та cacheTag, а React.cache — суто механізм React для одного проходу рендеру без жодної опції тривалості.',
    why: 'Без use cache єдиним способом кешувати щось довше одного рендеру був або fetch з опціями кешу, який не годиться для викликів баз даних і ORM напряму, або unstable_cache — обидва незручні для довільних асинхронних функцій і компонентів.',
    simple:
      'Уяви два різних записники. React.cache — це чернетка на одну зустріч: поки триває нарада, у ній записують відповіді на питання, щоб не перепитувати те саме двічі протягом цієї самої зустрічі, а щойно нарада закінчується, чернетку викидають і на наступній зустрічі починають з чистого аркуша. use cache — це вже архів компанії: результат записують туди один раз, і будь-хто, хто звернеться з тим самим запитом сьогодні, завтра чи навіть через тиждень, отримує той самий запис з архіву, доки хтось спеціально не оновить його чи не спливе термін зберігання. Технічно React.cache дедублює виклики функції лише в межах одного серверного рендеру одного запиту: якщо два різні компоненти на одній сторінці викликають ту саму функцію з тими самими аргументами, вона виконається один раз, а не двічі, але для наступного відвідувача рендер почнеться заново з нуля. use cache працює на рівень вище: результат виконання зберігається в кеші Next.js і використовується повторно для будь-якого наступного запиту з тими самими аргументами, аж доки не спливе cacheLife чи не прийде on-demand інвалідація через cacheTag. Ще одна відмінність — де саме кожен можна застосувати: React.cache добре підходить для дедуплікації викликів ORM чи бази даних, яким fetch не потрібен, тоді як use cache можна поставити на будь-яку асинхронну функцію, цілий компонент чи навіть файл, включно з JSX, що повертається. На практиці їх часто комбінують: React.cache дедублює виклик усередині одного рендеру, а use cache навколо нього ж додає персистентність між запитами.',
    related: [
      {
        title: 'cacheLife та cacheTag',
        text: 'Інструменти, доступні саме всередині use cache для керування тривалістю життя й тегами інвалідації; у React.cache їхнього аналога не існує взагалі.',
      },
      {
        title: 'Request Memoization',
        text: 'Механізм попередньої моделі, побудований на тому самому принципі, що й React.cache, — дедуплікація в межах одного рендеру без персистентності між запитами.',
      },
      {
        title: 'Cache Components',
        text: 'Загальна модель, частиною якої є use cache; React.cache натомість — незалежна від Next.js можливість самого React, що працює й поза цією моделлю.',
      },
    ],
    codeExamples: [
      {
        label: 'Дедуплікація в межах рендеру проти персистентного кешу',
        code: `// React.cache: дедуплікація лише в межах одного рендеру
import { cache } from 'react'
export const getUser = cache(async (id: string) => {
  return db.user.findUnique({ where: { id } }) // раз на рендер, не на застосунок
})

// use cache: персистентний результат між запитами
import { cacheLife } from 'next/cache'
export async function getPopularProducts() {
  'use cache'
  cacheLife('hours')
  return db.product.findMany({ orderBy: { sales: 'desc' } }) // раз на годину для всіх
}`,
      },
    ],
    seniorNotes: [
      'React.cache має власну ізольовану область дії всередині межі use cache: значення, записані через React.cache зовні, не видні кешованій функції всередині, тому передати через нього дані в use cache не вийде.',
      'Функції й компоненти з use cache мають бути async — React.cache такої вимоги не має і працює як із синхронними, так і з асинхронними функціями.',
      'use cache вимагає увімкненого cacheComponents: true в конфігурації; React.cache — звичайне API React, що працює незалежно від будь-якого режиму кешування Next.js.',
      'Дедуплікація React.cache застосовується в межах рендеру незалежно від того, використовує функція fetch чи клієнт бази даних; персистентність use cache, натомість, підпорядкована обмеженням серіалізації і аргументів, і результату.',
    ],
  },

  {
    id: 'next-cachelife-cachetag-cachekey',
    title: 'Як працюють cacheLife(), cacheTag() та формування cache key?',
    definition:
      'cacheLife() задає тривалість життя кешованого результату через три параметри: stale — скільки клієнт може показувати кешовані дані без перевірки сервера, revalidate — через скільки сервер почне фоново оновлювати застарілий результат, і expire — після скількох секунд без запитів сервер обчислить результат синхронно заново. cacheTag() привʼязує до результату один чи кілька рядкових тегів, за якими пізніше можна інвалідувати саме цей запис через revalidateTag чи updateTag, не чіпаючи інші записи. Cache key формується автоматично із build ID, хешу функції та серіалізованих аргументів виклику, включно зі значеннями, захопленими із зовнішньої області видимості замикання, тому виклики з різними аргументами завжди отримують окремі записи в кеші.',
    why: 'Без явного контролю через ці три інструменти розробник не може окремо налаштувати, як довго кешувати результат, за яким критерієм інвалідувати саме його, і чи справді різні виклики функції потрапляють у різні комірки кешу, а не змішуються в одну.',
    simple:
      'Уяви склад із іменними полицями, де на кожній коробці написано, скільки часу вона може лежати, перш ніж її треба перевірити, і які ярлики на ній наклеєні для швидкого пошуку. cacheLife визначає час: stale каже, скільки браузер клієнта може показувати вміст коробки, не питаючи склад, revalidate каже, через скільки сервер тихо замінить вміст на свіжий, продовжуючи поки що видавати стару коробку тим, хто прийшов раніше, а expire — це крайній термін, після якого склад більше не видасть застарілу коробку взагалі, а спершу згенерує нову. cacheTag наклеює на коробку ярлики: одна коробка може мати кілька ярликів одразу, наприклад products і featured, і коли потрібно замінити весь товар з ярликом products, достатньо покликати revalidateTag з цим ярликом, і всі коробки з ним підуть на оновлення, незалежно від того, скільки їх і де вони лежать. Найскладніша частина — як склад узагалі розуміє, яку коробку видати на конкретний запит: цим займається cache key, який Next.js будує автоматично з ідентифікатора збірки, унікального відбитка самої функції в коді і серіалізованої версії всіх аргументів виклику. Якщо кешована функція використовує змінну із зовнішньої області видимості, наприклад userId, захоплений через замикання, ця змінна теж автоматично стає частиною ключа, тому виклик для одного користувача ніколи не поверне закешований результат іншого. Це означає, що getData(active) і getData(archived) завжди потрапляють у різні комірки складу, навіть якщо це одна й та сама функція, викликана в одному компоненті.',
    related: [
      {
        title: 'revalidatePath, revalidateTag, updateTag',
        text: 'Функції, що використовують саме теги, привʼязані через cacheTag, для точкової інвалідації конкретних записів кешу без перебудови всього застосунку.',
      },
      {
        title: 'Директива use cache',
        text: 'Область дії, всередині якої тільки й мають сенс cacheLife і cacheTag: поза use cache-функцією виклик будь-якої з них не має ефекту.',
      },
      {
        title: 'Обмеження серіалізації',
        text: 'Напряму повʼязана тема: саме серіалізовність аргументів визначає, чи можуть вони стати частиною cache key, і чому функції чи класи туди потрапити не можуть.',
      },
    ],
    codeExamples: [
      {
        label: 'cacheLife і cacheTag разом на одній функції',
        code: `import { cacheLife, cacheTag } from 'next/cache'

async function getOrderSummary(accountId: string) {
  'use cache'
  cacheLife('hours') // stale: 5хв, revalidate: 1год, expire: 1день
  cacheTag('orders', \`account-\${accountId}\`) // два теги одразу

  // accountId захоплений як аргумент — окремий запис кешу на кожен акаунт
  return db.orders.findMany({ where: { accountId } })
}`,
      },
    ],
    seniorNotes: [
      'Вбудовані профілі cacheLife — seconds, minutes, hours, days, weeks, max — мають фіксовані значення stale, revalidate і expire, які можна перевизначити чи доповнити власними іменованими профілями в next.config.',
      'За один виклик функції має виконатись лише один виклик cacheLife, хоча він може стояти в різних умовних гілках коду залежно від логіки застосунку.',
      'Мінімальний stale на клієнті — 30 секунд незалежно від меншого налаштованого значення, спеціально щоб попередньо завантажені посилання лишалися придатними до кліку.',
      'Cache key за замовчуванням включає build ID, тому записи use cache ніколи не переживають новий деплой, якщо тільки не налаштовано deploymentId, що перевизначає цю частину ключа.',
      'cacheTag можна викликати лише всередині області дії use cache; поза кешованою функцією виклик не має жодного ефекту.',
    ],
  },

  {
    id: 'next-revalidatepath-revalidatetag-updatetag',
    title: 'У чому різниця між revalidatePath(), revalidateTag() та updateTag()?',
    definition:
      'revalidatePath інвалідує всі кешовані записи, привʼязані до конкретного шляху застосунку, використовуючи для цього неявні soft tags, які Next.js автоматично генерує з URL. revalidateTag інвалідує всі записи з конкретним явним тегом, незалежно від того, на яких сторінках вони використовуються, і може викликатися і в Server Action, і в Route Handler. updateTag робить те саме, що й revalidateTag, але доступний лише всередині Server Action і гарантує, що наступний запит одразу дочекається свіжих даних, а не покаже застарілу версію — саме це потрібно для сценарію read-your-own-writes, коли користувач має побачити наслідок власної дії негайно.',
    why: 'Плутанина між цими трьома функціями типово призводить або до зайвої інвалідації цілих розділів сайту через revalidatePath, коли досить було б одного тега, або до ситуації, коли користувач після збереження форми на мить бачить старі дані, бо revalidateTag дозволяє віддати застарілий кеш, поки свіжий генерується у фоні.',
    simple:
      'Уяви бібліотеку з покажчиком книжок за назвами полиць і окремим покажчиком за темами. revalidatePath — це коли кажеш «онови все на цій конкретній полиці»: інвалідуються всі книжки, привʼязані саме до цього шляху в бібліотеці, навіть якщо серед них є книжки на зовсім різні теми. revalidateTag — це коли кажеш «онови всі книжки з міткою історія», незалежно від того, на якій полиці вони фізично стоять: одна мітка може стосуватися книжок, розкиданих по десятках різних полиць-шляхів одночасно. Обидва варіанти працюють однаково в плані швидкості для читача: якщо хтось зайде в бібліотеку одразу після revalidateTag, бібліотекар видасть йому ще стару версію книжки, поки нову підвозять у фоні — це і є behavior за замовчуванням, схожий на stale-while-revalidate. updateTag — це строгіший варіант тієї самої мітки: якщо саме ти щойно здав книжку на редагування і повертаєшся забрати її, бібліотекар більше не видасть тобі стару версію взагалі, а прямо на місці дочекається, поки нову занесуть, і лише тоді віддасть її тобі. Тому updateTag можна викликати лише зсередини Server Action — тієї самої дії, де сталася зміна, а revalidateTag і revalidatePath доступні і в Server Action, і в Route Handler, наприклад для вебхука від сторонньої системи, що не привʼязаний до конкретного користувача чи його останньої дії.',
    related: [
      {
        title: 'cacheLife та cacheTag',
        text: 'Тег, яким позначають кешований запис, — саме те, на що орієнтуються revalidateTag і updateTag; без cacheTag ці функції просто не мали б за що інвалідувати конкретний запис.',
      },
      {
        title: 'stale-while-revalidate',
        text: 'Стратегія, яку використовує revalidateTag з профілем max, але яку свідомо не використовує updateTag, віддаючи перевагу негайній свіжості перед фоновим оновленням.',
      },
      {
        title: 'Server Actions',
        text: 'Єдиний контекст, звідки можна викликати updateTag; розуміння того, чим Server Action відрізняється від Route Handler, напряму пояснює це обмеження.',
      },
    ],
    codeExamples: [
      {
        label: 'Три способи інвалідації після однієї дії',
        code: `// app/actions.ts
'use server'
import { revalidatePath, revalidateTag, updateTag } from 'next/cache'

export async function publishPost(id: string) {
  await db.post.update({ where: { id }, data: { published: true } })

  revalidatePath('/blog') // усе, привʼязане до шляху /blog
  revalidateTag('posts') // усе з тегом 'posts', де б не було
  updateTag(\`post-\${id}\`) // ✅ читач одразу бачить саме цю зміну
}`,
      },
    ],
    seniorNotes: [
      'updateTag можна викликати лише зсередини Server Action; виклик у Route Handler кидає помилку, тоді як revalidateTag працює в обох контекстах.',
      'revalidateTag, викликаний з профілем кешу max як другим аргументом, використовує stale-while-revalidate; без цього аргументу поведінка застаріла й негайна, фактично еквівалентна updateTag.',
      'revalidatePath спирається на ті самі автоматично згенеровані soft tags, що Next.js виводить із URL-шляху, тому працює навіть для контенту, який ніколи явно не позначали через cacheTag.',
      'revalidateTag чи revalidatePath у Server Action одразу інвалідують і клієнтський Router Cache відповідного маршруту, а ті самі виклики в Route Handler цього не роблять, бо не привʼязані до конкретного маршруту.',
    ],
  },

  {
    id: 'next-personalized-data-caching-leak',
    title: 'Як правильно реалізувати кешування персоналізованих даних без витоку між користувачами?',
    definition:
      'Персоналізовані дані кешують або через use cache: private, яка дозволяє читати cookies() і headers() всередині кешованої функції, але зберігає результат лише в памʼяті браузера конкретного користувача, ніколи в спільному серверному кеші, або передаючи ідентифікатор користувача явним аргументом, щоб він автоматично став частиною cache key. В обох випадках ключове правило — дані одного користувача ніколи не повинні потрапити у спільний серверний запис кешу, який читають усі відвідувачі. Найнебезпечніша помилка — прочитати cookies() усередині звичайної use cache-функції: якби Next.js це дозволив, результат для першого користувача показувався б усім наступним, тому use cache прямо забороняє такий виклик.',
    why: 'Кешування, що не враховує, який саме користувач робить запит, — найпряміший спосіб показати одному користувачу персональні дані іншого: кошик, імʼя, історію замовлень, тому Next.js навмисно блокує прямий доступ до cookies() і headers() усередині звичайної use cache-функції.',
    simple:
      'Уяви камеру схову на вокзалі, де кожна шафка має свій номер і код, а не одну спільну шафку на всіх пасажирів. Якщо кешувати персоналізовані дані неправильно — прочитати cookies() прямо всередині use cache-функції, — це все одно що видати всім пасажирам ключ від однієї й тієї самої шафки: перший, хто її відкриє, покладе туди свої речі, а кожен наступний випадково забере не своє. Саме тому Next.js прямо забороняє викликати cookies() чи headers() усередині звичайної use cache-функції — помилка зʼявляється навмисно, щоб зупинити цю небезпечну поведінку ще до продакшну. Є два безпечні шляхи. Перший — прочитати cookies() зовні, у некешованому компоненті, і передати конкретне значення, наприклад ідентифікатор сесії, як звичайний аргумент у функцію з use cache: тоді цей ідентифікатор автоматично стає частиною cache key, і кожен користувач гарантовано отримує окремий запис, а не чужий. Другий шлях — use cache: private, яка дозволяє читати cookies() і headers() прямо всередині себе, але свідомо платить за це тим, що результат ніколи не потрапляє у спільний серверний кеш між різними користувачами, а зберігається лише в памʼяті браузера того самого користувача на час, заданий cacheLife. Вибір між ними залежить від масштабу: якщо персоналізованих варіантів небагато, наприклад тема оформлення чи мова, перший підхід із явним аргументом дає кращу перевикористовуваність кешу, а якщо персоналізація по-справжньому унікальна для кожного, use cache: private простіше писати, ціною відсутності спільного кешування між користувачами.',
    related: [
      {
        title: 'use cache: private',
        text: 'Варіант директиви, спеціально призначений для функцій, що читають cookies() чи headers() напряму, з результатом, обмеженим памʼяттю браузера конкретного користувача.',
      },
      {
        title: 'Формування cache key',
        text: 'Пояснює, чому передача ідентифікатора користувача аргументом працює як спосіб ізоляції: серіалізовані аргументи автоматично стають частиною ключа кешу.',
      },
      {
        title: 'Обмеження серіалізації',
        text: 'Суміжна тема: ті самі правила, що визначають, які аргументи можна передати в use cache-функцію, визначають і те, що саме може стати частиною cache key для ізоляції користувачів.',
      },
    ],
    codeExamples: [
      {
        label: 'Два безпечні способи кешувати персоналізацію',
        code: `// ❌ Заборонено: cookies() напряму всередині use cache
async function getRecommendations() {
  'use cache'
  const session = (await cookies()).get('session')?.value // помилка
  return fetchRecs(session)
}

// ✅ Варіант 1: значення передається аргументом, стає частиною cache key
async function ProfileContent() {
  const sessionId = (await cookies()).get('session')?.value
  return <CachedRecs sessionId={sessionId} />
}
async function CachedRecs({ sessionId }: { sessionId?: string }) {
  'use cache'
  return fetchRecs(sessionId) // окремий запис кешу на кожен sessionId
}

// ✅ Варіант 2: use cache: private — кеш лише в браузері цього користувача
async function getPrivateRecommendations() {
  'use cache: private'
  const sessionId = (await cookies()).get('session')?.value
  return fetchRecs(sessionId)
}`,
      },
    ],
    seniorNotes: [
      'Помилка, що кидається при виклику cookies() чи headers() напряму всередині use cache, спрацьовує за всім стеком викликів: навіть допоміжна функція, покликана з кешованої, впаде так само.',
      'Результати use cache: private завжди виключені зі статичної оболонки маршруту, бо за визначенням залежать від даних, невідомих під час збірки.',
      'Передача ідентифікатора користувача аргументом працює тому, що Next.js автоматично включає серіалізовні аргументи, і значення, захоплені із замикання, у cache key — різні ідентифікатори завжди дають різні записи.',
      'use cache: private не підтримує кастомний cache handler: місце зберігання для неї не налаштовується, на відміну від звичайної use cache чи use cache: remote.',
      'cacheLife({ stale: Infinity }) всередині use cache: private не дає короткому часу життя цієї функції скорочувати stale-час усього маршруту — корисно, коли функція потрібна лише для дедуплікації в межах запиту, а не для справжнього кешування персоналізації.',
    ],
  },

  {
    id: 'next-multi-instance-cache-invalidation',
    title: 'Як синхронізувати інвалідацію кешу між кількома інстансами Next.js?',
    definition:
      'Типовий in-memory кеш Next.js ізольований у межах одного процесу: якщо застосунок працює на кількох серверних інстансах чи контейнерах, кожен має власну копію кешу, і виклик revalidateTag на одному з них не впливає на кеш інших. Щоб синхронізувати інвалідацію, потрібен спільний cache handler — власна реалізація, підключена через cacheHandlers у next.config, що зберігає записи в зовнішньому сховищі на кшталт Redis, доступному всім інстансам одночасно. Такий handler реалізує не лише збереження й читання записів, а й окремі методи updateTags і refreshTags: перший записує момент інвалідації тега у спільне сховище, а другий, викликаний перед кожним запитом, підтягує ці позначки з інших інстансів у локальний стан поточного процесу.',
    why: 'Без спільного cache handler команда, що масштабує застосунок горизонтально на кілька серверів, ризикує отримати ситуацію, де користувач бачить оновлені дані на одному запиті й застарілі на наступному, залежно від того, на який саме інстанс потрапив його запит через балансувальник навантаження.',
    simple:
      'Уяви мережу з кількох пекарень одного бренду в різних районах міста, де кожна тримає власний, окремий журнал замовлень, ніяк не звʼязаний із журналами інших філій. Якщо клієнт скасовує замовлення у філії А, а журнал не спільний, філія Б про це не дізнається і надалі готуватиме той самий скасований хліб — рівно так само поводиться типовий in-memory кеш Next.js, коли застосунок запущено на кількох серверах: revalidateTag на одному інстансі просто не існує для інших, бо кожен тримає власну копію кешу в памʼяті власного процесу. Розвʼязання — завести спільний журнал, який бачать усі філії одночасно: у термінах Next.js це кастомний cache handler, підключений через cacheHandlers, що зберігає записи не в памʼяті процесу, а в зовнішньому сховищі на кшталт Redis, доступному з будь-якого інстансу. Такий handler відповідає не лише за читання й запис самих даних, а й за координацію тегів: коли викликають revalidateTag, метод updateTags handler-а записує в Redis, що конкретний тег щойно інвалідовано, і в який момент. Перед обробкою кожного нового запиту Next.js викликає метод refreshTags цього ж handler-а, і саме тут він підтягує з Redis список нещодавно інвалідованих тегів від усіх інстансів і оновлює локальний стан поточного процесу, щоб той теж почав вважати відповідні записи застарілими. Такий підхід потрібен лише при самостійному хостингу на кількох серверах: платформи на кшталт Vercel вирішують цю координацію на рівні власної інфраструктури, тому там кастомний cache handler для базового сценарію зазвичай не потрібен.',
    related: [
      {
        title: 'cacheHandlers і CacheEntry',
        text: 'Конфігурація й формат даних, якими реалізується кастомний cache handler; саме через неї підключають зовнішнє сховище замість типового in-memory кешу.',
      },
      {
        title: 'revalidateTag та updateTag',
        text: 'Функції, чий виклик на одному інстансі має дійти до всіх інших саме завдяки методам updateTags і refreshTags кастомного cache handler-а.',
      },
      {
        title: 'Data Cache у попередній моделі',
        text: 'Та сама проблема координації між інстансами існувала і в попередній моделі кешування — розвʼязання через зовнішнє сховище застосовне до обох моделей.',
      },
    ],
    codeExamples: [
      {
        label: 'Кастомний cache handler для координації між інстансами',
        code: `// next.config.ts
const nextConfig = {
  cacheComponents: true,
  cacheHandlers: {
    default: require.resolve('./cache-handlers/redis-handler.js'),
  },
}

// cache-handlers/redis-handler.js (спрощено)
module.exports = {
  async get(cacheKey) {
    /* читання запису з Redis */
  },
  async set(cacheKey, pendingEntry) {
    /* запис результату в Redis */
  },
  // ✅ саме ці два методи синхронізують інвалідацію між інстансами
  async updateTags(tags) {
    for (const tag of tags) await redis.set(\`tag:\${tag}\`, Date.now())
  },
  async refreshTags() {
    // підтягує позначки інвалідації, зроблені іншими інстансами
  },
  async getExpiration(tags) {
    /* найновіша позначка серед переданих тегів */
  },
}`,
      },
    ],
    seniorNotes: [
      'Типовий in-memory cache handler трактує refreshTags як no-op, бо синхронізувати процес-локальний кеш нема з чим; саме змістовну реалізацію цього методу й додає розподілений handler.',
      'getExpiration() має повертати найновішу позначку ревалідації серед переданих тегів або Infinity, щоб делегувати цю перевірку методу get() через soft tags — за цим значенням фреймворк вирішує, чи запис застарів.',
      'Cache key вже включає build ID, тому навіть спільний зовнішній cache handler не поверне записи попереднього деплою інстансам, які мід-релізу опинилися на різних версіях коду.',
      "use cache: remote — варіант, спеціально призначений для довговічного спільного кешування між інстансами через кастомний remote handler, на відміну від типової use cache, що лишається процес-локальною, навіть коли для неї не налаштовано кастомний handler.",
    ],
  },
]

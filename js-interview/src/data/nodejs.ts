import type { Topic } from '../types'

/*
 * Node.js на бекенді: важкі обчислення, стріми, фонові воркери,
 * черги задач і надійність операцій із грошима.
 * Event loop, мікротаски і проміси живуть у вкладці JavaScript.
 */

export const nodejsTopics: Topic[] = [
  {
    id: 'node-worker-threads',
    title: 'Як виконувати CPU-intensive задачі в Node.js, не блокуючи сервер?',
    definition:
      'Node.js виконує весь JavaScript в одному потоці, тому важке обчислення не просто сповільнює один запит, а зупиняє event loop і разом із ним усі інші зʼєднання. Правильна відповідь залежить від типу навантаження: короткі обчислення виносять у worker_threads, зручно через пул на кшталт Piscina; готові нативні бібліотеки на кшталт bcrypt чи zlib самі виконуються в пулі libuv і event loop не блокують; довгі задачі на секунди й хвилини взагалі не місце в HTTP-процесі — їх віддають у чергу фоновому воркеру. Підхід із child_process або кластером теж робочий, але дорожчий за памʼяттю, бо кожен процес тримає власний екземпляр V8.',
    why: 'Асинхронність Node рятує лише від очікування вводу-виводу: поки запит іде в базу, потік вільний. Обчислення ж займають сам потік, і поки воно триває, сервер не приймає нові зʼєднання, не відповідає на health check і не обробляє навіть готові відповіді — один важкий запит кладе весь інстанс.',
    simple:
      'Уяви одного кухаря, який швидко перемикається між десятком страв: поки вода закипає, він ріже овочі. Саме так Node тримає тисячі зʼєднань одним потоком — він не чекає, а перемикається. Але якщо йому дати вручну перемолоти мішок зерна, він стоятиме на місці, і всі страви згорять. Worker thread — це другий кухар за окремим столом: у нього власний екземпляр V8, власна купа і власний event loop, а спілкуються вони через повідомлення. Передача даних копіює їх структурованим клонуванням, тому гнати туди-сюди великі обʼєкти дорого; для бінарних даних беруть ArrayBuffer із передачею володіння або SharedArrayBuffer, який фізично спільний. Створення воркера коштує десятки мілісекунд і кілька мегабайт памʼяті, тому воркер на запит — помилка: тримають пул фіксованого розміру, зазвичай за кількістю ядер. Практичний орієнтир для рішення: якщо обробка коротша за кілька мілісекунд, залиш у головному потоці; якщо це десятки або сотні мілісекунд, віддай у пул воркерів; якщо секунди й більше — це фонова задача через чергу, а користувачу треба віддати ідентифікатор і статус.',
    related: [
      {
        title: 'Пул потоків libuv і те, що вже не блокує',
        text: 'Частина вбудованих операцій виконується не в JS-потоці, а в пулі потоків libuv розміром чотири за замовчуванням: файлові операції, DNS-резолв через getaddrinfo, zlib і crypto на кшталт pbkdf2. Саме тому асинхронний bcrypt.hash не блокує event loop, а синхронний bcrypt.hashSync блокує. Розмір пулу задається змінною UV_THREADPOOL_SIZE до старту процесу.',
      },
      {
        title: 'Piscina і власний пул воркерів',
        text: 'Piscina — стандартна бібліотека-пул для worker_threads: тримає воркери живими, розподіляє задачі, підтримує черги, таймаути й скасування. Власноруч написаний пул неминуче доводиться дописувати тими самими речами, тому на співбесіді назва готового рішення виглядає сильніше за код пулу з нуля.',
      },
      {
        title: 'Кластер і горизонтальне масштабування',
        text: 'Модуль cluster чи менеджер на кшталт PM2 запускає кілька процесів Node на одному сервері й балансує зʼєднання між ними. Це дає використання всіх ядер, але не рятує від блокування: важке обчислення все одно вбиває той процес, у який потрапило. Кластер і воркери вирішують різні задачі й часто застосовуються разом.',
      },
    ],
    codeExamples: [
      {
        label: 'Пул воркерів через Piscina',
        code: `// server.ts
import Piscina from 'piscina';

const pool = new Piscina({
  filename: new URL('./heavy-worker.js', import.meta.url).href,
  maxThreads: 4,          // ≈ кількість ядер, а не кількість запитів
});

app.post('/report', async (req, res) => {
  // event loop вільний: чекає лише проміс
  const result = await pool.run(req.body, { name: 'buildReport' });
  res.json(result);
});

// heavy-worker.js — виконується в окремому потоці з власним V8
export function buildReport(input) {
  return computeSomethingExpensive(input);
}`,
      },
      {
        label: 'Що блокує event loop, а що ні',
        code: `// ❌ синхронний хеш: весь сервер стоїть 300 мс
const hash = bcrypt.hashSync(password, 12);

// ✅ асинхронний варіант іде в пул потоків libuv
const hash = await bcrypt.hash(password, 12);

// ❌ синхронне читання великого файлу блокує всі зʼєднання
const data = fs.readFileSync('10gb.csv');

// ❌ важкий цикл у головному потоці — await тут не рятує
for (let i = 0; i < 1e9; i++) sum += i;`,
      },
    ],
    seniorNotes: [
      'Додавання async перед функцією не переносить її код в інший потік: await допомагає лише там, де є реальна асинхронна операція. Цикл на мільярд ітерацій блокує event loop однаково в async і в звичайній функції.',
      'Кожен worker thread — це окремий екземпляр V8 із власною купою, приблизно кілька мегабайт мінімум і десятки мілісекунд на старт, тому воркер на кожен HTTP-запит гірший за обчислення в головному потоці.',
      'Передача даних у воркер за замовчуванням копіює їх алгоритмом structured clone, тому великий JSON коштує окремого часу; transferList для ArrayBuffer віддає буфер без копії, але робить його непридатним у потоці-відправнику.',
      'Метрика, яка ловить проблему в продакшні, — event loop lag: різниця між запланованим і реальним часом спрацювання таймера. У Prometheus це nodejs_eventloop_lag_seconds, і стійке значення понад сто мілісекунд означає, що щось блокує потік.',
      'JSON.parse і JSON.stringify виконуються синхронно в головному потоці, тому відповідь на кількадесят мегабайт блокує сервер саме на серіалізації, навіть якщо дані прийшли асинхронно.',
    ],
  },

  {
    id: 'node-streams',
    title: 'Що таке стріми в Node.js і навіщо потрібен Transform stream?',
    definition:
      'Стрім — це абстракція над даними, що надходять частинами: замість зчитати все в памʼять і потім віддати, процес обробляє шматок за шматком. Типів чотири: Readable — джерело, Writable — приймач, Duplex — обидва напрямки одночасно, як сокет, і Transform — дуплекс, що перетворює дані на льоту між входом і виходом. Стріми зʼєднують через pipeline, який, на відміну від pipe, коректно пробрасує помилки й прибирає ресурси. Ключова властивість — backpressure: коли приймач не встигає, він сигналізує джерелу зупинитись, і памʼять не росте.',
    why: 'Мільйон рядків з бази, зібраний у масив, це кілька гігабайт у купі V8, і процес падає з out of memory ще до того, як користувач отримає перший байт. Стрім тримає в памʼяті лише поточний шматок, тому обсяг даних перестає впливати на споживання памʼяті — обмеженням стає тільки час.',
    simple:
      'Порівняй два способи перелити воду: перенести все відром за раз або пустити шланг. Стрім — це шланг: дані течуть безперервно, а в памʼяті одночасно є тільки те, що зараз у трубі. Readable — кран, Writable — злив, а Transform — фільтр посередині, крізь який вода проходить і міняється. Кожен Transform отримує шматок, викликає callback із результатом і одразу забуває про нього, тому послідовність із кількох Transform читається як конвеєр: узяти рядок з бази, перетворити на потрібну форму, перевести в CSV, віддати у відповідь. Найважливіше поняття тут — backpressure. Якщо джерело читає швидше, ніж приймач пише, у внутрішньому буфері накопичується черга; метод write повертає false, і правильно написаний конвеєр ставить читання на паузу, доки не спрацює подія drain. Саме тому ручне звʼязування через on data і write без перевірки повернутого значення — головна причина того, що стрім не рятує від out of memory. Використовуй pipeline, і ця логіка йде безкоштовно. Ще одна деталь для співбесіди: objectMode. За замовчуванням стріми оперують буферами й рядками, а objectMode дозволяє пропускати крізь них обʼєкти, що якраз потрібно для рядків із бази.',
    related: [
      {
        title: 'pipeline замість pipe',
        text: 'Метод pipe не пробрасує помилки між стрімами: помилка в середині конвеєра лишає попередні стріми відкритими, і процес тече файловими дескрипторами. Функція pipeline із модуля stream, особливо її проміс-версія з stream/promises, закриває всі стріми при помилці й повертає проміс, який зручно чекати в async-обробнику.',
      },
      {
        title: 'Курсор бази як джерело стріму',
        text: 'Щоб стрім мав сенс, дані мають надходити частинами з самого початку: драйвер має віддавати курсор, а не готовий масив. У PostgreSQL це pg-query-stream або cursor, у MongoDB — cursor.stream(), у MySQL — режим query stream. Якщо ORM спершу матеріалізує всю вибірку в масив, подальший стрім уже нічого не економить.',
      },
      {
        title: 'Web Streams і async-ітератори',
        text: 'Сучасний Node підтримує стандартні Web Streams із ReadableStream і TransformStream, сумісні з fetch і з браузером, і має міст Readable.fromWeb і toWeb. Крім того, будь-який Readable є async-ітератором, тому його можна читати звичайним for await, і backpressure при цьому працює автоматично — це найчитабельніший варіант для простої обробки.',
      },
    ],
    codeExamples: [
      {
        label: 'Конвеєр: курсор бази → трансформація → CSV → відповідь',
        code: `import { pipeline } from 'node:stream/promises';
import { Transform } from 'node:stream';
import QueryStream from 'pg-query-stream';

app.get('/export.csv', async (req, res) => {
  const query = new QueryStream('SELECT id, email, total FROM orders');
  const dbStream = client.query(query); // objectMode: рядок за рядком

  const toCsv = new Transform({
    objectMode: true,
    transform(row, _enc, cb) {
      cb(null, \`\${row.id},\${row.email},\${(row.total / 100).toFixed(2)}\\n\`);
    },
  });

  res.setHeader('Content-Type', 'text/csv');
  // pipeline сам закриє все при помилці й поверне помилку сюди
  await pipeline(dbStream, toCsv, res);
});`,
      },
      {
        label: 'Backpressure: чому ручний pipe тече памʼяттю',
        code: `// ❌ результат write ігнорується — буфер росте до out of memory
readable.on('data', (chunk) => {
  writable.write(transform(chunk));
});

// ✅ pipeline сам ставить читання на паузу й чекає drain
await pipeline(readable, transformStream, writable);

// ✅ або for await — backpressure працює автоматично
for await (const chunk of readable) {
  if (!writable.write(transform(chunk))) {
    await once(writable, 'drain');
  }
}`,
      },
    ],
    seniorNotes: [
      'highWaterMark задає поріг внутрішнього буфера: 16 кілобайт для байтових стрімів і 16 обʼєктів для objectMode. Підняття цього значення збільшує пропускну здатність ціною памʼяті, і саме через objectMode-ліміт у шістнадцять елементів стрім обʼєктів поводиться не так, як здається з байтової інтуїції.',
      'Помилка всередині Transform передається через перший аргумент callback, а не через throw: кинутий виняток у callback-стилі стає uncaughtException і кладе процес. У async-версії _transform підтримується і проміс-стиль.',
      'Стрім у HTTP-відповідь не дає відправити помилковий статус після початку передачі: заголовки вже пішли. Типове рішення — валідувати все до відкриття стріму, а збій посеред потоку фіксувати у файлі-результаті або обривати зʼєднання.',
      'Гzip прямо в конвеєрі через createGzip зменшує обсяг CSV у рази і виконується в пулі потоків libuv, тому не блокує event loop, на відміну від gzipSync.',
      'Node підтримує як власні стріми, так і Web Streams; змішувати їх безпосередньо не можна — потрібні Readable.fromWeb і Readable.toWeb, інакше pipeline кидає помилку про неправильний тип аргументу.',
    ],
  },

  {
    id: 'node-queues-workers',
    title: 'Як влаштована черга задач і воркер, і яку бібліотеку для цього обрати?',
    definition:
      'Черга задач розділяє того, хто ставить роботу, і того, хто її виконує: producer, тобто HTTP-обробник, кладе в чергу повідомлення й одразу відповідає клієнту, а consumer, тобто окремий процес-воркер, забирає задачі й виконує їх у своєму темпі. Сама черга живе у зовнішньому сховищі — Redis, RabbitMQ, SQS, — тому падіння воркера не втрачає роботу: задача повертається в чергу за таймаутом видимості й дістається іншому. Типовий набір можливостей, за який беруть готову бібліотеку: повтори з експоненційною затримкою, обмеження паралельності, пріоритети, dead letter queue для остаточно невдалих. У Node стандартний вибір це BullMQ поверх Redis, в екосистемі Python — Celery, у Ruby — Sidekiq, а для хмарних черг беруть SQS чи Cloud Tasks.',
    why: 'HTTP-запит має відповідати за сотні мілісекунд, а відправка тисячі листів, генерація звіту чи обробка відео тривають хвилини. Без черги така робота або тримає зʼєднання відкритим, або виконується прямо в процесі сервера і масштабується разом із ним, хоча потребує зовсім інших ресурсів.',
    simple:
      'Уяви кухню з вікном видачі замовлень: офіціант не стоїть біля плити, він чіпляє чек на стрічку і йде далі, а кухарі знімають чеки в міру звільнення. Черга — це та стрічка. Веб-процес кладе в неї опис роботи, отримує ідентифікатор задачі й миттєво відповідає клієнту, а окремий процес-воркер забирає й виконує. Важлива частина механіки — підтвердження. Воркер не видаляє задачу одразу, а бере її в роботу; якщо він упаде посеред виконання, задача за таймаутом повернеться в чергу й дістанеться іншому. Звідси прямий наслідок: доставка гарантується як мінімум один раз, тобто задача може виконатись двічі, і обробник мусить бути ідемпотентним. Масштабування тут просте й приємне: стало багато роботи — підняв ще кілька процесів воркера, вони самі розберуть чергу, а concurrency всередині процесу обмежує, скільки задач він тягне одночасно. Обовʼязкові елементи робочої системи: обмежена кількість повторів із наростаючою затримкою, окрема черга для задач, що остаточно провалились, і метрики глибини черги — саме вона показує, що воркерів не вистачає. Для черги на Redis варто памʼятати, що надійність обмежена надійністю самого Redis: без persistence задачі губляться при перезапуску.',
    related: [
      {
        title: 'Redis-черги проти брокерів повідомлень',
        text: 'BullMQ на Redis простий у запуску й швидкий, але Redis лишається сховищем у памʼяті: без AOF-персистентності перезапуск втрачає задачі. RabbitMQ дає справжню маршрутизацію, підтвердження і стійкі черги на диску, SQS знімає експлуатацію взагалі ціною затримок і обмежень. Kafka тут окремий випадок: це лог подій із перечитуванням, а не черга задач із підтвердженнями.',
      },
      {
        title: 'At-least-once доставка й ідемпотентність',
        text: 'Практично всі черги гарантують доставку щонайменше один раз: за таймаутом чи при падінні воркера задача виконається повторно. Тому обробник має бути безпечним до повтору — перевіряти, чи робота вже зроблена, за ключем задачі, або використовувати вставку з ON CONFLICT DO NOTHING. Точно-один-раз на рівні черги практично недосяжний, і правильна відповідь переносить відповідальність в обробник.',
      },
      {
        title: 'Dead letter queue і повтори',
        text: 'Задача, що провалилась задану кількість разів, не має крутитись у черзі вічно: її переносять у dead letter queue для ручного розбору, інакше одна отруйна задача зʼїдає всю пропускну здатність воркерів. Затримку між спробами роблять експоненційною з випадковою добавкою, щоб тисяча задач після відновлення зовнішнього сервісу не вдарила по ньому одночасно.',
      },
      {
        title: 'Де фізично живе воркер',
        text: 'Воркер — це окремий процес і зазвичай окремий деплой із тим самим кодом, але іншою точкою входу і власними лімітами памʼяті й процесора. Запускати воркер усередині веб-процесу можна лише в маленьких проєктах: важка задача відбирає event loop у HTTP-обробників, а масштабувати веб і фонову обробку доводиться разом, хоча навантаження в них різне.',
      },
    ],
    codeExamples: [
      {
        label: 'Producer і consumer на BullMQ',
        code: `// producer: HTTP-обробник лише ставить задачу
import { Queue } from 'bullmq';
const exportQueue = new Queue('exports', { connection: redis });

app.post('/exports', async (req, res) => {
  const job = await exportQueue.add(
    'orders-xlsx',
    { userId: req.user.id, filters: req.body },
    {
      jobId: \`export:\${req.user.id}:\${req.body.hash}\`, // дедуплікація
      attempts: 5,
      backoff: { type: 'exponential', delay: 2000 },
      removeOnComplete: 1000,
    },
  );
  res.status(202).json({ jobId: job.id }); // одразу відповідь
});

// consumer: окремий процес, окремий деплой
import { Worker } from 'bullmq';
new Worker('exports', async (job) => buildExport(job.data), {
  connection: redis,
  concurrency: 5,           // скільки задач тягне один процес
});`,
      },
      {
        label: 'Обробник має переживати повтор',
        code: `// ❌ повтор задачі спише гроші вдруге
async function handler(job) {
  await charge(job.data.userId, job.data.amount);
}

// ✅ результат зафіксований за ключем задачі
async function handler(job) {
  const inserted = await db.query(
    'INSERT INTO job_results (job_id) VALUES ($1) ON CONFLICT DO NOTHING',
    [job.id],
  );
  if (inserted.rowCount === 0) return; // вже виконано
  await charge(job.data.userId, job.data.amount);
}`,
      },
    ],
    seniorNotes: [
      'BullMQ реалізує черги на Redis через Lua-скрипти й потребує Redis із увімкненою персистентністю AOF, інакше перезапуск втрачає задачі в роботі. Це головна відмінність від RabbitMQ, де стійкі черги пишуться на диск за замовчуванням.',
      'Concurrency воркера і кількість процесів воркера — різні регулятори: перший обмежує паралельність усередині одного event loop і має сенс для очікування вводу-виводу, другий дає реальне використання ядер для обчислень.',
      'Задачу не можна ставити в чергу в тій самій транзакції, що й зміну в базі: воркер може забрати її до комміту й не побачити даних. Класичні рішення — transactional outbox або постановка задачі після комміту з відкладеним стартом.',
      'Kafka не є заміною черги задач: у ній немає підтвердження окремого повідомлення й повернення його в чергу, а паралельність обмежена кількістю партицій. Вона підходить для потоку подій, а не для задач із повторами.',
      'Основна метрика здоровʼя — не кількість оброблених задач, а глибина черги й вік найстарішої задачі: зростання саме цих двох показує, що воркерів менше, ніж потрібно, ще до того, як користувачі почнуть скаржитись.',
    ],
  },

  {
    id: 'node-big-export',
    title: 'Як експортувати мільйони рядків у Excel, не поклавши бекенд?',
    definition:
      'Такий експорт не робиться в HTTP-запиті взагалі. Запит лише створює задачу в черзі, повертає 202 із ідентифікатором, а фоновий воркер читає дані курсором із репліки для читання, стрімом перетворює їх на XLSX чи CSV і пише одразу в обʼєктне сховище на кшталт S3, не збираючи файл у памʼяті. Коли файл готовий, користувач отримує посилання з обмеженим часом життя через нотифікацію або опитування статусу. Ключові деталі: курсор замість вибірки всього масиву, потокова генерація файлу, репліка замість основної бази, обмеження паралельних експортів і ліміт рядків, після якого формат примусово стає CSV.',
    why: 'Спроба зробити це синхронно ламається одразу в чотирьох місцях: вибірка мільйонів рядків зʼїдає памʼять процесу, генерація XLSX у памʼяті зʼїдає її ще раз, важкий запит навантажує основну базу, а HTTP-зʼєднання все одно відвалиться за таймаутом балансувальника. Асинхронна схема знімає всі чотири обмеження одночасно.',
    simple:
      'Розклади задачу на чотири питання: хто читає, хто пише, куди пише і як користувач дізнається результат. Читає воркер, і читає курсором: драйвер віддає рядки порціями, тому в памʼяті одночасно живе кілька тисяч записів, а не мільйони. Читає він із репліки, бо це довгий аналітичний прохід, який не має заважати оплатам на основній базі. Пише він стрімом: між курсором і файлом стоїть Transform, що на льоту перетворює запис бази на рядок вихідного формату — приводить дати до локального часу, ділить копійки на сто, підставляє назви замість ідентифікаторів. Пише не в памʼять і не у відповідь, а у файл в обʼєктному сховищі через multipart upload, тому розмір файлу обмежений лише сховищем. Користувач у цей час не тримає зʼєднання: він отримав ідентифікатор задачі одразу й дивиться статус або чекає листа. Окремий момент саме про Excel: XLSX — це zip-архів із XML, і більшість бібліотек будують усю книгу в памʼяті, тому для мільйонів рядків потрібен саме потоковий режим, наприклад exceljs зі stream.xlsx.WorkbookWriter. Якщо потоковий XLSX усе одно не тягне, чесна відповідь — віддати CSV, бо ліміт аркуша Excel однаково трохи більше мільйона рядків.',
    related: [
      {
        title: 'Ліміт аркуша Excel',
        text: 'Аркуш XLSX вміщує 1 048 576 рядків і 16 384 стовпці, тому експорт на кілька мільйонів рядків фізично не існує як один аркуш. Варіанти відповіді: розбити на кілька аркушів чи файлів, віддати CSV, або, найчастіше, спитати замовника, навіщо йому мільйони рядків у Excel — типово потрібен агрегат, а не сирі дані.',
      },
      {
        title: 'Репліка для читання',
        text: 'Довгий послідовний прохід по великій таблиці витісняє з кешу базы гарячі сторінки і навантажує диск, тому його відправляють на репліку. Наслідок, який треба озвучити самому: репліка відстає на секунди, тому дані в експорті можуть бути трохи несвіжими, і для звітів за минулий період це прийнятно, а для операцій із грошима — ні.',
      },
      {
        title: 'Статус задачі й видача файлу',
        text: 'Клієнту повертають 202 Accepted із ідентифікатором і посиланням на статус. Далі або опитування цього URL, або WebSocket, або лист після завершення. Готовий файл не віддають через власний процес: видають підписане посилання S3 з обмеженим часом життя, щоб трафік ішов повз бекенд і не тримав його зʼєднання.',
      },
      {
        title: 'Захист від зловживання експортом',
        text: 'Важкий експорт легко перетворюється на самообстріл: десять користувачів одночасно замовили повний звіт і поклали воркери. Обмеження, які реально ставлять: не більше однієї активної задачі на користувача, дедуплікація за ключем фільтрів, окрема черга з малою паралельністю, жорсткий ліміт рядків і таймаут задачі.',
      },
    ],
    codeExamples: [
      {
        label: 'Воркер: курсор → трансформація → потоковий XLSX → S3',
        code: `import ExcelJS from 'exceljs';
import { Upload } from '@aws-sdk/lib-storage';
import { PassThrough } from 'node:stream';

export async function buildExport(job) {
  const passThrough = new PassThrough();
  const upload = new Upload({
    client: s3,
    params: { Bucket: 'exports', Key: \`\${job.id}.xlsx\`, Body: passThrough },
  });

  // потоковий режим: рядки не накопичуються в памʼяті
  const wb = new ExcelJS.stream.xlsx.WorkbookWriter({ stream: passThrough });
  const ws = wb.addWorksheet('orders');

  // читання з РЕПЛІКИ курсором, а не одним масивом
  const cursor = replica.query(new Cursor('SELECT * FROM orders'));
  for await (const row of cursor) {
    ws.addRow(transformRow(row)).commit(); // commit звільняє рядок
  }

  await wb.commit();
  const { Location } = await upload.done();
  await notifyUser(job.data.userId, Location);
}`,
      },
      {
        label: 'HTTP-шар: 202 замість очікування',
        code: `// ❌ синхронний експорт: памʼять, таймаут балансувальника, навантаження на прод-базу
app.get('/export', async (req, res) => {
  const rows = await db.query('SELECT * FROM orders'); // мільйони рядків у масив
  res.send(await buildXlsxInMemory(rows));
});

// ✅ запит лише створює задачу
app.post('/exports', async (req, res) => {
  const job = await exportQueue.add('orders-xlsx', { userId: req.user.id });
  res.status(202).json({ jobId: job.id, statusUrl: \`/exports/\${job.id}\` });
});`,
      },
    ],
    seniorNotes: [
      'Метод commit у потоковому режимі exceljs — обовʼязковий: без виклику row.commit і worksheet.commit бібліотека тримає рядки в памʼяті, і потоковий режим не дає жодної переваги перед звичайним.',
      'XLSX це zip із XML, тому процесор витрачається не на базу, а на серіалізацію й стиснення; при десятках паралельних експортів вузьким місцем стає саме процесор воркера, а не диск чи мережа.',
      'Курсор тримає відкритою транзакцію на весь час читання, а в PostgreSQL довга транзакція блокує очищення мертвих рядків — ще одна причина виносити експорт на репліку, де це нікому не заважає.',
      'S3 multipart upload вимагає частин не менше пʼяти мегабайт, окрім останньої, тому PassThrough із занадто малим буфером дає багато дрібних запитів; клас Upload з lib-storage накопичує частини сам.',
      'Посилання на готовий файл має бути підписаним і короткоживучим: експорт містить персональні дані, і публічний URL у бакеті — типова знахідка аудиту безпеки.',
    ],
  },

  {
    id: 'node-idempotency',
    title: 'Як захиститись від подвійного списання коштів при повторному запиті?',
    definition:
      'Захист будується на ідемпотентності: клієнт генерує унікальний Idempotency-Key на кожну логічну операцію й надсилає його в заголовку, а сервер зберігає ключ разом із результатом у таблиці з унікальним індексом. Перший запит виконує операцію в одній транзакції з вставкою ключа, повторний натикається на конфлікт унікальності й повертає збережену відповідь, не виконуючи списання вдруге. Другий шар захисту — на рівні даних: блокування рядка рахунку через SELECT FOR UPDATE або оновлення з умовою, щоб паралельні запити не читали однаковий залишок. Кнопка з блокуванням на фронтенді не є захистом: повтор може прийти від мережевого ретраю, балансувальника або черги.',
    why: 'Мережа ненадійна: клієнт міг отримати таймаут, хоча платіж пройшов, і його повторна спроба спише гроші двічі. Те саме дає повтор задачі в черзі або ретрай HTTP-клієнта. Без ключа ідемпотентності сервер принципово не може відрізнити повтор старої операції від нової такої самої.',
    simple:
      'Проблема в тому, що сервер бачить два однакові запити й не має способу зрозуміти, це дві різні покупки чи одна, надіслана двічі. Рішення — попросити клієнта самому дати імʼя операції: унікальний ключ, згенерований у момент натискання кнопки і незмінний при всіх повторах. Сервер тримає таблицю з унікальним індексом по цьому ключу. Прийшов запит — сервер у межах однієї транзакції вставляє ключ і виконує списання. Якщо це повтор, вставка падає на порушенні унікальності, транзакція відкочується, і сервер віддає раніше збережену відповідь з тим самим статусом і тілом. Важлива деталь: ключ і результат операції мусять фіксуватись однією транзакцією, інакше падіння між ними лишає ключ без результату або результат без ключа. Друга деталь — стан у процесі: якщо повтор прийшов, поки перший запит ще виконується, правильна відповідь це 409 або статус processing, а не тихе виконання вдруге. Паралельно працює захист на рівні даних: списання пишуть як UPDATE balance = balance - 100 WHERE id = ... AND balance >= 100 замість читання залишку в код і запису назад, бо між читанням і записом встигає вклинитись інший запит. І остання деталь, яку люблять питати: ключ має бути привʼязаний до користувача й до тіла запиту, щоб той самий ключ із іншою сумою не повертав чужу відповідь, а давав помилку.',
    related: [
      {
        title: 'Ідемпотентність методів HTTP',
        text: 'GET, PUT і DELETE ідемпотентні за визначенням: повторення дає той самий стан. POST ідемпотентним не є, тому саме для нього вводять окремий заголовок Idempotency-Key — так це зроблено в Stripe і в більшості платіжних API. Заголовок не є стандартом RFC, але фактично став галузевою конвенцією, і згадка Stripe тут доречна.',
      },
      {
        title: 'Втрачене оновлення й атомарний UPDATE',
        text: 'Схема прочитати залишок у код, відняти, записати назад дає класичне втрачене оновлення: два запити прочитали по тисячі й обидва записали девʼятсот. Лікується або атомарним UPDATE з арифметикою й умовою прямо в SQL, або SELECT FOR UPDATE, який тримає рядок до комміту, або перевіркою версії рядка з повтором при конфлікті.',
      },
      {
        title: 'Exactly-once не існує на мережі',
        text: 'Гарантувати рівно одну доставку неможливо: відправник ніколи не знає напевно, чи дійшла відповідь. Практична схема завжди виглядає як доставка щонайменше один раз плюс ідемпотентний обробник, що дає ефект рівно одного виконання. Саме такою формулою варто відповідати замість обіцянки exactly-once.',
      },
      {
        title: 'Узгодженість між сервісами',
        text: 'Коли списання й замовлення живуть у різних сервісах, однієї транзакції немає. Тоді беруть transactional outbox: подія пишеться в таблицю тією ж транзакцією, що й зміна даних, а окремий процес доставляє її в чергу. Для відкату розподіленої операції використовують сагу з компенсуючими діями замість спроби зробити двофазний комміт.',
      },
    ],
    codeExamples: [
      {
        label: 'Idempotency-Key: ключ і операція в одній транзакції',
        code: `CREATE TABLE idempotency_keys (
  key         text PRIMARY KEY,
  user_id     bigint NOT NULL,
  request_hash text NOT NULL,
  response    jsonb,
  created_at  timestamptz DEFAULT now()
);`,
      },
      {
        label: 'Обробник платежу',
        code: `app.post('/payments', async (req, res) => {
  const key = req.header('Idempotency-Key');
  if (!key) return res.status(400).json({ error: 'Idempotency-Key required' });

  const existing = await db.oneOrNone(
    'SELECT response, request_hash FROM idempotency_keys WHERE key = $1', [key]);
  if (existing) {
    if (existing.request_hash !== hash(req.body))
      return res.status(422).json({ error: 'key reused with different body' });
    return res.json(existing.response); // ✅ повтор: та сама відповідь
  }

  await db.tx(async (t) => {
    // унікальний індекс ловить гонку двох одночасних запитів
    await t.none('INSERT INTO idempotency_keys (key, user_id, request_hash) VALUES ($1,$2,$3)',
      [key, req.user.id, hash(req.body)]);
    // атомарне списання: без read-modify-write
    const r = await t.result(
      'UPDATE accounts SET balance = balance - $1 WHERE id = $2 AND balance >= $1',
      [req.body.amount, req.user.accountId]);
    if (r.rowCount === 0) throw new PaymentError('insufficient funds');
    await t.none('UPDATE idempotency_keys SET response = $1 WHERE key = $2',
      [{ status: 'ok' }, key]);
  });

  res.json({ status: 'ok' });
});`,
      },
    ],
    seniorNotes: [
      'Ключ ідемпотентності генерує клієнт, а не сервер, і генерує його один раз на логічну операцію: якщо ретрай-клієнт генерує новий ключ на кожну спробу, уся схема перетворюється на звичайний незахищений POST.',
      'Записи ідемпотентності треба чистити за строком життя, типово 24 години, інакше таблиця росте нескінченно; Stripe у своєму API теж обмежує життя ключа добою.',
      'Повтор, що прийшов під час виконання першого запиту, має отримати 409 Conflict або відповідь зі статусом processing — віддавати порожній успіх до фіксації результату означає підтвердити операцію, яка ще може провалитись.',
      'Унікальний індекс по ключу — єдиний надійний арбітр гонки: перевірка через SELECT перед INSERT не рятує, бо два паралельні запити можуть одночасно не знайти запис і обидва піти виконувати списання.',
      'Схема read-modify-write у коді застосунку небезпечна навіть під транзакцією на READ COMMITTED: рядок треба або блокувати через FOR UPDATE, або міняти одним UPDATE з арифметикою й умовою в самому SQL.',
    ],
  },
]

import type { Topic } from '../types'

/*
 * ЯК ДОДАТИ НОВЕ ПИТАННЯ 👇
 * Скопіюйте блок нижче в кінець масиву (перед закривною дужкою ]):
 *
 * {
 *   id: 'unikalnyi-id',            // латиницею, через дефіс
 *   title: 'Питання, як його ставлять на співбесіді',
 *   description:
 *     'Пояснення простими словами. Пишіть кількома реченнями — сайт сам розібʼє їх на абзаци. Назви функцій і код у тексті підсвітяться автоматично.',
 *   codeExamples: [
 *     {
 *       label: 'Що показує приклад',
 *       code: `const x = 1; // сам код`,
 *     },
 *   ],
 *   gotchas: [
 *     'Підводний камінь або каверзне запитання №1.',
 *     'Підводний камінь №2.',
 *   ],
 * },
 */

export const javascriptTopics: Topic[] = [
  {
    id: 'data-types',
    title: 'Усі типи даних у JS (Map, Set, Symbol, WeakSet, WeakMap)',
    description:
      'У JavaScript 8 типів: 7 примітивів і обʼєкти. Примітиви (прості неподільні значення): string, number (один тип для цілих і дробових), bigint (цілі понад 2^53, суфікс n), boolean, undefined («не задали»), null («свідомо порожньо»), symbol (гарантовано унікальний ідентифікатор). Усе інше — object: обʼєкти, масиви, функції, дати, колекції. Ключова різниця: примітиви незмінні, копіюються й порівнюються ЗА ЗНАЧЕННЯМ; обʼєкти — ЗА ПОСИЛАННЯМ (дві змінні можуть вказувати на той самий обʼєкт). Колекції: Map — словник із будь-якими ключами (навіть обʼєктами) і збереженням порядку вставки; Set — лише унікальні значення (прибрати дублікати); WeakMap/WeakSet — тримають ключі «слабко», не заважаючи GC видалити непотрібний обʼєкт.',
    codeExamples: [
      {
        label: 'Перевірка типів',
        code: `typeof "hi"        // "string"
typeof 42          // "number"
typeof 10n         // "bigint"
typeof true        // "boolean"
typeof undefined   // "undefined"
typeof null        // "object"  ← історичний баг
typeof Symbol()    // "symbol"
typeof {}          // "object"
typeof []          // "object"  (масив — теж об'єкт)
typeof (() => {})  // "function"`,
      },
      {
        label: 'Map / Set / Symbol',
        code: `const map = new Map();
map.set("a", 1).set({}, 2); // ключем може бути будь-що
map.get("a"); // 1

const set = new Set([1, 1, 2, 3]);
[...set]; // [1, 2, 3] — лише унікальні

const id = Symbol("id"); // унікальний завжди
const user = { [id]: 123 };`,
      },
    ],
    gotchas: [
      'typeof null === "object" — історичний баг, для перевірки на null використовуйте value === null.',
      'NaN — це number, причому NaN !== NaN; перевіряйте через Number.isNaN.',
      'Map.get/set працюють за посиланням: new Map().set({}, 1).get({}) поверне undefined — це різні обʼєкти.',
      'WeakMap не ітерується і не має .size — це плата за слабкі посилання.',
      'Symbol не можна привести до рядка неявно (String(sym) — ок, "" + sym — TypeError).',
      'Кожен Symbol унікальний навіть з однаковим описом: Symbol("id") !== Symbol("id"); опис читається через .description.',
    ],
  },
  {
    id: 'event-loop',
    title: 'Event loop (call stack, macrotask, microtask, requestAnimationFrame)',
    description:
      'Event loop — механізм на рівні JS-рушія, який координує роботу трьох сутностей: Call Stack (стек викликів, де виконується код), Web API (браузерні API, яким віддаються таймери й запити) і Callback Queue (черги завдань). JavaScript однопотоковий: асинхронна операція йде у Web API, коли готова — її колбек стає в чергу, і щойно стек порожній, event loop бере наступне завдання з черги. Черг дві. Макротаски: setTimeout/setInterval, події, мережеві відповіді — беруться ПО ОДНОМУ за цикл. Мікротаски: колбеки промісів (.then, await), queueMicrotask — виконуються ВСІ підряд одразу після поточного коду. Порядок: синхронний код → всі мікротаски → можливо рендер → один макротаск → знову всі мікротаски. requestAnimationFrame — окрема черга перед перемальовуванням екрана (~60 разів/с), для анімацій.',
    codeExamples: [
      {
        label: 'Порядок виконання',
        code: `console.log("1");
setTimeout(() => console.log("2"), 0);
Promise.resolve().then(() => console.log("3"));
console.log("4");

// Вивід: 1, 4, 3, 2
// Синхронний код → усі мікротаски → один макротаск`,
      },
      {
        label: 'Мікротаски «голодять» рендер',
        code: `// Цей цикл мікротасків заблокує рендер і обробку подій,
// бо мікротаски виконуються повністю до наступного макротаска
function loop() {
  Promise.resolve().then(loop);
}
// loop(); // не запускайте :)`,
      },
    ],
    gotchas: [
      'await — це синтаксичний цукор над .then: код після await потрапляє в чергу мікротасків.',
      'setTimeout(fn, 0) не означає «зараз» — мінімум ~4мс при вкладеності й завжди після мікротасків.',
      'Важка синхронна робота блокує весь loop: ні події, ні рендер не оброблятимуться.',
      'rAF не виконується у фонових вкладках — анімації на ньому ставляться на паузу.',
    ],
  },
  {
    id: 'web-workers',
    title: 'Web Workers — фонові потоки в браузері',
    description:
      'JavaScript у браузері виконується в одному потоці: важке обчислення заморожує сторінку — не працюють кліки, анімації, все «висить». Web Worker — спосіб винести таку роботу в окремий фоновий потік. Воркер — це окремий JS-файл, який виконується паралельно з головним потоком і спілкується з ним лише повідомленнями: postMessage — надіслати, onmessage — отримати. У воркера немає доступу до DOM, window і document — він уміє лише обчислювати, робити fetch і працювати з даними. Типові застосування: парсинг великих JSON, обробка зображень, сортування або фільтрація величезних масивів, шифрування. Не плутати: Service Worker — це проксі між сайтом і мережею (кеш, offline, push-повідомлення), а «web-сервіс» — узагалі серверне API (REST, SOAP), а не браузерна технологія.',
    codeExamples: [
      {
        label: 'main.js ↔ worker.js',
        code: `// main.js — головний потік
const worker = new Worker("worker.js");
worker.postMessage({ numbers: hugeArray }); // віддали роботу
worker.onmessage = (e) => {
  console.log("Результат:", e.data); // отримали відповідь
};
// Інтерфейс весь цей час живий і реагує на кліки

// worker.js — фоновий потік
onmessage = (e) => {
  const sum = e.data.numbers.reduce((a, b) => a + b, 0);
  postMessage(sum); // повертаємо результат
};`,
      },
      {
        label: 'Без воркера vs з воркером',
        code: `// ❌ У головному потоці: сторінка «зависне» на час розрахунку
button.onclick = () => {
  const result = heavyCalculation(); // блокує ВСЕ
  show(result);
};

// ✅ У воркері: сторінка жива, результат прийде подією
button.onclick = () => {
  worker.postMessage("start");
  worker.onmessage = (e) => show(e.data);
};`,
      },
    ],
    gotchas: [
      'У воркері немає DOM, window, document — змінювати сторінку може лише головний потік.',
      'Дані в postMessage КОПІЮЮТЬСЯ (structured clone) — великі обʼєкти передавати дорого; для буферів є transferable objects.',
      'Воркер живе, поки його не зупинили: terminate() з головного потоку або close() зсередини.',
      'Web Worker ≠ Service Worker: перший — про важкі обчислення, другий — про мережу, кеш і offline.',
      'Створення воркера не безкоштовне (час і памʼять) — не плодіть їх на кожну дрібну задачу.',
    ],
  },
  {
    id: 'timers',
    title: 'setTimeout, setInterval, setImmediate, process.nextTick',
    description:
      'Таймери планують код на майбутнє. setTimeout(fn, ms) — виконати один раз через затримку; повертає id, який можна скасувати через clearTimeout. setInterval(fn, ms) — виконувати повторно кожні ms мілісекунд (скасовується clearInterval). Затримка — це мінімум, а не гарантія: якщо движок зайнятий, виклик відкладеться. setImmediate і process.nextTick існують лише в Node.js (не в браузері). setImmediate — «виконай одразу після того, як event loop завершить поточний прохід» (окремий різновид макротаска). process.nextTick — найвищий пріоритет: виконується одразу після поточної операції, навіть раніше за мікротаски промісів. Порядок у Node зазвичай: nextTick → проміси → таймери/immediate.',
    codeExamples: [
      {
        label: 'setTimeout vs setInterval',
        code: `const t = setTimeout(() => console.log("раз"), 1000);
clearTimeout(t);

let n = 0;
const i = setInterval(() => {
  if (++n === 3) clearInterval(i);
  console.log("tick", n);
}, 1000);`,
      },
      {
        label: 'Порядок у Node.js',
        code: `setTimeout(() => console.log("timeout"), 0);
setImmediate(() => console.log("immediate"));
Promise.resolve().then(() => console.log("promise"));
process.nextTick(() => console.log("nextTick"));

// Зазвичай: nextTick → promise → timeout/immediate
// nextTick виконується раніше за мікротаски промісів`,
      },
    ],
    gotchas: [
      'setInterval не чекає завершення колбека: повільні ітерації накладаються — краще рекурсивний setTimeout.',
      'Затримка таймера — це мінімум, а не гарантія: завантажений стек відкладе виконання.',
      'Рекурсивний process.nextTick блокує event loop, не даючи обробити I/O.',
      'this всередині setTimeout-колбека не вказує на оточення — використовуйте стрілки.',
      'setImmediate не існує в браузері (тільки Node), нестандартний у застарілих API.',
    ],
  },
  {
    id: 'promise-generators',
    title: 'Promise і yield (генератори)',
    description:
      'Promise — обʼєкт-«обіцянка» результату, якого ще немає: представляє майбутній результат асинхронної операції (запиту, таймера). Три стани: pending (триває), fulfilled (успіх, є значення), rejected (помилка); стан змінюється лише ОДИН раз. Підписка на результат: .then — успіх, .catch — помилка, .finally — у будь-якому разі (сховати спінер). Дві неочевидні речі: проміс «жадібний» — код у new Promise стартує одразу, не чекаючи .then; і скасувати його штатно не можна (для запитів є AbortController). Генератор — функція function*, яку можна ставити на паузу словом yield і продовжувати через .next(); кожен .next() повертає обʼєкт { value, done }, yield віддає значення назовні, а next(value) передає назад усередину. Коротко: проміс — про ОДНЕ майбутнє значення, генератор — про ПОСЛІДОВНІСТЬ значень по одному. Історично async/await побудований саме на звʼязці генератори + проміси.',
    codeExamples: [
      {
        label: 'Promise',
        code: `const p = new Promise((resolve, reject) => {
  setTimeout(() => resolve("готово"), 100);
});
p.then(v => console.log(v)).catch(e => console.error(e));`,
      },
      {
        label: 'Генератор з yield',
        code: `function* gen() {
  const a = yield 1;   // пауза, повертає 1
  const b = yield a + 1;
  return b;
}
const it = gen();
it.next();    // { value: 1, done: false }
it.next(10);  // a=10 → { value: 11, done: false }
it.next(20);  // b=20 → { value: 20, done: true }`,
      },
    ],
    gotchas: [
      'Проміс «жадібний» (eager): код у new Promise() запуститься одразу, ще до .then.',
      'Перший .next() лише доходить до першого yield; значення, передане в перший next(), ігнорується.',
      'Помилку всередині генератора можна «вкинути» через it.throw().',
      'Проміс не можна скасувати штатно — для цього використовують AbortController.',
      'Забутий .catch на rejected-промісі → unhandled rejection.',
      'finally не отримує результат проміса (аргумент порожній) і «пропускає» його далі; повернене з finally значення ігнорується.',
    ],
  },
  {
    id: 'async-await',
    title: 'async / await',
    description:
      'async/await — зручний синтаксис поверх промісів, який робить асинхронний код схожим на синхронний. Функція з ключовим словом async завжди повертає Promise: навіть якщо ви повернули звичайне число, воно автоматично обгорнеться в проміс. Слово await можна ставити лише всередині async-функції; воно «чекає», поки проміс завершиться, і повертає його значення — при цьому основний потік НЕ блокується (інші задачі продовжують виконуватись). Помилки ловляться звичайним try/catch — await кидає виняток, якщо проміс відхилився. Важлива деталь продуктивності: послідовні await виконуються один за одним, а незалежні операції краще запускати паралельно через Promise.all.',
    codeExamples: [
      {
        label: 'Базовий приклад',
        code: `async function getUser(id) {
  try {
    const res = await fetch("/api/users/" + id);
    if (!res.ok) throw new Error("HTTP " + res.status);
    return await res.json();
  } catch (e) {
    console.error("Не вдалося:", e);
    throw e;
  }
}`,
      },
      {
        label: 'Паралельно vs послідовно',
        code: `// ❌ Послідовно — повільно (сумарний час)
const a = await fetchA();
const b = await fetchB();

// ✅ Паралельно — швидше (максимум з двох)
const [a2, b2] = await Promise.all([fetchA(), fetchB()]);`,
      },
    ],
    gotchas: [
      'await у циклі серіалізує ітерації — для незалежних задач беріть Promise.all.',
      'array.map(async ...) повертає масив промісів, а не результатів — потрібен await Promise.all.',
      'forEach не чекає async-колбеків — використовуйте for...of.',
      'Помилку в async-функції без await/catch ззовні легко проґавити (unhandled rejection).',
      'async-функція завжди повертає Promise — навіть return 5 стане Promise<number>.',
    ],
  },
  {
    id: 'destructuring',
    title: 'Деструктуризація масивів і обʼєктів',
    description:
      'Деструктуризація — синтаксис, що «розпаковує» значення з масивів чи властивості з обʼєктів у окремі змінні одним виразом. Для масивів значення беруться за позицією: const [a, b] = arr. Для обʼєктів — за назвою властивості: const { name } = user. Підтримує: значення за замовчуванням (const { role = "user" } = obj) — спрацьовує лише коли значення undefined; перейменування (const { city: town } = obj) — змінна матиме нове імʼя; пропуск елементів масиву (const [, second] = arr); вкладеність; та збір решти через rest (const [head, ...tail] = arr). Дуже зручно в параметрах функцій — отримуєш «іменовані аргументи» з дефолтами, не залежні від порядку.',
    codeExamples: [
      {
        label: 'Обʼєкти і масиви',
        code: `const user = { name: "Ada", age: 36, city: "London" };
const { name, age = 0, city: town } = user;
// name="Ada", age=36, town="London" (перейменували city → town)

const [first, , third] = [10, 20, 30]; // пропуск елемента
const [head, ...tail] = [1, 2, 3];      // head=1, tail=[2,3]`,
      },
      {
        label: 'У параметрах функції',
        code: `function createUser({ name, role = "user" } = {}) {
  return name + ": " + role;
}
createUser({ name: "Bob" }); // "Bob: user"
createUser();                // не впаде завдяки = {}`,
      },
    ],
    gotchas: [
      'Дефолт застосовується тільки до undefined; null його не активує.',
      'Деструктуризація обʼєкта без обгортки в дужки на початку рядка ламає парсер — потрібні ({...}).',
      'Без = {} деструктуризація відсутнього аргументу кидає TypeError.',
      'Це поверхнева операція: вкладені обʼєкти/масиви лишаються тими ж посиланнями.',
      'Перейменування { city: town } часто плутають із типізацією — це саме нова назва змінної.',
    ],
  },
  {
    id: 'getter-setter',
    title: 'getter / setter',
    description:
      'Геттер (get) і сеттер (set) — спеціальні методи, які зовні виглядають як звичайна властивість, але під час читання чи запису виконують код. Геттер викликається, коли властивість читають (obj.x), і повертає значення — зручно для обчислюваних полів (наприклад, fullName з імені та прізвища). Сеттер викликається при присвоєнні (obj.x = 5), отримує нове значення й може його перевірити чи перетворити перед збереженням. Це інструмент інкапсуляції: ззовні простий доступ obj.x, а всередині — контроль. Оголошуються в обʼєктах і класах через get/set, або вручну через Object.defineProperty. Геттер без сеттера робить властивість фактично «тільки для читання».',
    codeExamples: [
      {
        label: 'У класі',
        code: `class Temperature {
  #celsius = 0;
  get celsius() { return this.#celsius; }
  set celsius(v) {
    if (typeof v !== "number") throw new TypeError("number expected");
    this.#celsius = v;
  }
  get fahrenheit() { return this.#celsius * 1.8 + 32; }
}
const t = new Temperature();
t.celsius = 25;
t.fahrenheit; // 77 — обчислюється на льоту`,
      },
      {
        label: 'Через defineProperty',
        code: `const obj = {};
Object.defineProperty(obj, "full", {
  get() { return this.first + " " + this.last; },
  enumerable: true,
});
obj.first = "Ada"; obj.last = "Lovelace";
obj.full; // "Ada Lovelace"`,
      },
    ],
    gotchas: [
      'Геттер без сеттера → властивість read-only; присвоєння в strict mode кине TypeError.',
      'Важка логіка в геттері маскується під «звичайне читання» і ускладнює профілювання.',
      'Рекурсія: this.x усередині геттера/сеттера для тієї ж властивості → нескінченний цикл (зберігайте у #поле).',
      'Геттери не серіалізуються очікувано: JSON.stringify візьме обчислене значення, а сеттери ігноруються.',
      'У class fields порядок ініціалізації важливий — сеттер може спрацювати під час конструювання.',
    ],
  },
  {
    id: 'prototype',
    title: 'Навіщо потрібен prototype в обʼєктів і функцій',
    description:
      'Прототип — це обʼєкт, від якого інший обʼєкт успадковує властивості й методи. Кожен обʼєкт має приховане посилання [[Prototype]] (читається через Object.getPrototypeOf, історично — __proto__). Коли ви звертаєтесь до властивості, якої в обʼєкта немає, движок шукає її в прототипі, потім у прототипі прототипа — і так угору по «ланцюгу прототипів» аж до null. Функції-конструктори мають властивість prototype: усе, що в неї покладено, стає спільним для всіх обʼєктів, створених через new. Саме тому методи кладуть у prototype — вони існують в одному екземплярі для всіх обʼєктів, а не копіюються в кожен. Класи (class) — це лише зручний синтаксис над цим самим механізмом.',
    codeExamples: [
      {
        label: 'Ланцюг прототипів',
        code: `function Animal(name) { this.name = name; }
Animal.prototype.speak = function () {
  return this.name + " видає звук";
};
const dog = new Animal("Рекс");
dog.speak();                          // метод знайдено в прототипі
Object.getPrototypeOf(dog) === Animal.prototype; // true`,
      },
      {
        label: 'Пошук по ланцюгу',
        code: `const obj = {};
obj.toString();          // метод з Object.prototype
obj.hasOwnProperty("x"); // теж із прототипу
// ланцюг: obj → Object.prototype → null`,
      },
    ],
    gotchas: [
      'Стрілкові функції не мають властивості prototype і не можуть бути конструкторами.',
      'Зміна Object.setPrototypeOf на льоту різко деоптимізує доступ до властивостей.',
      'Розширення вбудованих прототипів (Array.prototype.foo) — антипатерн, ризик колізій.',
      'for...in перебирає і власні, і успадковані властивості; Object.keys/values — лише власні (перевірка — hasOwnProperty).',
      'Метод, узятий із прототипу, виконується з this поточного обʼєкта — методи спільні, але працюють із даними конкретного екземпляра.',
      '__proto__ — застаріле/нестандартне; коректно Object.getPrototypeOf/setPrototypeOf.',
    ],
  },
  {
    id: 'new-event',
    title: 'new Event — навіщо потрібен і як використовувати',
    description:
      'Конструктор new Event() створює подію, яку можна запустити програмно, без дії користувача. Приймає два аргументи: перший — тип події (рядок, напр. "change"), другий — обʼєкт options, усі властивості якого необовʼязкові й за замовчуванням дорівнюють false. Опція bubbles — чи спливатиме подія по DOM до батьківських елементів (без неї не працює делегування). Опція cancelable — чи можна подію скасувати через preventDefault(). Опція composed — чи вийде подія за межу Shadow DOM. Сама подія нічого не робить, поки її не «вистрелити» на елементі через element.dispatchEvent(event); ловлять її звичайним addEventListener. Якщо треба передати дані — беруть CustomEvent з опцією detail, яку слухач читає як e.detail. Типове застосування — веб-компоненти та звʼязок між незалежними частинами застосунку.',
    codeExamples: [
      {
        label: 'CustomEvent з даними',
        code: `const evt = new CustomEvent("cart:add", {
  detail: { id: 42, qty: 2 },
  bubbles: true,
});
document.querySelector("#cart")?.dispatchEvent(evt);

document.addEventListener("cart:add", (e) => {
  console.log("Додано:", e.detail); // { id: 42, qty: 2 }
});`,
      },
      {
        label: 'Простий Event та його опції',
        code: `const evt = new Event("change", {
  bubbles: true,     // спливає до батьківських елементів
  cancelable: true,  // можна скасувати через preventDefault()
  composed: false,   // не вийде за межі Shadow DOM
});

document.querySelector("input")?.dispatchEvent(evt);
// корисно, коли треба програмно «емулювати» подію`,
      },
    ],
    gotchas: [
      'Дані треба класти в detail; довільні поля на event — нестандартно і ненадійно.',
      'bubbles за замовчуванням false — без нього подія не спливе і делегування не зловить.',
      'preventDefault працює лише для cancelable: true.',
      'У React синтетичні події живуть окремо — нативний CustomEvent треба слухати на DOM-вузлі напряму.',
      'Старий спосіб document.createEvent застарів — використовуйте конструктори.',
      'dispatchEvent синхронний: усі слухачі відпрацюють до наступного рядка коду.',
      'У штучної події isTrusted === false, і дія за замовчуванням не виконується: new Event("submit") викличе слухачів, але форму не відправить.',
    ],
  },
  {
    id: 'oop-principles',
    title: 'Принципи ООП: інкапсуляція, наслідування, поліморфізм, абстракція',
    description:
      'Чотири «кити» обʼєктно-орієнтованого програмування — про них зазвичай питають перед SOLID. Інкапсуляція: дані й методи, що з ними працюють, живуть разом в обʼєкті, а внутрішня «кухня» схована — ззовні доступний лише публічний інтерфейс (у JS це приватні поля #field або замикання). Наслідування: клас-нащадок отримує властивості й методи батька і додає свої (extends); у JS під капотом це прототипний ланцюжок. Поліморфізм: різні класи мають метод з одним іменем, але своєю поведінкою — код викликає pet.speak(), не знаючи, кіт це чи собака, і кожен відповідає по-своєму. Абстракція: виділяємо суттєве й ховаємо деталі — користувачу методу save() не потрібно знати, як саме влаштоване збереження. Важливо сказати на співбесіді: JS — прототипно-орієнтована мова, і class — це синтаксичний цукор над прототипами.',
    codeExamples: [
      {
        label: 'Інкапсуляція + наслідування',
        code: `class Animal {
  #energy = 100;            // приватне поле — ззовні не дістати
  get energy() { return this.#energy; } // публічний доступ
  eat() { this.#energy += 10; }
}

class Dog extends Animal {  // наслідування
  bark() { return "Гав!"; } // + своє
}

const rex = new Dog();
rex.eat();     // метод успадковано від Animal
rex.bark();    // власний метод
// rex.#energy // SyntaxError — інкапсуляція працює`,
      },
      {
        label: 'Поліморфізм',
        code: `class Cat  { speak() { return "Мяу"; } }
class Dog  { speak() { return "Гав"; } }
class Duck { speak() { return "Кря"; } }

// Один код працює з усіма, не знаючи конкретного класу
for (const pet of [new Cat(), new Dog(), new Duck()]) {
  console.log(pet.speak()); // Мяу, Гав, Кря
}`,
      },
    ],
    gotchas: [
      'JS — прототипна мова: class не створює «справжніх класів», це цукор над прототипним успадкуванням.',
      'Справжню приватність дають лише #поля (ES2022) та замикання; _underscore — тільки домовленість між людьми.',
      'Глибокі ієрархії наслідування — антипатерн: на практиці «композиція краща за наслідування».',
      'У самому JS немає інтерфейсів та абстрактних класів на рівні мови — їх додає TypeScript.',
      'Поліморфізм у JS — «качина типізація»: важливо, щоб метод існував, а не від якого класу обʼєкт.',
    ],
  },
  {
    id: 'solid',
    title: 'SOLID principles',
    description:
      'SOLID — пʼять правил, як писати код, який легко змінювати й не страшно розширювати. S — Single Responsibility (єдина відповідальність): один клас робить одну річ, і в нього лише одна причина змінюватися. O — Open/Closed (відкритий/закритий): нову поведінку додаємо НОВИМ кодом (новий клас), а не редагуванням старого — тоді старе не ламається. L — Liskov Substitution (підстановка Лісков): нащадка можна підставити замість батька, і програма цього не помітить; якщо нащадок «вміє менше» за батька — ієрархія побудована неправильно. I — Interface Segregation (розділення інтерфейсів): краще кілька маленьких інтерфейсів, ніж один «жирний», щоб клас не мусив реалізовувати методи, які йому не потрібні. D — Dependency Inversion (інверсія залежностей): код залежить від абстракції («щось, що вміє зберігати»), а не від конкретики («саме MySQL») — конкретику підставляємо ззовні, тому її легко замінити або замокати в тестах.',
    codeExamples: [
      {
        label: 'S — Single Responsibility: один клас = одна робота',
        code: `// ❌ Клас робить ТРИ різні речі
class Report {
  calculate() { /* рахує дані */ }
  saveToFile() { /* пише на диск */ }
  sendEmail()  { /* шле листа */ }
}
// Зміна формату листа змушує чіпати клас із розрахунками

// ✅ Кожен клас — одна відповідальність
class ReportCalculator { calculate() { /* ... */ } }
class ReportSaver  { save(report)  { /* ... */ } }
class ReportMailer { send(report)  { /* ... */ } }
// Змінився спосіб відправки? Чіпаємо лише ReportMailer`,
      },
      {
        label: 'O — Open/Closed: розширюємо, не редагуючи',
        code: `// ❌ Кожен новий спосіб оплати = правка цього if
function pay(order, method) {
  if (method === "card") { /* ... */ }
  else if (method === "paypal") { /* ... */ }
  // завтра сюди доліплять "crypto", "applePay"...
}

// ✅ Новий спосіб оплати = новий клас, старе не чіпаємо
interface Payment { pay(order: Order): void; }
class CardPayment   implements Payment { pay(o) { /* ... */ } }
class PayPalPayment implements Payment { pay(o) { /* ... */ } }

function checkout(order: Order, method: Payment) {
  method.pay(order); // працює з будь-якою реалізацією
}`,
      },
      {
        label: 'L — Liskov: нащадок не ламає очікувань',
        code: `// ❌ Пінгвін — птах, але fly() кидає помилку
class Bird { fly() { /* летить */ } }
class Penguin extends Bird {
  fly() { throw new Error("Пінгвіни не літають!"); }
}
function launch(bird: Bird) { bird.fly(); } // впаде на Penguin

// ✅ Літає лише той, хто справді вміє
class Bird {}
class FlyingBird extends Bird { fly() { /* летить */ } }
class Sparrow extends FlyingBird {}
class Penguin extends Bird {} // без fly — і без сюрпризів`,
      },
      {
        label: 'I — Interface Segregation: без «жирних» інтерфейсів',
        code: `// ❌ Старий принтер ЗМУШЕНИЙ реалізувати все
interface Device { print(): void; scan(): void; fax(): void; }
class OldPrinter implements Device {
  print() { /* ок */ }
  scan()  { throw new Error("не вмію"); } // зайві методи
  fax()   { throw new Error("не вмію"); }
}

// ✅ Маленькі інтерфейси — береш лише потрібне
interface Printer { print(): void; }
interface Scanner { scan(): void; }
class OldPrinter implements Printer { print() { /* ок */ } }
class MFP implements Printer, Scanner { print() {} scan() {} }`,
      },
      {
        label: 'D — Dependency Inversion: залежимо від абстракції',
        code: `// ❌ Сервіс намертво привʼязаний до MySQL
class UserService {
  private db = new MySQLDatabase(); // не замінити, не замокати
  save(user) { this.db.insert(user); }
}

// ✅ Залежимо від «вміє зберігати», конкретика — ззовні
interface Storage { insert(user: User): void; }
class UserService {
  constructor(private db: Storage) {} // будь-яка реалізація
  save(user: User) { this.db.insert(user); }
}
new UserService(new MySQLDatabase());   // прод
new UserService(new InMemoryStorage()); // тести`,
      },
    ],
    gotchas: [
      'Надмірне дроблення (SRP до абсурду) множить файли й абстракції без користі.',
      'OCP легко перетворити на передчасну узагальненість — абстрагуйте за фактом, а не «на майбутнє».',
      'Порушення LSP часто непомітне: підклас кидає виняток там, де базовий працював.',
      'DIP без потреби (інтерфейс на кожен клас) роздуває код у малих проєктах.',
      'SOLID — про ООП; у функціональному стилі ті ж цілі досягаються композицією функцій.',
    ],
  },
  {
    id: 'var-let-const',
    title: 'Відмінність var, const, let',
    description:
      'Три способи оголосити змінну з різною поведінкою. var має функціональну область видимості (видно у всій функції, ігнорує блоки if/for) і «піднімається» (hoisting) на початок функції з початковим значенням undefined. let і const мають блокову область видимості (видно лише у {} блоці). Вони теж піднімаються, але потрапляють у «мертву зону» (TDZ) — звернення до них до рядка оголошення кидає помилку, а не повертає undefined. Різниця між let і const: let можна переприсвоювати, const — ні. Важливо: const забороняє лише переприсвоєння змінної, але не забороняє мутувати вміст обʼєкта чи масиву (поля можна змінювати, push робити можна). Сучасне правило: const за замовчуванням, let — коли значення міняється, var — не використовувати.',
    codeExamples: [
      {
        label: 'Scope і hoisting',
        code: `function demo() {
  if (true) {
    var a = 1;   // функціональна — видно поза if
    let b = 2;   // блокова — лише в if
  }
  console.log(a); // 1
  // console.log(b); // ReferenceError
}`,
      },
      {
        label: 'const і мутація',
        code: `const arr = [1, 2];
arr.push(3);     // ✅ можна — мутуємо вміст
// arr = [];      // ❌ TypeError — не можна переприсвоїти

console.log(typeof x); // ReferenceError (TDZ), а не "undefined"
let x = 5;`,
      },
    ],
    gotchas: [
      'const забороняє переприсвоєння, але не забороняє мутацію обʼєкта/масиву.',
      'Звернення до let/const до оголошення — ReferenceError (TDZ), не undefined.',
      'var-оголошення піднімається й ініціалізується undefined — джерело прихованих багів.',
      'var у циклі — одна спільна змінна на всі ітерації; класична пастка із замиканнями.',
      'var ігнорує блоковий scope (if, for) — видно у всій функції.',
    ],
  },
  {
    id: 'scope',
    title: 'Область видимості (scope)',
    description:
      'Scope (область видимості) визначає, де змінна доступна в коді. Рівні: глобальна (видно скрізь), функціональна (всередині функції) і блокова (всередині {} — лише для let/const). Коли код звертається до змінної, движок шукає її спочатку в поточній області, потім у зовнішній, далі ще вище — це «ланцюг областей видимості» (scope chain). JavaScript використовує лексичну (статичну) область видимості: доступність змінної визначається тим, ДЕ функцію оголошено в коді, а не звідки її викликали. Саме з лексичного scope випливають замикання. Важливо не плутати scope (для змінних, визначається місцем оголошення) із this (для контексту, визначається місцем виклику).',
    codeExamples: [
      {
        label: 'Scope chain',
        code: `const g = "global";
function outer() {
  const o = "outer";
  function inner() {
    const i = "inner";
    return g + " " + o + " " + i; // має доступ до всіх рівнів
  }
  return inner();
}
outer(); // "global outer inner"`,
      },
      {
        label: 'Лексичність, а не місце виклику',
        code: `const name = "module";
function greet() { return name; }
function run() {
  const name = "local";
  return greet(); // "module" — бачить scope ОГОЛОШЕННЯ
}
run();`,
      },
    ],
    gotchas: [
      'Не плутати лексичний scope (для змінних) із динамічним this (для контексту).',
      'Shadowing: внутрішня змінна непомітно перекриває зовнішню однойменну.',
      'Випадкове створення глобальної змінної (присвоєння без оголошення в non-strict) — баг.',
      'Блоковий scope мають лише let/const, var його ігнорує.',
      'Глибока вкладеність подовжує scope chain і ускладнює читабельність.',
    ],
  },
  {
    id: 'closures',
    title: 'Замикання (closures)',
    description:
      'Замикання — це функція разом із «памʼяттю» про змінні з її зовнішнього оточення. Коли функцію оголошено всередині іншої, вона зберігає доступ до змінних зовнішньої функції — навіть після того, як зовнішня вже завершила виконання. Важлива деталь: замикання захоплює не копію значення, а саму змінну («живий» доступ до неї), тому завжди бачить її актуальний стан. Це наслідок лексичної області видимості. Замикання — щоденний інструмент: приватний стан (змінна доступна лише через повернені функції), фабрики функцій, мемоізація, debounce/throttle (зберігають таймер між викликами), а в React хуки useState/useCallback тримають значення саме в замиканні. Зворотний бік — памʼять: захоплені змінні не звільняються, поки існує функція.',
    codeExamples: [
      {
        label: 'Лічильник із приватним станом',
        code: `function makeCounter() {
  let count = 0;            // приватна змінна
  return {
    inc: () => ++count,
    get: () => count,
  };
}
const c = makeCounter();
c.inc(); c.inc();
c.get(); // 2 — count недоступний ззовні напряму`,
      },
      {
        label: 'Класична пастка циклу',
        code: `// var — ОДНА спільна змінна на всі ітерації → завжди 3
for (var i = 0; i < 3; i++) setTimeout(() => console.log(i));
// → 3, 3, 3

// let — нова змінна на кожну ітерацію → 0, 1, 2
for (let j = 0; j < 3; j++) setTimeout(() => console.log(j));`,
      },
    ],
    gotchas: [
      'Замикання тримають посилання на змінні — необережне захоплення великих обʼєктів дає витоки памʼяті.',
      'Stale closure в React: колбек бачить старе значення стейту без правильних залежностей.',
      'var у циклі — одна спільна змінна для всіх ітерацій, тому всі колбеки бачать фінальне значення.',
      'Захоплюється змінна, а не її значення на момент створення функції.',
      'Надмірне створення замикань у гарячих циклах тисне на GC.',
    ],
  },
  {
    id: 'object-equality',
    title: 'Чому порівняння двох схожих обʼєктів дає false',
    description:
      'Обʼєкти в JS порівнюються за посиланням, а не за вмістом. Кожен обʼєктний літерал ({} чи []) створює нову комірку в памʼяті, і змінна зберігає посилання на неї. Тому {} === {} дає false — це два різні обʼєкти, навіть якщо вони порожні чи мають однакові поля. true буде лише тоді, коли обидві змінні вказують на той самий обʼєкт (одне посилання). Примітиви натомість порівнюються за значенням: 1 === 1 і "hi" === "hi" дають true. Щоб порівняти обʼєкти за вмістом, потрібне «глибоке» порівняння: або вручну порівнювати потрібні поля, або через бібліотеку (lodash.isEqual), або (з обмеженнями) через JSON.stringify.',
    codeExamples: [
      {
        label: 'Reference vs value',
        code: `{} === {};                 // false — різні посилання
const a = { x: 1 };
const b = a;
a === b;                    // true — те саме посилання
[1,2] === [1,2];            // false

// примітиви порівнюються за значенням
1 === 1;                    // true
"hi" === "hi";              // true`,
      },
      {
        label: 'Як порівняти за вмістом',
        code: `const eq = (a, b) =>
  JSON.stringify(a) === JSON.stringify(b); // прийом, але крихкий
eq({ x: 1 }, { x: 1 }); // true

// надійніше — глибоке порівняння (lodash.isEqual)
// або порівняти потрібні поля вручну`,
      },
    ],
    gotchas: [
      'JSON.stringify для порівняння залежить від порядку ключів і губить undefined/функції.',
      'NaN !== NaN, а Object.is(NaN, NaN) === true — окрема семантика.',
      'У React новий обʼєкт із тим самим вмістом тригерить ререндер (порівняння за посиланням).',
      'Передача обʼєкта у функцію дозволяє мутувати оригінал — це теж наслідок посилань.',
      '-0 === 0 → true, але Object.is(-0, 0) → false.',
    ],
  },
  {
    id: 'hof',
    title: 'High order functions',
    description:
      'Функція вищого порядку (HOF) — функція, яка приймає іншу функцію як аргумент або повертає функцію як результат (чи і те, й інше). Це можливо, бо в JS функція — звичайне значення, як число: її можна класти в змінну, передавати, повертати. «Приймають функцію»: map, filter, reduce, sort, addEventListener — ви передаєте СВОЮ функцію, вони її викликають. «Повертають функцію»: debounce(fn, 300) повертає «стриману» версію вашої функції; так само декоратори (обгортки з логуванням/кешем) і каррінг (фіксація частини аргументів). Навіщо: замість «ЯК обходити масив циклом» кажете «ЩО зробити з кожним елементом». Коротка відповідь для співбесіди: «HOF — функція, що приймає та/або повертає функцію; приклади — map і debounce».',
    codeExamples: [
      {
        label: 'Приймає функцію',
        code: `const nums = [1, 2, 3, 4];
nums.map(x => x * 2);          // [2,4,6,8]
nums.filter(x => x % 2 === 0); // [2,4]
nums.reduce((s, x) => s + x);  // 10`,
      },
      {
        label: 'Повертає функцію (фабрика/каррінг)',
        code: `const multiplier = factor => x => x * factor;
const double = multiplier(2);
double(5); // 10

const withLog = fn => (...args) => {
  console.log("call", args);
  return fn(...args);
};`,
      },
    ],
    gotchas: [
      'Ланцюг map().filter() створює проміжні масиви й кілька проходів — на великих даних дорого.',
      'Інлайн-стрілки як пропси в React щоразу нові — можуть провокувати зайві ререндери.',
      'reduce без початкового значення на порожньому масиві кидає помилку.',
      'Глибока композиція/каррінг ускладнює стектрейси й дебаг.',
      'Передача методу як колбека втрачає this (arr.forEach(obj.method)) — потрібен bind/стрілка.',
    ],
  },
  {
    id: 'spread',
    title: 'Spread оператор',
    description:
      'Spread-оператор (...) — оператор розширення: «бере» кожен окремий елемент ітерованого обʼєкта (масиву, рядка, Set) і «розпаковує» його в інший масив, обʼєкт або в аргументи функції. Використання: створення масивів ([...a, item]), злиття обʼєктів ({ ...obj, newField }), передача елементів масиву як окремих аргументів (Math.max(...arr)), копіювання. Дуже важлива деталь: spread робить лише ПОВЕРХНЕВУ копію — розгортається тільки верхній рівень, а вкладені обʼєкти й масиви лишаються спільними посиланнями (зміна вкладеного поля копії зачепить і оригінал). Порядок має значення: у { ...a, ...b } пізніші поля перекривають ранні. Це основний інструмент імутабельних оновлень стану в React та Redux.',
    codeExamples: [
      {
        label: 'Масиви й обʼєкти',
        code: `const a = [1, 2];
const b = [...a, 3];          // [1,2,3]
const merged = [...a, ...b];  // конкатенація

const user = { name: "Ada" };
const updated = { ...user, age: 36 }; // копія + нове поле
Math.max(...[3, 1, 4]);       // розгортання в аргументи`,
      },
      {
        label: 'Поверхневість копії',
        code: `const orig = { nested: { x: 1 } };
const copy = { ...orig };
copy.nested.x = 99;
orig.nested.x; // 99 — вкладене поле спільне!`,
      },
    ],
    gotchas: [
      'Spread — поверхнева копія: вкладені обʼєкти лишаються спільними посиланнями.',
      '[...acc, x] у reduce/циклі → O(n²); використовуйте push.',
      'Обʼєктний spread копіює лише власні перелічувані властивості (не прототип, не геттери як геттери).',
      'Не можна спредити non-iterable у масив (spread числа кине помилку).',
      'Порядок важливий: { ...a, ...b } — пізніші поля перекривають ранні.',
    ],
  },
  {
    id: 'spread-vs-rest',
    title: 'Різниця між spread і rest',
    description:
      'Символ однаковий (...), але напрямок дії протилежний, і контекст вирішує, що це. Spread РОЗГОРТАЄ колекцію в окремі елементи — застосовується справа від = (при створенні масивів/обʼєктів) та в аргументах виклику функції. Rest НАВПАКИ ЗБИРАЄ кілька окремих елементів в один масив (або решту властивостей у новий обʼєкт) — застосовується зліва від = (у деструктуризації) та в параметрах функції. Просте мнемонічне правило: rest пакує, spread розпаковує. Rest-параметр (function f(...args)) дає справжній масив усіх аргументів — кращу заміну застарілому arguments. Rest у деструктуризації зручний, щоб «відрізати» поле: const { password, ...safe } = user. Обмеження: rest має бути останнім і єдиним.',
    codeExamples: [
      {
        label: 'Rest — збирає',
        code: `function sum(...nums) {        // rest: усі аргументи → масив
  return nums.reduce((s, n) => s + n, 0);
}
sum(1, 2, 3); // 6

const [first, ...others] = [1, 2, 3, 4]; // others = [2,3,4]
const { id, ...rest } = { id: 1, a: 2, b: 3 }; // rest = {a:2,b:3}`,
      },
      {
        label: 'Spread — розгортає',
        code: `const nums = [1, 2, 3];
sum(...nums);            // spread: масив → окремі аргументи
const copy = [...nums];  // розгортання при створенні масиву`,
      },
    ],
    gotchas: [
      'Rest-параметр має бути останнім і єдиним — function f(...a, b) — синтаксична помилка.',
      'arguments — це не масив (rest — справжній масив); у стрілках arguments взагалі немає.',
      'Обʼєктний rest бере лише власні перелічувані властивості.',
      'Плутанина новачків: однаковий символ ... має протилежний сенс залежно від позиції.',
      'Rest у деструктуризації робить поверхневу копію решти, як і spread.',
    ],
  },
  {
    id: 'typeof-null',
    title: 'Чому typeof null повертає "object"',
    description:
      'Це історичний баг ще з першої версії JavaScript (1995 рік). У ранній реалізації значення зберігалися як тег типу плюс самі дані; тег 000 означав «обʼєкт», а null був представлений як нульовий вказівник (усі нулі) — тож перевірка тега класифікувала null як "object". Це визнана помилка, але виправити її вже не можна: занадто багато коду в інтернеті на неї розраховує, і зміна зламала б веб. Практичний наслідок: typeof не годиться для перевірки на null — потрібно писати value === null. А якщо треба переконатися, що значення саме «справжній» обʼєкт, обовʼязково спершу відсіяти null: value !== null && typeof value === "object", інакше null помилково пройде як обʼєкт.',
    codeExamples: [
      {
        label: 'Прояв',
        code: `typeof null;       // "object"  ← баг
typeof undefined;  // "undefined"
typeof {};         // "object"
typeof function(){}; // "function"`,
      },
      {
        label: 'Як перевіряти правильно',
        code: `const v = null;
v === null;                          // ✅ єдиний надійний спосіб
v == null;                           // true і для null, і для undefined

// перевірка «справжнього» обʼєкта:
const isObject = x => x !== null && typeof x === "object";`,
      },
    ],
    gotchas: [
      'typeof null === "object" — не можна використовувати typeof для перевірки на null.',
      'Перевірка isObject без відсіювання null хибно прийме null за обʼєкт.',
      '== null зручно ловить і null, і undefined одночасно — іноді це бажано.',
      'Array теж дає typeof "object" — для масивів є Array.isArray.',
      'Виправити баг неможливо без злому сумісності — це навмисно лишили.',
    ],
  },
  {
    id: 'null-undefined',
    title: 'Відмінність null і undefined',
    description:
      'Обидва означають «немає значення», але різного походження. undefined — це значення за замовчуванням від самого рантайму: змінна оголошена, але не присвоєна; властивість, якої немає в обʼєкті; функція без return; відсутній аргумент. null — це навмисна порожнеча, яку програміст явно ставить, щоб сказати «тут свідомо немає значення». typeof undefined дає "undefined", а typeof null — "object" (історичний баг). Порівняння: null == undefined → true (нестроге), але null === undefined → false (строге). Ключова практична відмінність: значення за замовчуванням у параметрах і деструктуризації спрацьовують лише на undefined, а null проходить як «реальне» значення. Сучасні оператори ?? і ?. однаково трактують обидва.',
    codeExamples: [
      {
        label: 'Звідки беруться',
        code: `let x;                 // undefined — не присвоєно
({}).missing;          // undefined — немає властивості
(() => {})();          // повертає undefined

let y = null;          // null — явно «порожньо»
typeof undefined;      // "undefined"
typeof null;           // "object"`,
      },
      {
        label: 'Порівняння та дефолти',
        code: `null == undefined;     // true  (нестроге)
null === undefined;    // false (строге)

function f(a = 10) { return a; }
f(undefined); // 10 — дефолт спрацював
f(null);      // null — дефолт НЕ спрацював`,
      },
    ],
    gotchas: [
      'Дефолтні параметри й деструктуризація реагують лише на undefined, не на null.',
      'null == undefined → true, але === → false; легко переплутати в перевірках.',
      'typeof null === "object", typeof undefined === "undefined".',
      'JSON.stringify зберігає null, але викидає поля зі значенням undefined.',
      '|| ловить також 0/""/false; для «лише null/undefined» потрібен ??.',
      'В арифметиці null перетворюється на 0, а undefined дає NaN: 5 + null === 5, але 5 + undefined → NaN.',
    ],
  },
  {
    id: 'this',
    title: 'Контекст this і як він працює',
    description:
      'Значення this називається контекстом виклику і буде визначено в момент виклику функції. У глобальній області: у звичайному скрипті this — це window, а на верхньому рівні ES-модуля — undefined (модулі завжди strict). Стрілкові функції власного this не мають — значення контексту береться з батьківської області видимості. На відміну від звичайних функцій, змінити значення this всередині стрілки після її оголошення не можна: call/apply/bind на неї не діють. Тому стрілки ідеальні для колбеків, але не для методів обʼєкта. Класична пастка — «вирвати» метод (const f = obj.method; f()): this губиться.',
    codeExamples: [
      {
        label: 'Правила виклику',
        code: `// верхній рівень: <script> → window, ES-модуль → undefined

function show() { return this; }
const obj = { name: "A", show };
obj.show();        // obj — виклик як метод
show();            // undefined (strict) / window
new show();        // новий обʼєкт
show.call({ x: 1 }); // { x: 1 } — явна привʼязка`,
      },
      {
        label: 'Стрілка vs звичайна',
        code: `const timer = {
  secs: 0,
  startBad() { setTimeout(function () { this.secs++; }, 0); }, // this загублено
  startGood() { setTimeout(() => { this.secs++; }, 0); },      // стрілка бере this з методу
};`,
      },
    ],
    gotchas: [
      'Вирваний метод (const f = obj.m; f()) втрачає this.',
      'Стрілка як метод обʼєкта/класу бере this з модуля, а не з екземпляра — баг.',
      'this у звичайному колбеку (setTimeout, forEach) не вказує на оточення.',
      'bind повертає нову функцію; повторний bind не перевизначає перший.',
      'call/apply/bind не діють на стрілку — її this зафіксовано в момент оголошення.',
      'У звичайному виклику this — undefined (strict) або global — джерело прихованих помилок.',
    ],
  },
  {
    id: 'array-methods',
    title: 'Методи масивів: мутуючі vs немутуючі, every / some / find',
    description:
      'Головне, що перевіряють на співбесіді — чи знаєте ви, які методи ЗМІНЮЮТЬ сам масив (мутують), а які повертають новий. Мутуючі: push і pop (додати/зняти з кінця), shift і unshift (з початку), splice (видалити/вставити будь-де), а також sort і reverse — так, вони змінюють оригінал! Немутуючі — оригінал не чіпають, повертають новий масив або значення: map (перетворити кожен елемент), filter (залишити ті, що пройшли умову), slice (вирізати копію шматка), concat (склеїти масиви), reduce (згорнути все в одне значення). Трійка перевірок, яку люблять питати: every — чи ВСІ елементи проходять умову; some — чи проходить хоч ОДИН; find — повернути ПЕРШИЙ елемент, що пройшов (findIndex — його індекс, includes — чи є конкретне значення). У сучасному JS (ES2023) зʼявилися немутуючі версії: toSorted, toReversed, toSpliced.',
    codeExamples: [
      {
        label: 'Мутуючі vs немутуючі',
        code: `const arr = [3, 1, 2];

// Мутуючі — ЗМІНЮЮТЬ arr
arr.push(4);   // arr тепер [3, 1, 2, 4]
arr.sort();    // arr тепер [1, 2, 3, 4] — оригінал зіпсовано!

// Немутуючі — оригінал цілий, повертається нове
const doubled = arr.map(n => n * 2);        // [2, 4, 6, 8]
const evens   = arr.filter(n => n % 2 === 0); // [2, 4]
const part    = arr.slice(1, 3);            // копія шматка
const sorted  = arr.toSorted();             // ES2023: sort без мутації`,
      },
      {
        label: 'every / some / find — у чому різниця',
        code: `const nums = [1, 2, -3, 4];

nums.every(n => n > 0);     // false — НЕ всі додатні
nums.some(n => n < 0);      // true  — є хоч один відʼємний
nums.find(n => n < 0);      // -3    — ПЕРШИЙ відʼємний елемент
nums.findIndex(n => n < 0); // 2     — його індекс
nums.includes(4);           // true  — чи є конкретне значення`,
      },
      {
        label: 'sort — класична пастка',
        code: `[10, 9, 1].sort();               // [1, 10, 9] — порівнює як РЯДКИ!
[10, 9, 1].sort((a, b) => a - b); // [1, 9, 10] — як числа

// sort мутує; щоб не зіпсувати оригінал:
const sorted = [...arr].sort((a, b) => a - b);
// або сучасне: arr.toSorted((a, b) => a - b)`,
      },
    ],
    gotchas: [
      'sort() без компаратора сортує як рядки: [10, 9, 1].sort() дасть [1, 10, 9].',
      'sort і reverse мутують масив — у React-стані це джерело багів; беріть [...arr].sort() або toSorted().',
      'find повертає undefined, якщо нічого не знайдено — перевірка if (item) обманить на знайдених 0, "" чи false.',
      'map завжди повертає масив тієї ж довжини; «перетворити і відфільтрувати» за один прохід — це flatMap.',
      'forEach не можна перервати (break не працює, return ігнорується) — для раннього виходу беріть for..of або some.',
    ],
  },
  {
    id: 'reduce',
    title: 'reduce і reduceRight',
    description:
      'reduce «згортає» масив в одне підсумкове значення. Він проходить елементи зліва направо, накопичуючи результат у акумуляторі: callback отримує (acc, current, index, array), і те, що ви повернули, стає новим acc для наступної ітерації. Другий аргумент reduce — початкове значення акумулятора, і його варто задавати завжди. reduce — найзагальніша з функцій масиву: через нього виражаються сума, групування, побудова обʼєкта/Map, навіть map і filter. reduceRight робить те саме, але йде справа наліво — потрібен, коли напрямок важливий (композиція функцій, правоасоціативні операції). Без початкового значення на порожньому масиві reduce кидає помилку, а на масиві з одного елемента взагалі не викликає колбек.',
    codeExamples: [
      {
        label: 'Сума й групування',
        code: `[1, 2, 3, 4].reduce((acc, x) => acc + x, 0); // 10

const people = [{ age: 20 }, { age: 30 }, { age: 20 }];
people.reduce((m, p) => {
  m[p.age] = (m[p.age] || 0) + 1;
  return m;
}, {}); // { "20": 2, "30": 1 }`,
      },
      {
        label: 'reduceRight (напрямок має значення)',
        code: `["a", "b", "c"].reduce((a, x) => a + x);      // "abc"
["a", "b", "c"].reduceRight((a, x) => a + x); // "cba"
// композиція функцій справа наліво — типовий кейс reduceRight`,
      },
    ],
    gotchas: [
      'reduce без initialValue на порожньому масиві кидає TypeError.',
      'Без initialValue перший елемент стає acc, і колбек пропускає індекс 0 — легко проґавити.',
      'Мутація acc працює, але приховує побічні ефекти — для чистоти повертайте новий обʼєкт (ціною швидкості).',
      'Надто хитрий reduce читається гірше за звичайний цикл — не зловживайте.',
      'Не плутати напрямок: reduceRight для композиції/правоасоціативних операцій.',
    ],
  },
  {
    id: 'regexp',
    title: 'RegExp',
    description:
      'Регулярний вираз (RegExp) — шаблон для пошуку в тексті: описуєте, ЩО шукаєте («чотири цифри, дефіс, дві цифри»), а движок знаходить збіги. Створення: /шаблон/прапори або new RegExp(). Методи: re.test(str) — чи є збіг (валідація), str.match(re) — дістати збіг, str.replace(re, нове) — замінити, str.matchAll(re) — усі збіги. Цеглинки шаблону: класи символів (\\d — цифра, \\w — літера/цифра/_, \\s — пробіл), квантифікатори — скільки разів (* — 0 і більше, + — 1 і більше, ? — 0 або 1, {2,4} — від 2 до 4), групи (...) — виділити частину збігу, іменовані групи (?<year>\\d{4}), якорі ^ і $ — початок/кінець рядка. Прапори: g — усі збіги, i — без урахування регістру, m — багаторядковий, u — коректний Unicode (емодзі). Складний шаблон збирайте поступово й перевіряйте на regex101 — він пояснює кожен символ.',
    codeExamples: [
      {
        label: 'Основні методи',
        code: `const re = /(\\d{4})-(\\d{2})-(\\d{2})/;
"2026-06-25".match(re);   // повний матч + групи
re.test("2026-06-25");    // true

"a1b2c3".replace(/\\d/g, "#"); // "a#b#c#"
[..."a1b2".matchAll(/(\\w)(\\d)/g)]; // усі матчі з групами`,
      },
      {
        label: 'Іменовані групи й прапор g',
        code: `const m = "25.06.2026".match(/(?<d>\\d+)\\.(?<m>\\d+)\\.(?<y>\\d+)/);
m.groups.y; // "2026"

const g = /x/g;
g.test("x"); g.test("x"); // lastIndex «памʼятає» позицію!`,
      },
    ],
    gotchas: [
      'Глобальний regex (g) зберігає lastIndex — повторні test/exec дають збій через раз.',
      'Catastrophic backtracking ((a+)+) → зависання/ReDoS на спеціально дібраному вводі.',
      'Не парсіть HTML/JSON/вкладені структури регулярками — це нерегулярні граматики.',
      '. за замовчуванням не матчить перенос рядка — потрібен прапор s.',
      'Без прапора u емодзі/сурогатні пари обробляються некоректно.',
    ],
  },
  {
    id: 'object-creation',
    title: 'Способи створення обʼєктів у JS',
    description:
      'Обʼєкт у JS можна створити пʼятьма основними способами — і на співбесіді варто назвати всі та сказати, коли який доречний. 1) Літерал {} — найпростіший і найуживаніший: пишемо властивості одразу; підходить для одиничних обʼєктів. 2) Клас (class + new) — сучасний стандарт, коли потрібно багато однакових обʼєктів зі спільними методами. 3) Функція-конструктор (function + new) — так робили до появи класів; class під капотом — це вона ж. 4) Object.create(proto) — створює обʼєкт із явно вказаним прототипом; Object.create(null) дає «чистий» обʼєкт узагалі без прототипу (без toString тощо) — зручно як словник без успадкованих сюрпризів. 5) Фабрична функція — звичайна функція, що повертає обʼєкт: не потребує new і природно «ховає» приватні дані в замиканні. Ключова думка: class і функція-конструктор — один механізм (прототипне успадкування), просто різний синтаксис.',
    codeExamples: [
      {
        label: 'Літерал, клас, фабрика',
        code: `// 1. Літерал — для одиничних обʼєктів
const user = { name: "Дан", age: 25 };

// 2. Клас — коли обʼєктів багато і в них спільні методи
class Car {
  constructor(brand) { this.brand = brand; }
  drive() { return \`\${this.brand} їде\`; }
}
const car = new Car("Toyota");

// 3. Фабрика — без new, з приватними даними в замиканні
function createCounter() {
  let count = 0; // приватна: ззовні не дістати
  return { inc: () => ++count, get: () => count };
}`,
      },
      {
        label: 'Функція-конструктор і Object.create',
        code: `// 4. Функція-конструктор (так було до класів)
function Person(name) { this.name = name; }
Person.prototype.hi = function () {
  return "Привіт, " + this.name;
};
const p = new Person("Іван");

// 5. Object.create — обʼєкт із явним прототипом
const proto = { greet() { return "hello"; } };
const obj = Object.create(proto);
obj.greet(); // "hello" — взято з прототипу

const dict = Object.create(null); // взагалі без прототипу`,
      },
    ],
    gotchas: [
      'Виклик функції-конструктора БЕЗ new не створить обʼєкт: this буде undefined (strict mode) або window.',
      'Стрілочну функцію не можна викликати з new — у неї немає власного this і внутрішнього конструктора.',
      'Object.create(null) не має hasOwnProperty і toString — деякий код на цьому несподівано падає.',
      'new Object() і {} — те саме, але в реальному коді пишуть лише літерал: коротше і швидше.',
      'Фабрика створює копію методів на кожен обʼєкт (замикання), клас ділить методи через прототип — памʼять економніша.',
    ],
  },
  {
    id: 'classes',
    title: 'Класи в JS',
    description:
      'class — шаблон для створення багатьох схожих обʼєктів через new. Важливо сказати на співбесіді: це НЕ нова модель обʼєктів, а зручний синтаксис поверх прототипного успадкування. Складові: constructor — виконується при new і заповнює поля; методи — лежать у prototype, спільні для всіх екземплярів (не копіюються в кожен обʼєкт); поля екземпляра — свої в кожного; static — належить самому класу (Class.method()); геттери/сеттери; приватні поля #field — справжня рантайм-приватність, а не домовленість, як _underscore. Спадкування: extends; super() викликає батьківський конструктор (обовʼязково до звернення до this), super.method() — батьківський метод. Нюанси: тіло класу завжди в strict mode; клас не можна використати до оголошення (немає hoisting, як у function); методи не привʼязують this автоматично — «вирваний» метод губить контекст.',
    codeExamples: [
      {
        label: 'Спадкування',
        code: `class Animal {
  #name;
  constructor(name) { this.#name = name; }
  speak() { return this.#name + " видає звук"; }
  static create(n) { return new Animal(n); }
}
class Dog extends Animal {
  speak() { return super.speak() + " (гав)"; }
}
new Dog("Рекс").speak();`,
      },
      {
        label: 'Приватні поля',
        code: `class Account {
  #balance = 0;             // справді приватне (рантайм)
  deposit(v) { this.#balance += v; return this; }
  get balance() { return this.#balance; }
}
const a = new Account();
a.deposit(100);
// a.#balance — SyntaxError ззовні`,
      },
    ],
    gotchas: [
      'Методи класу не біндять this — вирваний метод втрачає контекст.',
      'Класи не гойстяться (TDZ): використання до оголошення → ReferenceError.',
      'Забутий super() у конструкторі похідного класу → помилка доступу до this.',
      'Приватні #поля недоступні ззовні навіть рефлексією; _name — лише конвенція.',
      'Глибокі ієрархії спадкування крихкі — частіше краща композиція.',
    ],
  },
  {
    id: 'webrtc',
    title: 'WebRTC',
    description:
      'WebRTC — технологія для звʼязку в реальному часі майже напряму між браузерами: аудіо, відео й довільні дані з низькою затримкою. Складається з трьох частин: getUserMedia (доступ до камери/мікрофона), RTCPeerConnection (саме зʼєднання, медіа автоматично шифрується), RTCDataChannel (передача довільних даних, як швидкий UDP-канал). Важливо: WebRTC peer-to-peer для медіа, але встановлення зʼєднання НЕ безсерверне — потрібен ваш власний «сигналінг» (зазвичай WebSocket), щоб два браузери обмінялися описом зʼєднання (SDP-офер/відповідь) та мережевими кандидатами (ICE). Для обходу NAT/фаєрволів використовують STUN (дізнатися свою публічну адресу) і TURN (ретранслятор трафіку, коли прямий звʼязок неможливий). Працює лише через HTTPS.',
    codeExamples: [
      {
        label: 'Захоплення медіа',
        code: `const stream = await navigator.mediaDevices.getUserMedia({
  video: true, audio: true,
});
document.querySelector("video").srcObject = stream;`,
      },
      {
        label: 'Як встановлюється зʼєднання — сторона A (хто дзвонить)',
        code: `const pc = new RTCPeerConnection({
  iceServers: [{ urls: "stun:stun.l.google.com:19302" }],
});

// 1) кладемо свої треки (камера/мікрофон) у зʼєднання
stream.getTracks().forEach(t => pc.addTrack(t, stream));

// 2) створюємо офер — SDP-текст «що я вмію: кодеки, аудіо, відео»
const offer = await pc.createOffer();
await pc.setLocalDescription(offer);
signaling.send({ offer }); // 3) шлемо через СВІЙ сервер (WebSocket)

// 4) паралельно браузер збирає ICE-кандидатів
//    (варіанти «як до мене достукатись») — теж шлемо співрозмовнику
pc.onicecandidate = e =>
  e.candidate && signaling.send({ candidate: e.candidate });

// 5) B відповів своїм SDP (answer) — зберігаємо
signaling.on("answer", a => pc.setRemoteDescription(a));
signaling.on("candidate", c => pc.addIceCandidate(c));

// 6) кандидати «зійшлися» → пряме зʼєднання, приходить медіа від B
pc.ontrack = e => { videoEl.srcObject = e.streams[0]; };`,
      },
      {
        label: 'Сторона B (хто приймає дзвінок)',
        code: `signaling.on("offer", async offer => {
  await pc.setRemoteDescription(offer);   // 1) запамʼятали, що вміє A
  const answer = await pc.createAnswer(); // 2) свій SDP у відповідь
  await pc.setLocalDescription(answer);
  signaling.send({ answer });             // 3) назад через сигналінг
});`,
      },
      {
        label: 'Шпаргалка термінів',
        code: `Термін     | Що це простими словами            | Навіщо потрібен
-----------|-----------------------------------|----------------------------------
Сигналінг  | ваш сервер (напр. WebSocket)      | передати SDP та ICE між двома
SDP        | текст «що я вмію»: кодеки, медіа  | домовитись про формат звʼязку
ICE        | перебір можливих адрес/шляхів     | знайти робочий маршрут
STUN       | сервер-«дзеркало»                 | дізнатись свою публічну IP за NAT
TURN       | сервер-ретранслятор трафіку       | коли напряму пробитись неможливо
SFU        | сервер, що роздає потоки          | групові дзвінки (P2P не тягне)`,
      },
    ],
    gotchas: [
      'WebRTC не включає сигналінг — обмін SDP/ICE треба реалізувати самому (напр. через WebSocket).',
      'Без TURN частина зʼєднань за симетричним NAT/фаєрволом не встановиться.',
      'getUserMedia працює лише в безпечному контексті (HTTPS/localhost) і потребує дозволу користувача.',
      'Mesh P2P не масштабується на багатьох учасників — потрібен SFU.',
      'ICE-кандидати приходять асинхронно (trickle) — логіку зʼєднання треба будувати на подіях.',
      'Медіа в WebRTC їде поверх UDP (швидкість важливіша за гарантію доставки кожного пакета), на відміну від HTTP, що працює поверх TCP.',
    ],
  },
  {
    id: 'soap',
    title: 'SOAP',
    description:
      'SOAP (Simple Object Access Protocol) — протокол обміну структурованими повідомленнями у форматі XML; транспорт може бути будь-який (зазвичай HTTP, але й SMTP чи FTP). Кожне повідомлення — «конверт» (Envelope) із заголовком (Header — авторизація, метадані), тілом (Body — самі дані чи виклик) та опціональним Fault (інформація про помилку). Для контрасту: REST — не протокол, а архітектурний стиль поверх HTTP із довільним форматом даних (зазвичай JSON). SOAP має суворий контракт, описаний у WSDL-файлі, з якого можна автоматично згенерувати клієнт і сервер та строго все типізувати. Має вбудовані корпоративні стандарти WS-* — зокрема WS-Security (шифрування й підпис на рівні повідомлення) і WS-AtomicTransaction. Це старша й важча альтернатива REST: більше XML-багатослівності, складніший дебаг, погано кешується. Досі живий у банкінгу, страхуванні, телекомі й легасі-інтеграціях.',
    codeExamples: [
      {
        label: 'Структура SOAP-повідомлення',
        code: `<soap:Envelope xmlns:soap="http://www.w3.org/2003/05/soap-envelope">
  <soap:Header><!-- авторизація, WS-Security --></soap:Header>
  <soap:Body>
    <getUser><id>42</id></getUser>
  </soap:Body>
</soap:Envelope>`,
      },
      {
        label: 'Виклик із JS',
        code: `await fetch("https://api.example.com/soap", {
  method: "POST",
  headers: { "Content-Type": "application/soap+xml" },
  body: envelopeXml, // рядок з XML вище
});`,
      },
    ],
    gotchas: [
      'Тільки XML — багатослівніше й важче за JSON, більший оверхед трафіку.',
      'Жорсткий WSDL-контракт: зміни ламають сумісність, потрібна перегенерація клієнтів.',
      'Погано кешується (зазвичай POST) і незручний для браузерного фронтенду.',
      'Плутають протокол SOAP зі стилем REST — це різні категорії.',
      'Дебаг складніший: треба читати XML-конверти, namespaces, заголовки WS-*.',
    ],
  },
  {
    id: 'websocket',
    title: 'Socket (WebSocket)',
    description:
      'WebSocket — технологія, яка дозволяє клієнту встановити двосторонній звʼязок із сервером у режимі реального часу через одне TCP-зʼєднання. Клієнт і сервер обмінюються даними без потреби щоразу створювати нове зʼєднання, і сервер може надсилати дані першим, не чекаючи запиту (на відміну від HTTP, де завжди «запит → відповідь»). Зʼєднання починається зі звичайного HTTP-запиту із заголовком Upgrade («рукостискання»), після чого канал лишається відкритим. Є 4 події, які слухають: open — зʼєднання встановлено, message — отримані дані, error — помилка, close — зʼєднання закрито; надсилають дані методом send. Дані передаються фреймами: текстові (UTF-8), бінарні, ping/pong (перевірка активності зʼєднання) і фрейми закриття. Закрити зʼєднання може будь-яка зі сторін. На проді: wss:// (шифровано), heartbeat і авто-перепідключення. Кейси: чати, живі котирування, нотифікації.',
    codeExamples: [
      {
        label: 'Клієнт',
        code: `const ws = new WebSocket("wss://example.com/chat");
ws.onopen = () => ws.send(JSON.stringify({ type: "join" }));
ws.onmessage = e => console.log("отримано:", e.data);
ws.onclose = () => console.log("зʼєднання закрито");
ws.onerror = err => console.error(err);`,
      },
      {
        label: 'Рукостискання (handshake) під капотом',
        code: `// Запит клієнта — звичайний HTTP із проханням переключитись:
GET /chat HTTP/1.1
Host: example.com
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==

// Відповідь сервера — протокол переключено:
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=
// далі це саме TCP-зʼєднання працює вже як WebSocket`,
      },
      {
        label: 'Heartbeat / reconnect (ідея)',
        code: `let ws;
function connect() {
  ws = new WebSocket("wss://example.com");
  ws.onclose = () => setTimeout(connect, 1000); // авто-перепідключення
  // періодичний ping для перевірки живого зʼєднання
}
connect();`,
      },
    ],
    gotchas: [
      'Зʼєднання обриваються — без heartbeat і reconnect додаток «тихо» перестає отримувати дані.',
      'Stateful-зʼєднання ускладнює горизонтальне масштабування (потрібні sticky/Redis pub-sub).',
      'wss:// (TLS) обовʼязковий на проді; ws:// блокується на HTTPS-сторінках.',
      'Деякі корпоративні проксі/фаєрволи ріжуть WebSocket — потрібен фолбек.',
      'Для односторонніх оновлень WebSocket надмірний — часто достатньо SSE.',
      'CORS-обмеження на WebSocket не діють — перевіряти заголовок Origin має сам сервер.',
    ],
  },
  {
    id: 'http',
    title: 'HTTP',
    description:
      'HTTP — «мова» спілкування браузера й сервера: клієнт надсилає запит, сервер повертає відповідь. Протокол stateless: сервер не памʼятає попередніх запитів, тому «хто ти» щоразу нагадують токеном або cookie. Методи — це намір: GET — прочитати, POST — створити, PUT — замінити повністю, PATCH — оновити частково, DELETE — видалити. GET/PUT/DELETE ідемпотентні (повтор дає той самий результат — безпечно ретраїти), POST — ні (повтор створить дублікат). Коди статусів: 2xx — успіх (200, 201, 204), 3xx — редіректи (301, 304 — «бери з кешу»), 4xx — помилка клієнта (400, 401, 403, 404, 429), 5xx — помилка сервера (500, 502, 503). Кешування — заголовки Cache-Control і ETag. Версії: HTTP/1.1 — класика; HTTP/2 — багато запитів паралельно одним зʼєднанням; HTTP/3 — поверх QUIC/UDP, швидше на поганих мережах. HTTPS — той самий HTTP, але зашифрований через TLS (стара назва — SSL): захищає дані від перехоплення й підміни.',
    codeExamples: [
      {
        label: 'Запит через fetch',
        code: `const res = await fetch("/api/users", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ name: "Ada" }),
});
res.status;            // 201
res.headers.get("etag");
const data = await res.json();`,
      },
      {
        label: 'Коди статусів',
        code: `// 2xx — успіх (200 OK, 201 Created, 204 No Content)
// 3xx — редірект (301, 304 Not Modified)
// 4xx — помилка клієнта (400, 401, 403, 404, 429)
// 5xx — помилка сервера (500, 502, 503)`,
      },
    ],
    gotchas: [
      'Неідемпотентний POST на повторі (ретрай/подвійний клік) створює дублікати — потрібні idempotency keys.',
      'CORS — це політика браузера, її не «лагодять» на фронті; налаштовується на сервері.',
      'Невірні коди статусів (200 на помилку) ламають обробку в клієнтів і кеш.',
      'Ігнорування кешування (ETag/Cache-Control) — втрата дешевої продуктивності.',
      'HTTP/1.1: браузер тримає лише ~6 паралельних зʼєднань на домен — решта запитів стає в чергу; HTTP/2 знімає це мультиплексуванням.',
    ],
  },
  {
    id: 'ajax',
    title: 'AJAX',
    description:
      'AJAX (Asynchronous JavaScript and XML) — підхід, що дозволяє обмінюватися даними із сервером і оновлювати лише частину сторінки без повного перезавантаження. Саме ця ідея свого часу зробила можливими SPA. Історично робився через XMLHttpRequest, а замість XML давно використовують JSON. Сьогодні стандарт — fetch: працює на промісах і має чистіший API. Важлива особливість fetch: він НЕ кидає помилку на HTTP-статусах 4xx/5xx — проміс завершується успішно, тож помилку треба ловити самому через перевірку res.ok. Тіло відповіді читається один раз (res.json()). Запити можна скасовувати через AbortController — критично, щоб уникати race condition при швидкій зміні вводу. Поверх fetch зазвичай будують шар із ретраями й кешем або беруть React Query/SWR.',
    codeExamples: [
      {
        label: 'Сучасний AJAX — fetch',
        code: `async function load() {
  const res = await fetch("/api/data");
  if (!res.ok) throw new Error("HTTP " + res.status);
  return res.json(); // оновлюємо лише частину UI
}`,
      },
      {
        label: 'Класичний XMLHttpRequest',
        code: `const xhr = new XMLHttpRequest();
xhr.open("GET", "/api/data");
xhr.onreadystatechange = () => {
  if (xhr.readyState === 4 && xhr.status === 200) {
    console.log(JSON.parse(xhr.responseText));
  }
};
xhr.send();`,
      },
    ],
    gotchas: [
      'fetch не кидає помилку на 404/500 — обовʼязково перевіряйте res.ok.',
      'Без AbortController застарілі запити створюють race condition і перетирають свіжі дані.',
      'Тіло відповіді читається один раз (res.json()) — повторний виклик кине помилку.',
      'CORS і credentials треба налаштовувати свідомо; cookies не йдуть без credentials: "include".',
      'XHR-стиль із колбеками легко породжує callback hell — fetch+async чистіше.',
    ],
  },
  {
    id: 'design-patterns',
    title: 'Патерни проєктування (основні)',
    description:
      'Патерн проєктування — готове, перевірене рішення типової задачі; не код для копіювання, а ідея-схема плюс спільна мова команди («тут Observer» — і всім зрозуміло). Три групи. Породжувальні — як створювати обʼєкти: Factory (функція створює обʼєкти, ховаючи деталі), Singleton (єдиний екземпляр на застосунок, наприклад зʼєднання з БД), Builder (складний обʼєкт збирається крок за кроком). Структурні — як поєднувати: Adapter (перехідник між несумісними інтерфейсами), Decorator (обгортка, що додає можливості без зміни оригіналу), Facade (простий інтерфейс над складною підсистемою), Proxy (заступник, що контролює доступ). Поведінкові — як обʼєкти спілкуються: Observer (підписка на події, як addEventListener), Strategy (взаємозамінні алгоритми, наприклад способи оплати), Command (дія як обʼєкт — можна зберегти чи скасувати). У JS Strategy — часто просто колбек, а ES-модуль — уже Singleton.',
    codeExamples: [
      {
        label: 'Observer (pub/sub)',
        code: `class EventBus {
  #subs = {};
  on(ev, fn) { (this.#subs[ev] ||= []).push(fn); }
  emit(ev, data) { (this.#subs[ev] || []).forEach(fn => fn(data)); }
}
const bus = new EventBus();
bus.on("login", u => console.log("Привіт,", u));
bus.emit("login", "Ada");`,
      },
      {
        label: 'Strategy',
        code: `const strategies = {
  card: amt => "Картка: " + amt,
  paypal: amt => "PayPal: " + amt,
};
function pay(method, amount) {
  return strategies[method](amount); // поведінка обирається на льоту
}
pay("paypal", 100);`,
      },
    ],
    gotchas: [
      'Singleton — це по суті глобальний стан: ускладнює тестування й приховує залежності.',
      'Сліпе нав’язування GoF-патернів у JS ігнорує, що функції першого класу часто роблять їх зайвими.',
      'Передчасні Factory/Abstract Factory у простому коді — over-engineering.',
      'Спостерігачі (Observer) без відписки → витоки памʼяті.',
      'Плутання патернів з архітектурними стилями (MVC/MVVM) — це різні рівні.',
    ],
  },
  {
    id: 'promise-all-race',
    title: 'Promise.all, race, allSettled, any',
    description:
      'Чотири комбінатори для роботи з кількома промісами одночасно; різняться тим, чого чекають і як реагують на помилку. Promise.all — «всі або ніхто»: запускає паралельно, повертає масив результатів у тому ж порядку; падає одразу з першою помилкою (fail-fast). Promise.race — «перегони»: завершується результатом ПЕРШОГО, що фінішував, успіхом чи помилкою; типовий кейс — таймаут для повільного запиту. Promise.allSettled — ніколи не падає: чекає всіх і дає масив обʼєктів {status:"fulfilled", value} або {status:"rejected", reason}; коли часткова відмова допустима. Promise.any — перший УСПІШНИЙ результат, помилки ігнорує; фолбек між кількома джерелами. Поруч: Promise.resolve(v) і Promise.reject(e) миттєво створюють уже завершений проміс. Важливо: жоден комбінатор не скасовує «програвших» — вони добігають у фоні.',
    codeExamples: [
      {
        label: 'all vs allSettled',
        code: `// all: один падає → весь падає
await Promise.all([fetchA(), fetchB()]); // [a, b] або throw

// allSettled: завжди дочекається всіх
const res = await Promise.allSettled([fetchA(), fetchB()]);
// [{status:"fulfilled",value}, {status:"rejected",reason}]`,
      },
      {
        label: 'race — таймаут',
        code: `function withTimeout(p, ms) {
  const t = new Promise((_, rej) =>
    setTimeout(() => rej(new Error("timeout")), ms));
  return Promise.race([p, t]); // хто перший
}
await withTimeout(fetch("/slow"), 3000);`,
      },
    ],
    gotchas: [
      'Promise.all падає на першій же помилці, але інші проміси НЕ скасовуються — для скасування потрібен AbortController.',
      'race/any не зупиняють «програвших» — вони добігають і можуть мати побічні ефекти.',
      'Plain Promise.race із вічним промісом «зависає», якщо не додати таймаут.',
      'allSettled повертає обʼєкти зі status, а не самі значення — легко забути розпакувати.',
      'Якщо в Promise.any впали ВСІ проміси — прилетить AggregateError, а окремі помилки лежать у його полі .errors.',
      'Передача в all не-промісів — ок (обгорнуться), але порожній масив одразу успішно завершується з [].',
    ],
  },
  {
    id: 'transpilation',
    title: 'Транспіляція (Babel, TypeScript): що це і навіщо',
    description:
      'Транспіляція — це перетворення коду з однієї мови чи версії в іншу ТОГО Ж рівня: написали сучасний JS або TypeScript — отримали звичайний JavaScript, який розуміють усі браузери. Навіщо: браузери користувачів оновлюються повільно, а писати хочеться сучасним синтаксисом; а TS-код браузер узагалі не виконує — його спершу треба перетворити на JS. Хто це робить: Babel (сучасний JS → старіший JS), компілятор TypeScript tsc (TS → JS плюс перевірка типів), esbuild і SWC (те саме, але в десятки разів швидше). Важлива пара понять: СИНТАКСИС транспілюється (стрілочну функцію можна переписати звичайною), а нові API — ні: щоб Promise чи fetch працювали у старому браузері, потрібен поліфіл — шматок коду, який додає відсутню функцію в рантаймі. Відмінність від компіляції: компілятор переводить код «вниз» у машинний або байт-код, транспілятор — «убік», у таку саму високорівневу мову.',
    codeExamples: [
      {
        label: 'Що робить транспілятор',
        code: `// Було (сучасний JS / TypeScript)
const greet = (name: string = "світ") => \`Привіт, \${name}!\`;

// Стало (після Babel/tsc для старих браузерів)
"use strict";
var greet = function (name) {
  if (name === void 0) { name = "світ"; }
  return "Привіт, " + name + "!";
};`,
      },
      {
        label: 'Транспіляція vs поліфіл',
        code: `// СИНТАКСИС — транспілюється:
a?.b   →   a == null ? undefined : a.b

// Нові API — НЕ транспілюються, потрібен ПОЛІФІЛ:
if (!Array.prototype.at) {
  Array.prototype.at = function (i) {
    return i < 0 ? this[this.length + i] : this[i];
  };
}`,
      },
    ],
    gotchas: [
      'Транспілятор не додає відсутні API: стрілки перепише, а Promise у старому браузері сам не зʼявиться — потрібен поліфіл (core-js).',
      'esbuild, SWC і Babel просто ВИКИДАЮТЬ типи TS без перевірки — тому в CI окремо ганяють tsc --noEmit.',
      'Налаштування target (tsconfig/browserslist) визначає, наскільки старий код генерувати — занадто старий target роздуває бандл.',
      'Транспільований код важко дебажити без source maps — вони мапають рядки назад на оригінал.',
    ],
  },
  {
    id: 'ci-cd',
    title: 'CI/CD',
    description:
      'CI/CD — це скорочення від Continuous Integration / Continuous Deployment (або Delivery). Це практика розробки, орієнтована на автоматизацію збирання, тестування й розгортання програмного забезпечення. Continuous Integration (CI): розробники регулярно зливають свій код до спільного репозиторію, і після кожного злиття автоматика збирає проєкт і ганяє тести — так одразу видно, чи не внесено помилок і чи не зламано наявну функціональність. Continuous Delivery: код, що пройшов тести, автоматично ГОТОВИЙ до релізу, але кнопку «викотити на прод» натискає людина. Continuous Deployment: і цієї кнопки немає — все, що пройшло тести, автоматично їде на прод без ручного втручання. Типовий пайплайн: install (npm ci) → lint + перевірка типів → тести (unit → integration → e2e) → build → deploy (staging → smoke-тести → prod). Навіщо це все: менше ручної роботи, швидші релізи нових функцій і фіксів, помилки виявляються рано — і загальна якість продукту зростає.',
    codeExamples: [
      {
        label: 'GitHub Actions (приклад)',
        code: `name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npm run lint
      - run: npm test -- --coverage
      - run: npm run build`,
      },
      {
        label: 'Типові стадії пайплайну',
        code: `// 1. install (npm ci — детермінований лок)
// 2. lint + typecheck
// 3. test (unit → integration → e2e)
// 4. build (артефакт)
// 5. deploy (staging → smoke-тести → prod)`,
      },
    ],
    gotchas: [
      'npm install замість npm ci — недетермінований білд (ігнорує точність лок-файлу).',
      'Секрети в репозиторії/логах — критична витока; тільки сховище секретів.',
      'Повільний/нестабільний (flaky) CI люди починають ігнорувати — довіра падає.',
      'Деплой без автоматичного rollback і смоук-тестів — ризик «зламати прод».',
      'Збирати окремий артефакт під кожне середовище замість build once, deploy many.',
    ],
  },
  {
    id: 'event-delegation',
    title: 'Делегування подій (Event Delegation)',
    description:
      'Делегування — техніка, коли замість того, щоб вішати обробник на кожен дочірній елемент, ми вішаємо один обробник на спільного батька. Працює завдяки спливанню (bubbling): подія, що сталася на дочірньому елементі, спливає вгору по DOM до предків, і там її можна перехопити. Загалом подія проходить три фази: занурення (capturing, зверху вниз) → ціль (target) → спливання (bubbling, знизу вгору); addEventListener за замовчуванням слухає фазу спливання. Усередині обробника визначаємо реальну ціль через event.target — зазвичай через event.target.closest(selector), щоб коректно зловити клік навіть по вкладеному елементу (наприклад, по іконці всередині кнопки). Дві головні переваги: менше памʼяті (один слухач замість сотень) і автоматична робота з динамічним контентом — елементи, додані пізніше, теж обробляються без переприсвоєння слухачів. Обмеження: події focus, blur, scroll, mouseenter не спливають.',
    codeExamples: [
      {
        label: 'Один обробник на список',
        code: `document.querySelector("#list").addEventListener("click", (e) => {
  const item = e.target.closest("li[data-id]");
  if (!item) return;                 // клік повз елемент
  console.log("Клікнуто id:", item.dataset.id);
});
// Працює навіть для <li>, доданих ПІСЛЯ навішування обробника`,
      },
      {
        label: 'Без делегування (анти-приклад)',
        code: `// ❌ обробник на кожен елемент — багато памʼяті,
// не працює для динамічно доданих
document.querySelectorAll("#list li").forEach(li =>
  li.addEventListener("click", handler)
);`,
      },
    ],
    gotchas: [
      'focus/blur/scroll/mouseenter не спливають — делегування не спрацює (беріть focusin/focusout).',
      'Клік по вкладеному елементу: порівнюйте через e.target.closest(), а не e.target === ...',
      'stopPropagation усередині дочірнього обробника зламає делегування на предку.',
      'Занадто «високе» делегування на document подорожчає часті події (mousemove).',
      'У React делегування вже вбудоване — ручне дублювання зазвичай зайве.',
    ],
  },
  {
    id: 'micro-macro-tasks',
    title: 'Мікро- і макротаски (micro/macro tasks)',
    description:
      'Дві черги завдань event loop із різним пріоритетом. Макротаски (macrotask): setTimeout, setInterval, обробники подій, мережеві відповіді, I/O, MessageChannel — з цієї черги движок бере по ОДНОМУ завданню за цикл. Мікротаски (microtask): колбеки промісів (.then, код після await), queueMicrotask, MutationObserver — ця черга виконується ПОВНІСТЮ за раз. Алгоритм такий: виконати поточний синхронний код → спорожнити ВСЮ чергу мікротасків (включно з тими, що додалися під час їх виконання) → можливо, перемалювати екран → взяти ОДИН макротаск → знову повністю спорожнити мікротаски. Звідси: проміс завжди спрацює раніше за setTimeout(0). І важливо: нескінченне додавання мікротасків блокує рендер і обробку подій.',
    codeExamples: [
      {
        label: 'Порядок',
        code: `console.log("script start");
setTimeout(() => console.log("setTimeout"), 0);
Promise.resolve()
  .then(() => console.log("promise 1"))
  .then(() => console.log("promise 2"));
console.log("script end");

// script start → script end → promise 1 → promise 2 → setTimeout`,
      },
      {
        label: 'Мікротаски можуть «голодувати» макротаски',
        code: `setTimeout(() => console.log("timeout"), 0);
Promise.resolve().then(function loop() {
  // якщо тут постійно плодити мікротаски,
  // "timeout" ніколи не виконається
});`,
      },
    ],
    gotchas: [
      'Усі мікротаски виконуються до наступного макротаска — нескінченне їх плодіння блокує рендер і події.',
      'await додає продовження в чергу мікротасків — код після await не синхронний.',
      'Між макротасками браузер може рендерити; між мікротасками — ні.',
      'Node має свої нюанси (process.nextTick раніше за мікротаски промісів).',
      'Покладатися на точний порядок setTimeout vs setImmediate поза I/O — недетерміновано.',
    ],
  },
  {
    id: 'call-apply-bind',
    title: 'call, apply, bind',
    description:
      'Три методи функцій для явного керування значенням this. call(thisArg, arg1, arg2, ...) — викликає функцію негайно із заданим this, аргументи передаються списком. apply(thisArg, [args]) — те саме, але аргументи передаються масивом (зручно, коли вони вже зібрані в масив). bind(thisArg, ...args) — НЕ викликає функцію, а повертає НОВУ функцію з назавжди привʼязаним this (і, за бажанням, частиною зафіксованих аргументів — часткове застосування) для виклику пізніше. Просте розрізнення: call і apply виконують одразу (різниця лише у формі аргументів), bind створює нову функцію на потім. Важливо: стрілкові функції ігнорують ці методи для this (він у них лексичний), а повторний bind не перевизначає вже привʼязаний this.',
    codeExamples: [
      {
        label: 'call / apply',
        code: `function greet(greeting, mark) {
  return greeting + ", " + this.name + mark;
}
const user = { name: "Ada" };
greet.call(user, "Привіт", "!");        // аргументи списком
greet.apply(user, ["Привіт", "!"]);     // аргументи масивом`,
      },
      {
        label: 'bind і часткове застосування',
        code: `const sayHiToAda = greet.bind(user, "Привіт");
sayHiToAda("!"); // "Привіт, Ada!" — this і перший аргумент зафіксовані

const btnHandler = this.onClick.bind(this); // класика React-класів`,
      },
    ],
    gotchas: [
      'Повторний bind не змінює вже привʼязаний this — «перший виграє».',
      'Викликати call/apply/bind на стрілковій функції можна (помилки не буде), але thisArg ігнорується: стрілка не має власного this і назавжди бере його з місця оголошення.',
      'Забіндена функція через new ігнорує привʼязаний this.',
      'bind створює НОВУ функцію — порівняння за посиланням ламається (важливо для removeEventListener).',
      'apply із дуже великим масивом аргументів може впертися в ліміт стека.',
    ],
  },
  {
    id: 'utility-types',
    title: 'Utility Types у TypeScript',
    description:
      'Utility Types — вбудовані «типи-помічники» TypeScript: беруть існуючий тип і роблять із нього новий, щоб не дублювати схожі форми вручну (потрібен «User без email» або «User, де все опціональне» — не копіюємо тип, а виводимо з одного). Головні: Partial<T> — усі поля необовʼязкові (патч-оновлення); Required<T> — усі обовʼязкові; Readonly<T> — лише читання; Pick<T, K> — взяти вказані поля; Omit<T, K> — усі, КРІМ вказаних (відрізати пароль); Record<K, V> — обʼєкт-словник; NonNullable<T> — прибрати null/undefined; Exclude/Extract — прибрати/лишити частини union; ReturnType<typeof fn> — тип результату функції; Parameters<typeof fn> — типи аргументів; InstanceType<T> — тип екземпляра класу; Awaited<T> — те, що всередині проміса. Суть: тип-джерело істини один, похідні виводяться автоматично й оновлюються разом із ним.',
    codeExamples: [
      {
        label: 'Найчастіші',
        code: `interface User { id: number; name: string; email: string; }

type Draft = Partial<User>;              // усі поля необовʼязкові
type Preview = Pick<User, "id" | "name">;// лише ці поля
type Safe = Omit<User, "email">;         // усі, крім email
type ById = Record<number, User>;        // словник id → User
type RO = Readonly<User>;                // тільки для читання`,
      },
      {
        label: 'Виведення з функцій',
        code: `function load() { return { ok: true, ts: Date.now() }; }
type Result = ReturnType<typeof load>;   // { ok: boolean; ts: number }
type P = Parameters<typeof load>;        // []
type Data = Awaited<Promise<string>>;    // string`,
      },
    ],
    gotchas: [
      'Omit структурний і не перевіряє існування ключа — описка в назві поля проходить мовчки.',
      'Partial і Readonly поверхневі — вкладені обʼєкти не зачіпаються (потрібен DeepPartial).',
      'Record<string, T> робить усі ключі «гарантованими», хоча реально їх може не бути — джерело undefined.',
      'Exclude/Extract працюють із юніонами, а не з полями обʼєктів — їх плутають з Omit/Pick.',
      'ReturnType<typeof fn> вимагає саме typeof для значення-функції, інакше помилка.',
    ],
  },
  {
    id: 'ts-access-modifiers',
    title: 'private, public, protected у класах TypeScript',
    description:
      'Модифікатори доступу контролюють видимість членів класу. public (за замовчуванням) — доступно звідусіль. private — лише всередині цього самого класу. protected — усередині класу й у його нащадках (підкласах), але не ззовні. Дуже важлива деталь: це перевірки рівня КОМПІЛЯЦІЇ TypeScript. Після компіляції в JS поле, позначене private, цілком доступне в рантаймі — тобто це інкапсуляція «за домовленістю», яку контролює лише типчекер. Якщо потрібна СПРАВЖНЯ приватність у рантаймі, треба використовувати ECMAScript-поля з #: вони недоступні ззовні навіть рефлексією. Зручний прийом TS — parameter properties: написавши private/public прямо в параметрі конструктора, ви автоматично оголошуєте й присвоюєте поле без зайвого коду.',
    codeExamples: [
      {
        label: 'Три модифікатори',
        code: `class Account {
  public owner: string;
  protected currency = "UAH";  // видно нащадкам
  private balance = 0;          // лише тут
  constructor(owner: string) { this.owner = owner; }
  public deposit(v: number) { this.balance += v; }
}
class Savings extends Account {
  showCurrency() { return this.currency; } // ✅ protected
  // this.balance — ❌ помилка компіляції
}`,
      },
      {
        label: 'TS private vs #private',
        code: `class A {
  private a = 1;   // TS-перевірка, у JS доступно через (obj as any).a
  #b = 2;          // справді приватне в рантаймі
}`,
      },
    ],
    gotchas: [
      'TS private — лише перевірка компілятора; у рантаймі поле доступне (на відміну від #).',
      'protected фактично частина контракту для підкласів — ускладнює рефакторинг.',
      'Можна звузити видимість при перевизначенні з public на більш закриту лише обмежено — стережіться помилок.',
      'private-поле з однаковою назвою в підкласі конфліктує — TS це контролює інакше, ніж #.',
      'public за замовчуванням — забутий модифікатор не робить поле приватним.',
    ],
  },
  {
    id: 'debounce-throttle',
    title: 'debounce (і throttle)',
    description:
      'Дві техніки обмеження частоти викликів функції на високочастотних подіях. debounce відкладає виклик доти, доки події не «вщухнуть» на заданий час, і виконує лише ОСТАННІЙ — ідеально для пошуку-як-друкуєш, автозбереження, валідації після паузи (реагуємо на фінальний стан). throttle гарантує виклик не частіше, ніж раз на інтервал, виконуючи функцію регулярно під час потоку подій — ідеально для scroll, mousemove, drag (потрібні проміжні оновлення з обмеженою частотою). Мнемоніка: debounce — «зачекай тишу й зроби раз», throttle — «роби, але не частіше ніж раз на N мс». Обидва зберігають продуктивність, відсікаючи дорогі обробники (запити, reflow, ререндери). Реалізуються через замикання й таймер; у проді зазвичай беруть lodash.',
    codeExamples: [
      {
        label: 'debounce',
        code: `function debounce(fn, delay) {
  let t;
  return (...args) => {
    clearTimeout(t);
    t = setTimeout(() => fn(...args), delay);
  };
}
// пошук: запит лише коли користувач перестав друкувати 300мс
input.addEventListener("input", debounce(search, 300));`,
      },
      {
        label: 'throttle',
        code: `function throttle(fn, limit) {
  let wait = false;
  return (...args) => {
    if (wait) return;
    fn(...args);
    wait = true;
    setTimeout(() => (wait = false), limit);
  };
}
// scroll: оновлюємо позицію максимум раз на 100мс
window.addEventListener("scroll", throttle(onScroll, 100));`,
      },
    ],
    gotchas: [
      'Плутанина: debounce — лише останній виклик; throttle — регулярні через інтервал.',
      'У React дебаунснуту функцію не можна створювати в тілі компонента — пересоздається щоренду й не дебаунсить (потрібен useMemo/useRef).',
      'Без cancel/cleanup відкладений виклик спрацює після анмаунту → помилка/витік.',
      'Втрата this/аргументів останнього виклику, якщо реалізувати недбало.',
      'Leading vs trailing edge змінює поведінку — треба обрати свідомо.',
    ],
  },
  {
    id: 'object-vs-map',
    title: 'Відмінність Object і Map',
    description:
      'Обидва зберігають пари ключ-значення, але призначені для різного. Map — спеціальна колекція: ключем може бути будь-що (рядок, число, навіть обʼєкт чи функція), зберігає порядок вставки, має властивість .size, напряму ітерується (for...of, forEach) і не «забруднений» прототипом. Object краще пасує для структур із відомими наперед полями (конфіги, DTO). Ключова відмінність ключів: у звичайного обʼєкта ключі завжди приводяться до рядка (obj[1] і obj["1"] — той самий ключ), а Map зберігає тип і обʼєкти-ключі за посиланням. Ще одна важлива річ: обʼєкт-словник вразливий до prototype pollution (ключі "__proto__", "constructor"), тому для словника користувацьких даних краще Map або Object.create(null). Object натомість лаконічніший і нативно серіалізується в JSON.',
    codeExamples: [
      {
        label: 'Ключі та розмір',
        code: `const obj = {};
obj[1] = "a"; obj["1"] = "b"; // ключі — рядки: "1" перетирає
Object.keys(obj); // ["1"]

const map = new Map();
map.set(1, "a").set("1", "b"); // різні ключі!
map.set({}, "obj-key");        // обʼєкт як ключ
map.size; // 3`,
      },
      {
        label: 'Ітерація',
        code: `for (const [k, v] of map) console.log(k, v); // прямо ітерується
map.forEach((v, k) => {});
// обʼєкт: Object.entries(obj).forEach(...)`,
      },
      {
        label: 'Основні методи (люблять питати списком)',
        code: `// Object
Object.keys(obj);    // масив ключів
Object.values(obj);  // масив значень
Object.entries(obj); // масив пар [ключ, значення]
Object.assign(target, source); // злиття обʼєктів

// Map
map.set(k, v); map.get(k); map.has(k);
map.delete(k); map.clear(); map.size;

// Set
set.add(v); set.has(v);
set.delete(v); set.clear(); set.size;`,
      },
    ],
    gotchas: [
      'Ключі об’єкта завжди приводяться до рядка (obj[1] === obj["1"]); Map зберігає тип ключа.',
      'Об’єкт-словник вразливий до prototype pollution (__proto__, constructor) — беріть Object.create(null) або Map.',
      'Map не серіалізується JSON.stringify напряму — потрібна конвертація.',
      'Object.keys/values створюють масиви; Map ітерується ліниво й має .size без обходу.',
      'Map як ключ використовує посилання — однакові за вмістом обʼєкти це різні ключі.',
    ],
  },
  {
    id: 'weak-collections',
    title: 'WeakMap, WeakSet, WeakRef',
    description:
      'Звичайний Map «міцно тримає» ключі: поки обʼєкт лежить ключем, збирач сміття (GC) не може його забрати, навіть якщо він більше ніде не потрібен — це витік памʼяті. WeakMap і WeakSet тримають ключі «слабко»: щойно на обʼєкт-ключ не лишилося інших посилань, GC забирає його разом із записом у колекції. Застосування: кеш, приватні дані, метадані, привʼязані до чужого обʼєкта — зникає обʼєкт, автоматично зникають і дані. Ціна: ключі — лише обʼєкти (не примітиви), і колекцію не можна перебрати (немає for...of, .size, .clear), бо вміст залежить від непередбачуваного моменту роботи GC. WeakRef — слабке посилання на один обʼєкт: .deref() поверне обʼєкт або undefined, якщо його вже зібрали.',
    codeExamples: [
      {
        label: 'WeakMap для приватних/кеш-даних',
        code: `const privateData = new WeakMap();
class User {
  constructor(name) { privateData.set(this, { name }); }
  getName() { return privateData.get(this).name; }
}
// коли екземпляр User зникне — запис у WeakMap прибере GC автоматично`,
      },
      {
        label: 'WeakRef',
        code: `let big = { data: new Array(1e6) };
const ref = new WeakRef(big);
big = null;            // тепер обʼєкт може бути зібраний
const obj = ref.deref(); // або сам обʼєкт, або undefined (якщо вже зібрано)`,
      },
    ],
    gotchas: [
      'Ключі WeakMap/WeakSet — тільки обʼєкти, примітиви заборонені.',
      'Не ітеруються, немає .size/.clear — це принципове обмеження через недетермінізм GC.',
      'WeakRef.deref() може повернути undefined будь-коли — не покладайтеся на наявність.',
      'Момент збирання сміття недетермінований — не будуйте на WeakRef/FinalizationRegistry критичну логіку.',
      'FinalizationRegistry-колбек може взагалі не викликатися — не для звільнення ресурсів.',
    ],
  },
  {
    id: 'garbage-collector',
    title: 'Garbage collector — як JS звільняє памʼять',
    description:
      'Garbage collector (збирач сміття, GC) — механізм движка, що автоматично звільняє памʼять від обʼєктів, які стали недосяжними. Вручну видалити обʼєкт із памʼяті в JS не можна — GC сам вирішує, коли його забрати. Головне поняття — досяжність (reachability): обʼєкт живий, поки до нього можна дійти по ланцюжку посилань від «коренів» (глобальний обʼєкт, змінні поточних функцій, замикання). Алгоритм називається mark-and-sweep: GC іде від коренів, помічає все досяжне, а непомічене видаляє. Циклічні посилання (a посилається на b, b на a) — не проблема: якщо до цієї пари не дійти від коренів, обидва зникнуть. Витік памʼяті — це коли непотрібний обʼєкт ЛИШАЄТЬСЯ досяжним: забута підписка чи таймер, глобальні змінні, великі дані в замиканнях. Коротка відповідь для співбесіди: «GC звільняє памʼять недосяжних обʼєктів за mark-and-sweep; витік — це коли зайвий обʼєкт випадково лишається досяжним».',
    codeExamples: [
      {
        label: 'Досяжність вирішує все',
        code: `let user = { name: "Ada" }; // досяжний через user
user = null;                 // посилань нема → GC забере

let a = {};
const b = { ref: a };
a = null;
// обʼєкт {} ще ЖИВИЙ — досяжний через b.ref`,
      },
      {
        label: 'Типові витоки памʼяті',
        code: `// ❌ таймер тримає замикання з даними вічно
const bigData = new Array(1e6);
setInterval(() => console.log(bigData.length), 1000);

// ❌ забута підписка: компонент зник, обробник — ні
window.addEventListener("resize", heavyHandler);

// ✅ прибирати за собою
clearInterval(id);
window.removeEventListener("resize", heavyHandler);`,
      },
    ],
    gotchas: [
      'Циклічні посилання НЕ спричиняють витік: GC дивиться на досяжність від коренів, а не рахує кількість посилань.',
      'delete obj.prop не звільняє памʼять — лише прибирає властивість; памʼять звільняє GC, коли обʼєкт стає недосяжним.',
      'Момент запуску GC непередбачуваний, і викликати його вручну зі звичайного коду не можна.',
      'Найчастіші витоки на фронтенді: забуті підписки й таймери, великі дані в замиканнях, відірвані DOM-вузли в масивах/Map.',
      'WeakMap/WeakSet не заважають GC забрати обʼєкт-ключ — тому саме вони підходять для кешів і метаданих.',
      'Движки оптимізують GC: збірка за поколіннями (нові обʼєкти перевіряють частіше за старі), інкрементальна (частинами) та у вільний час процесора (idle-time).',
    ],
  },
  {
    id: 'web-storage',
    title: 'Cookies, localStorage, sessionStorage',
    description:
      'Три способи зберігати дані в браузері — на співбесіді питають різницю. localStorage — ~5–10 МБ, живе, поки його не видалять, спільний для всіх вкладок сайту. sessionStorage — те саме API, але живе, лише поки відкрита вкладка (перезавантаження переживає, закриття — ні), і між вкладками не ділиться. Cookies — маленькі (~4 КБ), мають термін життя (max-age/expires), і ГОЛОВНА відмінність: автоматично відправляються на сервер із КОЖНИМ HTTP-запитом — тому саме в них тримають сесійні токени. Важливі атрибути cookie: httpOnly (недоступна з JS — захист від XSS), secure (лише HTTPS), sameSite (захист від CSRF). API сховищ: setItem/getItem/removeItem/clear; зберігаються лише рядки (обʼєкти — через JSON.stringify). Коротка відповідь: localStorage — довгострокові дані на клієнті, sessionStorage — тимчасові дані однієї вкладки, cookie — те, що має бачити сервер.',
    codeExamples: [
      {
        label: 'localStorage / sessionStorage',
        code: `localStorage.setItem("user", JSON.stringify({ name: "Ada" }));
const user = JSON.parse(localStorage.getItem("user"));
localStorage.removeItem("user");
localStorage.clear();

// sessionStorage — те саме API, але живе до закриття вкладки
sessionStorage.setItem("step", "2");`,
      },
      {
        label: 'Cookies',
        code: `// встановити (max-age у секундах)
document.cookie = "token=abc; path=/; max-age=3600; secure; samesite=strict";

// прочитати — всі cookie одним рядком
document.cookie; // "token=abc; theme=dark"

// видалити — виставити дату в минулому
document.cookie = "token=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT";`,
      },
    ],
    gotchas: [
      'Cookie летять на сервер із КОЖНИМ запитом — великі дані в них уповільнюють усі запити.',
      'httpOnly-cookie не читається з JavaScript — саме так захищають сесійні токени від XSS.',
      'Сховища зберігають лише рядки: обʼєкт без JSON.stringify стане "[object Object]".',
      'sessionStorage не ділиться між вкладками; localStorage ділиться, а подія storage дозволяє синхронізувати вкладки.',
      'У приватному режимі або при переповненні setItem може кинути помилку — обгортайте в try/catch.',
    ],
  },
  {
    id: 'proxy-reflect',
    title: 'Proxy і Reflect',
    description:
      'Proxy — обʼєкт-обгортка навколо іншого обʼєкта, яка перехоплює операції з ним (читання/запис властивостей, delete, in, виклик функції) і може обробити їх по-своєму або прозоро передати оригіналу. Перехоплювачі називаються «пастками» (traps): get, set, has, deleteProperty та інші. На відміну від геттерів/сеттерів, Proxy працює для ВСІХ властивостей одразу — навіть тих, що ще не існують. Reflect — вбудований обʼєкт із методами, що дзеркалять кожну пастку (Reflect.get, Reflect.set…); усередині пастки правильно викликати саме їх, щоб коректно передати операцію оригіналу. Застосування: валідація при записі, значення за замовчуванням, логування доступу, а головне — реактивність фреймворків (Vue 3 і MobX побудовані на Proxy). Коротка відповідь: «Proxy перехоплює операції над обʼєктом через пастки; Reflect — стандартний спосіб передати операцію далі оригіналу».',
    codeExamples: [
      {
        label: 'get — значення за замовчуванням',
        code: `let numbers = [0, 1, 2];
numbers = new Proxy(numbers, {
  get(target, prop) {
    return prop in target ? target[prop] : 0; // дефолт замість undefined
  },
});
numbers[1];   // 1
numbers[123]; // 0`,
      },
      {
        label: 'set — валідація (+ Reflect)',
        code: `const user = new Proxy({}, {
  set(target, prop, value, receiver) {
    if (prop === "age" && value < 0) throw new TypeError("age >= 0");
    return Reflect.set(target, prop, value, receiver); // передаємо оригіналу
  },
});
user.age = 30;   // ✅
// user.age = -1; // ❌ TypeError`,
      },
    ],
    gotchas: [
      'Усередині пасток використовуйте Reflect.get/set, а не target[prop] напряму — інакше зламаються геттери, залежні від this (receiver).',
      'Пастка set має повернути true при успіху — інакше в strict mode буде TypeError.',
      'Proxy не можна «зняти» з обʼєкта — обгорнуту версію треба використовувати замість оригіналу скрізь.',
      'Реактивність Vue 3 та MobX побудована саме на Proxy — улюблене уточнювальне питання.',
      'Proxy повільніший за прямий доступ — не обгортайте «гарячі» структури без потреби.',
    ],
  },
  {
    id: 'big-o',
    title: 'Складність алгоритмів (Big O)',
    description:
      'Big O описує, як росте час роботи (чи памʼять) алгоритму зі збільшенням обсягу даних n; константи відкидаються — важлива лише швидкість росту. Від швидкого до повільного: O(1) — константна: час не залежить від n (доступ за індексом, map.get). O(log n) — логарифмічна: щокроку задача зменшується вдвічі (бінарний пошук у відсортованому масиві). O(n) — лінійна: один прохід по даних (map, filter, find, includes). O(n log n) — ефективні сортування (merge sort; так працює Array.prototype.sort). O(n²) — квадратична: цикл у циклі, «кожен із кожним» (бульбашкове сортування). O(n!) — факторіальна: перебір усіх перестановок. Практичний кейс, який люблять на співбесіді: пошук у масиві через includes усередині циклу — це O(n²), а заміна масиву на Set робить перевірку O(1) і весь код O(n).',
    codeExamples: [
      {
        label: 'Типові складності в JS',
        code: `arr[5];          // O(1) — доступ за індексом
map.get(key);    // O(1) — у середньому
arr.includes(x); // O(n) — прохід по масиву
arr.sort(cmp);   // O(n log n)

// O(n²) — цикл у циклі
for (const a of arr)
  for (const b of arr) compare(a, b);`,
      },
      {
        label: 'Класична оптимізація O(n²) → O(n)',
        code: `// ❌ O(n²): includes (O(n)) всередині filter (O(n))
const dups = arr.filter(x => other.includes(x));

// ✅ O(n): Set дає перевірку за O(1)
const set = new Set(other);
const dups2 = arr.filter(x => set.has(x));`,
      },
    ],
    gotchas: [
      'Big O — про швидкість росту, а не реальний час: O(1) з важкою константою може програвати O(n) на малих даних.',
      'includes/indexOf/find — O(n); викликані в циклі вони дають O(n²) — найчастіша причина «повільного» коду.',
      'Set.has і Map.get — O(1) у середньому; заміна масиву на Set/Map — типова оптимізація на лайвкодингу.',
      '[...acc, x] або concat усередині reduce створюють новий масив щоітерації → O(n²); push — O(1).',
      'Питають і про памʼять (space complexity): сортування «на місці» — O(1) додаткової памʼяті, копія масиву — O(n).',
    ],
  },
]

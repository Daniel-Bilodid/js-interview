import type { Topic } from '../types'

/*
 * SQL: зʼєднання таблиць, агрегації, індекси, оптимізація запитів
 * і транзакції. Розгорнутий формат (definition / why / simple / ...).
 */

export const sqlTopics: Topic[] = [
  {
    id: 'sql-joins',
    title: 'Як зʼєднати дві таблиці і чим відрізняються типи JOIN?',
    definition:
      'JOIN зʼєднує рядки двох таблиць за умовою ON і віддає їх як один рядок результату. INNER JOIN лишає тільки ті пари, де умова справдилась з обох боків; LEFT JOIN лишає всі рядки лівої таблиці й підставляє NULL там, де пари не знайшлось; RIGHT JOIN робить те саме дзеркально; FULL OUTER JOIN зберігає непарні рядки з обох боків; CROSS JOIN дає декартів добуток без жодної умови. Окремо стоїть SELF JOIN — та сама таблиця, приєднана сама до себе під іншим псевдонімом, типово для ієрархій на кшталт працівник і його керівник.',
    why: 'Реляційна база навмисно розкладає дані по різних таблицях, щоб не дублювати їх: замовлення тримає не імʼя клієнта, а лише його id. JOIN — спосіб зібрати розкидане назад в одну відповідь силами бази, замість того щоб робити окремий запит на кожен id у коді застосунку.',
    simple:
      'Уяви дві коробки карток: у першій картки клієнтів, у другій картки замовлень, і на кожному замовленні написано номер клієнта. JOIN — це інструкція, як розкласти їх парами на столі. INNER JOIN кладе на стіл тільки повні пари: клієнт без замовлень і замовлення з неіснуючим клієнтом просто не потрапляють у результат. LEFT JOIN каже: усі картки клієнтів обовʼязково лягають на стіл, і якщо замовлення для когось немає, поруч кладеться порожня картка, тобто NULL. Саме тому LEFT JOIN — стандартний спосіб відповісти на питання хто НЕ має замовлень: робиш LEFT JOIN і фільтруєш WHERE orders.id IS NULL. Найчастіша помилка новачка — написати LEFT JOIN, а потім умову про праву таблицю покласти у WHERE: NULL не пройде жодну умову, і запит мовчки перетвориться на INNER JOIN. Умова про праву таблицю в LEFT JOIN має жити в ON, а не у WHERE. І ще одне: JOIN множить рядки. Якщо в клієнта три замовлення, він зʼявиться в результаті тричі, і будь-який SUM по полю клієнта порахує його суму три рази.',
    related: [
      {
        title: 'ON проти WHERE у зовнішніх зʼєднаннях',
        text: 'Умова в ON застосовується ДО того, як формується пара, тому LEFT JOIN усе одно зберігає непарні рядки лівої таблиці. Умова у WHERE застосовується ПІСЛЯ зʼєднання, до вже готового результату, і відкидає рядки з NULL — так LEFT JOIN непомітно вироджується в INNER JOIN. Для INNER JOIN різниці немає, тому звичка писати все у WHERE довго не дає про себе знати.',
      },
      {
        title: 'Розмноження рядків і дублі в агрегатах',
        text: 'Зʼєднання один-до-багатьох повертає стільки рядків, скільки є пар, тому після JOIN до двох різних дочірніх таблиць кількість рядків множиться, а SUM і COUNT дають завищені числа. Лікується або агрегацією в підзапиті до зʼєднання, або COUNT(DISTINCT id), який приховує симптом, але не причину.',
      },
      {
        title: 'Як база фізично виконує JOIN',
        text: 'Планувальник обирає один із трьох алгоритмів: Nested Loop — для кожного рядка зовнішньої таблиці шукати збіги в індексі внутрішньої, вигідний на малих обсягах; Hash Join — побудувати хеш-таблицю по меншому набору й пройти більший один раз; Merge Join — відсортувати обидва боки й іти по них паралельно. Тип JOIN у тексті запиту і алгоритм виконання — різні речі.',
      },
    ],
    codeExamples: [
      {
        label: 'INNER проти LEFT і пошук клієнтів без замовлень',
        code: `-- тільки клієнти, у яких Є замовлення
SELECT u.id, u.name, o.total
FROM users u
INNER JOIN orders o ON o.user_id = u.id;

-- усі клієнти; у кого немає замовлень — NULL у полях o
SELECT u.id, u.name, o.total
FROM users u
LEFT JOIN orders o ON o.user_id = u.id;

-- клієнти БЕЗ жодного замовлення (anti-join)
SELECT u.id, u.name
FROM users u
LEFT JOIN orders o ON o.user_id = u.id
WHERE o.id IS NULL;`,
      },
      {
        label: 'Пастка: умова правої таблиці у WHERE',
        code: `-- ❌ LEFT JOIN, який насправді працює як INNER JOIN
SELECT u.name, o.total
FROM users u
LEFT JOIN orders o ON o.user_id = u.id
WHERE o.status = 'paid';   -- NULL не проходить умову → клієнти без замовлень зникли

-- ✅ умова про праву таблицю живе в ON
SELECT u.name, o.total
FROM users u
LEFT JOIN orders o ON o.user_id = u.id AND o.status = 'paid';`,
      },
    ],
    seniorNotes: [
      'RIGHT JOIN у продакшн-коді майже не зустрічається: його завжди можна переписати як LEFT JOIN, помінявши таблиці місцями, і читати такий запит легше. FULL OUTER JOIN не підтримується в MySQL — там його емулюють через UNION двох зовнішніх зʼєднань.',
      'USING(user_id) — коротший запис ON a.user_id = b.user_id, який ще й лишає в результаті один спільний стовпець замість двох. Працює лише коли поля в обох таблицях названі однаково.',
      'Зʼєднання за полями різних типів або з різним collation вимикає використання індексу: база змушена приводити тип у кожному рядку. Типовий симптом — варчарний user_id в одній таблиці й bigint в іншій.',
      'CROSS JOIN на двох таблицях по мільйону рядків дає трильйон пар, тому випадково пропущена умова ON у старому синтаксисі через кому — класична причина запиту, що ніколи не завершиться.',
      'Порядок таблиць у тексті запиту не визначає порядок виконання: планувальник сам вирішує, яку таблицю зробити зовнішньою, спираючись на статистику. Побачити реальний порядок можна лише через EXPLAIN.',
    ],
  },

  {
    id: 'sql-union',
    title: 'Чим UNION відрізняється від UNION ALL і коли брати кожен?',
    definition:
      'UNION складає результати двох SELECT один під одним у вертикальний список і прибирає повні дублікати рядків. UNION ALL робить те саме, але дублікати лишає і нічого не порівнює. Обидва вимагають однакової кількості стовпців і сумісних типів у відповідних позиціях; імена стовпців беруться з першого SELECT. Різниця в ціні принципова: UNION мусить відсортувати або захешувати весь результат, щоб знайти дублікати, тому UNION ALL завжди дешевший і його беруть за замовчуванням, якщо дублікати неможливі або не заважають.',
    why: 'JOIN зʼєднує таблиці по горизонталі, додаючи стовпці, а часто потрібно навпаки: зібрати однорідні записи з кількох джерел в один список. Наприклад, показати одну стрічку подій із таблиць коментарів, лайків і підписок, або обʼєднати поточні замовлення з архівними, що лежать в окремій таблиці.',
    simple:
      'Уяви два списки на аркушах: у першому учасники з Києва, у другому з Львова. UNION ALL — це просто скласти аркуші один під одним і читати підряд. UNION — скласти і додатково викреслити рядки, що повністю повторюються. Важливо, що UNION порівнює рядок цілком, а не одне поле: якщо в двох рядках збігається імʼя, але різні id, обидва лишаться. Через це UNION часто не робить того, чого від нього очікують, і замість нього потрібен DISTINCT ON або GROUP BY по конкретному ключу. Ціна теж різна: щоб знайти дублікати, базі треба зібрати весь результат у памʼяті й відсортувати його, тому на мільйонах рядків UNION може бути в рази повільнішим за UNION ALL. ORDER BY і LIMIT у такій конструкції ставляться один раз у самому кінці і стосуються всього обʼєднаного результату; щоб відсортувати окремий SELECT, його треба загорнути в дужки. Практичне правило: пиши UNION ALL, і переходь на UNION лише тоді, коли реально можеш отримати однакові рядки з обох джерел.',
    related: [
      {
        title: 'UNION проти JOIN',
        text: 'Це відповіді на різні питання. JOIN відповідає на питання що ще відомо про цей запис і додає стовпці праворуч. UNION відповідає на питання які ще є такі самі записи і додає рядки знизу. Плутанина виникає, коли дані розкладені по однакових таблицях за періодами або шардами: там потрібен саме UNION ALL, а не JOIN.',
      },
      {
        title: 'INTERSECT і EXCEPT',
        text: 'Ті самі операції над множинами рядків: INTERSECT лишає рядки, присутні в обох результатах, EXCEPT лишає ті, що є в першому і відсутні в другому. Обидва, як і UNION, за замовчуванням прибирають дублікати і мають варіант ALL. У MySQL зʼявились лише у версії 8.0.31, тому там ту саму логіку зазвичай пишуть через JOIN або NOT EXISTS.',
      },
      {
        title: 'Типи стовпців і NULL-заглушки',
        text: 'Кількість і порядок стовпців мають збігатися точно, тому для обʼєднання різнорідних сутностей у спільну стрічку бракуючі поля дописують як NULL або порожній рядок із явним приведенням типу. Без явного CAST база сама виводить спільний тип, і тоді, наприклад, числовий стовпець може мовчки стати текстовим.',
      },
    ],
    codeExamples: [
      {
        label: 'Спільна стрічка подій через UNION ALL',
        code: `SELECT 'comment' AS kind, c.id, c.user_id, c.created_at
FROM comments c
WHERE c.user_id = $1

UNION ALL

SELECT 'like' AS kind, l.id, l.user_id, l.created_at
FROM likes l
WHERE l.user_id = $1

-- ORDER BY і LIMIT стосуються всього обʼєднаного результату
ORDER BY created_at DESC
LIMIT 20;`,
      },
      {
        label: 'Коли UNION не робить очікуваного',
        code: `-- ❌ здається, що прибере повтори людей — насправді порівнює рядок ЦІЛКОМ
SELECT id, name FROM kyiv_users
UNION
SELECT id, name FROM lviv_users;   -- один id з різним name лишиться двічі

-- ✅ дедуплікація за конкретним ключем
SELECT DISTINCT ON (id) id, name
FROM (
  SELECT id, name FROM kyiv_users
  UNION ALL
  SELECT id, name FROM lviv_users
) t
ORDER BY id;`,
      },
    ],
    seniorNotes: [
      'UNION прибирає дублікати сортуванням або хешуванням усього результату, тому на великих обсягах він може вилізти за work_mem і піти на диск. У плані PostgreSQL це видно як HashAggregate чи Sort над Append, тоді як UNION ALL дає чистий Append.',
      'ORDER BY всередині окремого SELECT у складі UNION стандартом не дозволений без дужок і підзапиту, бо порядок гілок не гарантується. Сортувати треба фінальний результат; покладатися на те, що рядки першої гілки підуть першими, не можна.',
      'LIMIT у зовнішньому запиті не заважає базі повністю виконати обидві гілки UNION, тому пагінація по обʼєднаній стрічці на великих таблицях вимагає LIMIT усередині кожної гілки окремо.',
      'У MySQL до версії 8.0.31 немає INTERSECT і EXCEPT, а FULL OUTER JOIN відсутній узагалі — його класична емуляція це LEFT JOIN UNION RIGHT JOIN. Це часта перевірка на знання конкретної СУБД, а не абстрактного SQL.',
      'Імена стовпців у результаті беруться лише з першого SELECT, тому псевдоніми в наступних гілках впливають лише на читабельність коду і мовчки ігноруються в назвах полів відповіді.',
    ],
  },

  {
    id: 'sql-where-having',
    title: 'Чим HAVING відрізняється від WHERE і коли потрібен саме HAVING?',
    definition:
      'WHERE фільтрує окремі рядки до групування, HAVING фільтрує вже сформовані групи після нього. Через це у WHERE не можна використати агрегатну функцію на кшталт COUNT чи SUM: у момент його роботи груп ще не існує. Логічний порядок виконання запиту такий: FROM і JOIN, потім WHERE, потім GROUP BY, потім HAVING, потім SELECT із його псевдонімами, потім ORDER BY і LIMIT. Саме тому псевдонім зі SELECT недоступний у WHERE, а ORDER BY ним уже користуватись може.',
    why: 'Відбір даних потрібен на двох різних рівнях: викинути непотрібні записи ще до підрахунку і викинути цілі групи вже за результатом підрахунку. Питання знайти клієнтів, що зробили більше десяти замовлень, неможливо виразити через WHERE, бо кількість існує тільки після групування.',
    simple:
      'Уяви, що ти рахуєш голоси на виборах по округах. WHERE — це правило, які бюлетені взагалі брати до рук: зіпсовані відкладаємо ще до підрахунку. GROUP BY — розкладання решти по стосах за округами. HAVING — правило, які стоси залишити в підсумковій таблиці: наприклад, тільки ті, де понад тисяча бюлетенів. Порядок має прямий наслідок для продуктивності: усе, що можна відкинути у WHERE, треба відкидати саме там, бо ці рядки навіть не братимуть участі в групуванні. Фільтр, перенесений у HAVING без потреби, змушує базу спершу порахувати групи, які потім однаково викинуться. Друга практична деталь: у SELECT із GROUP BY можна брати лише поля, за якими групуєш, або агрегати від інших полів. PostgreSQL за це лається помилкою column must appear in the GROUP BY clause, а MySQL у неповному режимі ONLY_FULL_GROUP_BY мовчки поверне випадкове значення з групи, і це джерело важковловимих багів. Третя деталь: COUNT(*) рахує всі рядки групи, а COUNT(поле) пропускає ті, де поле дорівнює NULL.',
    related: [
      {
        title: 'Логічний порядок виконання запиту',
        text: 'Речення виконуються не в порядку написання: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT. Це пояснює одразу три типові помилки: псевдонім зі SELECT не видно у WHERE і в GROUP BY, агрегат не видно у WHERE, але видно в HAVING і ORDER BY. Фізичний план виконання при цьому може відрізнятись — планувальник вільний переставляти операції, доки результат той самий.',
      },
      {
        title: 'Віконні функції як альтернатива',
        text: 'Коли треба і деталі рядків, і агрегат по групі одночасно, GROUP BY не підходить, бо згортає рядки. Віконна функція на кшталт COUNT(*) OVER (PARTITION BY user_id) рахує те саме, але лишає всі рядки на місці. Фільтрувати за її результатом у WHERE не можна з тієї самої причини, що й за агрегатом, тому запит загортають у підзапит або CTE.',
      },
      {
        title: 'FILTER і умовна агрегація',
        text: 'Щоб порахувати в одному запиті кілька різних підсумків по одній групі, беруть FILTER (WHERE умова) у PostgreSQL або COUNT(CASE WHEN умова THEN 1 END) у будь-якій СУБД. Це замінює кілька окремих запитів або кілька JOIN до тієї самої таблиці з різними умовами.',
      },
    ],
    codeExamples: [
      {
        label: 'WHERE до групування, HAVING після',
        code: `SELECT u.id, u.name, COUNT(o.id) AS orders_count, SUM(o.total) AS revenue
FROM users u
JOIN orders o ON o.user_id = u.id
WHERE o.created_at >= '2026-01-01'   -- фільтр РЯДКІВ, до групування
GROUP BY u.id, u.name
HAVING COUNT(o.id) > 10              -- фільтр ГРУП, після групування
ORDER BY revenue DESC
LIMIT 50;`,
      },
      {
        label: 'Типові помилки з агрегатами',
        code: `-- ❌ агрегат у WHERE: груп ще не існує
SELECT user_id FROM orders
WHERE COUNT(*) > 10 GROUP BY user_id;

-- ❌ фільтр рядків у HAVING: групи рахуються дарма
SELECT user_id, COUNT(*) FROM orders
GROUP BY user_id HAVING status = 'paid';

-- ✅ кожне на своєму місці + умовні підсумки одним проходом
SELECT user_id,
       COUNT(*) FILTER (WHERE status = 'paid')     AS paid,
       COUNT(*) FILTER (WHERE status = 'canceled') AS canceled
FROM orders
GROUP BY user_id
HAVING COUNT(*) > 10;`,
      },
    ],
    seniorNotes: [
      'HAVING без GROUP BY законний: запит трактується як одна група на всю таблицю, тому HAVING COUNT(*) > 0 повертає або один рядок, або жодного. Це рідкісний, але валідний прийом для перевірки умови на всьому наборі.',
      'MySQL історично дозволяє посилатись у HAVING на псевдоніми зі SELECT, а PostgreSQL за стандартом цього не дозволяє і вимагає повторити вираз. Той самий запит може працювати в одній СУБД і падати в іншій.',
      'COUNT(*) і COUNT(поле) дають різні числа, якщо поле буває NULL, а COUNT(DISTINCT поле) додатково вимагає сортування або хешування і коштує помітно дорожче за звичайний COUNT.',
      'У MySQL без режиму ONLY_FULL_GROUP_BY можна вибрати поле, якого немає в GROUP BY, і база поверне довільне значення з групи без помилки. У версії 8 цей режим увімкнений за замовчуванням, і старі запити після оновлення падають.',
      'Фільтр по агрегату не можна прискорити індексом, тому єдиний реальний спосіб пришвидшити важкий GROUP BY на великих обсягах це або звузити набір у WHERE індексованою умовою, або тримати лічильники в окремій таблиці чи матеріалізованому представленні.',
    ],
  },

  {
    id: 'sql-indexes',
    title: 'Що таке індекс, як він працює і які в нього недоліки?',
    definition:
      'Індекс — це окрема структура даних, зазвичай B-tree, яка зберігає значення стовпця у відсортованому вигляді разом із посиланням на фізичне розташування рядка. Завдяки сортуванню база знаходить потрібне значення за логарифм від кількості рядків замість повного сканування таблиці. Плата за це — місце на диску і сповільнення запису: кожен INSERT, UPDATE і DELETE мусить оновити ще й усі індекси таблиці. Крім звичайного B-tree існують хеш-індекси для строгої рівності, GIN для масивів і повнотекстового пошуку, GiST для геометрії, а також складені, часткові й унікальні індекси.',
    why: 'Без індексу єдиний спосіб знайти рядок за значенням поля — прочитати всю таблицю до кінця, бо база не має жодного уявлення, де саме лежить потрібне значення. На таблиці в десять мільйонів рядків це десятки секунд і повне навантаження на диск для кожного запиту.',
    simple:
      'Індекс у базі влаштований як алфавітний покажчик у кінці книжки: там відсортовані слова і номери сторінок, де вони зустрічаються. Щоб знайти слово, ти не гортаєш усю книжку, а робиш кілька кроків по відсортованому списку. B-tree — це дерево, кожен вузол якого звужує діапазон пошуку, тому навіть у мільярді рядків потрібне значення знаходиться за пару десятків читань. Сортування дає й побічну вигоду: індекс прискорює не лише пошук за рівністю, а й діапазони, ORDER BY і MIN з MAX. Але покажчик треба підтримувати: щоразу, коли в книжку дописують сторінку, покажчик треба переписати — тому таблиця з десятьма індексами пише в рази повільніше, ніж без них. Індекс марний, якщо запит однаково зачіпає більшу частину таблиці: перестрибувати туди-сюди по диску дорожче, ніж прочитати все підряд, і планувальник свідомо обирає повне сканування. Важлива деталь про складені індекси: індекс по парі полів працює як телефонний довідник, відсортований за прізвищем, а потім за імʼям. Шукати за самим лише імʼям по ньому неможливо — це правило лівого префікса. І ще: будь-яка функція чи приведення типу над стовпцем у WHERE вимикає індекс, бо індекс зберігає значення стовпця, а не результат функції від нього.',
    related: [
      {
        title: 'Кластерний індекс і heap-таблиця',
        text: 'У MySQL InnoDB рядки фізично зберігаються всередині B-tree первинного ключа, тому вторинний індекс містить не адресу рядка, а значення первинного ключа і вимагає другого пошуку. У PostgreSQL таблиця це неупорядкована купа, а всі індекси вторинні й посилаються на фізичний tid. Звідси різні поради: в InnoDB важливий короткий і зростаючий первинний ключ, бо його копія лежить у кожному індексі.',
      },
      {
        title: 'Покривний індекс і index-only scan',
        text: 'Якщо всі стовпці, потрібні запиту, є в самому індексі, база взагалі не читає таблицю — це index-only scan, найшвидший сценарій. Додаткові стовпці можна покласти в індекс через INCLUDE, не роблячи їх частиною ключа сортування. У PostgreSQL для цього ще потрібна свіжа visibility map, тобто регулярний VACUUM.',
      },
      {
        title: 'Часткові й функціональні індекси',
        text: 'Частковий індекс будується лише по рядках, що задовольняють умову, наприклад WHERE deleted_at IS NULL, і тому в рази менший за повний. Функціональний індекс будується по виразу, наприклад по lower(email), і саме він рятує запити, де на стовпець накладається функція, яка інакше вимкнула б звичайний індекс.',
      },
      {
        title: 'Селективність і статистика',
        text: 'Планувальник обирає індекс за оцінкою кількості рядків, спираючись на зібрану статистику розподілу значень. Індекс по полю з двома значеннями, наприклад по булевому прапорцю, майже завжди марний, бо половина таблиці однаково буде прочитана. Застаріла статистика після масового імпорту — часта причина раптово повільного запиту без жодних змін у коді.',
      },
    ],
    codeExamples: [
      {
        label: 'Складений індекс і правило лівого префікса',
        code: `CREATE INDEX idx_orders_user_created
  ON orders (user_id, created_at DESC);

-- ✅ працює: перший стовпець індексу присутній
WHERE user_id = 42 ORDER BY created_at DESC
-- ✅ працює: рівність по першому + діапазон по другому
WHERE user_id = 42 AND created_at >= '2026-01-01'
-- ❌ не працює: пропущений лівий префікс user_id
WHERE created_at >= '2026-01-01'`,
      },
      {
        label: 'Що вимикає індекс',
        code: `-- ❌ функція над стовпцем — індекс по email не використається
WHERE lower(email) = 'a@b.com';
-- ✅ або функціональний індекс:
CREATE INDEX idx_users_email_lower ON users (lower(email));

-- ❌ пошук за суфіксом: B-tree відсортований зліва направо
WHERE name LIKE '%son';
-- ✅ префікс індексується нормально
WHERE name LIKE 'son%';

-- ❌ приведення типу в кожному рядку
WHERE user_id::text = '42';`,
      },
    ],
    seniorNotes: [
      'Індекс по (a, b) робить окремий індекс по a зайвим, а от окремий індекс по b він не замінює. Це основний спосіб прибрати дублювальні індекси: у PostgreSQL їх видно через pg_stat_user_indexes з нульовим idx_scan.',
      'CREATE INDEX блокує запис у таблицю на весь час побудови; на живому продакшні потрібен CREATE INDEX CONCURRENTLY у PostgreSQL або ALTER TABLE ... ALGORITHM=INPLACE у MySQL. Concurrently-побудова може впасти й лишити невалідний індекс, який доведеться видалити вручну.',
      'У PostgreSQL кожен UPDATE створює нову версію рядка і мусить дописати запис у кожен індекс таблиці, окрім випадку HOT-update, коли жодне індексоване поле не змінилось і в сторінці є вільне місце. Тому зайві індекси дорожчають не лише вставку, а й звичайні оновлення.',
      'Індекс не використовується для умови з OR по різних стовпцях так само ефективно, як для AND: часто швидше переписати запит через UNION ALL двох гілок, кожна з яких бере власний індекс.',
      'NULL у B-tree зберігаються, тому IS NULL може користуватись індексом, а от у MySQL унікальний індекс дозволяє кілька NULL, бо NULL не дорівнює NULL — часта причина дублів там, де очікувалась унікальність.',
    ],
  },

  {
    id: 'sql-slow-query',
    title: 'Запит на великій таблиці виконується повільно. Як ти його оптимізуєш?',
    definition:
      'Починаю не з правок запиту, а з EXPLAIN ANALYZE: він показує реальний план, фактичний час кожного вузла і розбіжність між очікуваною й реальною кількістю рядків. Далі шукаю типові причини за спаданням частоти: відсутній або невикористаний індекс через функцію над стовпцем, вибірка зайвих стовпців і рядків, пагінація через великий OFFSET, підзапит, що виконується для кожного рядка, і застаріла статистика. Виправлення йдуть у тому ж порядку: спершу звузити обсяг даних і додати правильний індекс, потім переписати запит, і лише в останню чергу міняти схему — денормалізація, партиціонування, матеріалізовані представлення, репліка для читання.',
    why: 'На тисячі рядків будь-який запит швидкий, і різниця між планами непомітна. Проблема виникає, коли таблиця виростає до мільйонів: повне сканування, яке займало мілісекунди, перетворюється на десятки секунд і починає блокувати пул зʼєднань, через що падає весь застосунок, а не один запит.',
    simple:
      'Перше правило: не вгадувати, а дивитись план. EXPLAIN ANALYZE виконує запит і показує, що база зробила насправді — де був Seq Scan по всій таблиці, де Index Scan, скільки рядків вона очікувала і скільки отримала. Якщо очікувала сто, а отримала мільйон, проблема у статистиці, і починати треба з ANALYZE, а не з переписування SQL. Далі дивишся, скільки даних запит узагалі чіпає. Найдешевша оптимізація — не читати зайвого: прибрати SELECT зірочку, звузити діапазон дат, додати LIMIT. Потім індекс: поле з WHERE і поле з ORDER BY складають один складений індекс у правильному порядку, спершу рівності, потім діапазон. Класична пастка — пагінація через OFFSET сто тисяч: база однаково читає й викидає всі сто тисяч рядків, і чим далі сторінка, тим повільніше. Лікується keyset-пагінацією, тобто умовою WHERE created_at менше за останнє побачене значення. Друга класична пастка — запит у циклі в коді застосунку, коли на сто рядків летить сто один запит; це вирішується одним JOIN або одним IN. Якщо після всього цього запит усе одно важкий, значить оптимізується вже не запит, а схема: партиціонування за датою, окрема таблиця-агрегат, що оновлюється фоново, або винесення аналітики на репліку для читання.',
    related: [
      {
        title: 'Як читати EXPLAIN ANALYZE',
        text: 'Дерево читається знизу вгору й ізсередини назовні. Seq Scan на великій таблиці в поєднанні з малою кількістю повернутих рядків означає брак індексу. Різниця між rows у прогнозі й actual rows у виконанні вказує на застарілу статистику. Вузли Sort і Hash із позначкою про запис на диск означають брак памʼяті під операцію, який у PostgreSQL регулюється work_mem.',
      },
      {
        title: 'Проблема N+1 запитів',
        text: 'ORM, що ліниво підвантажує звʼязок для кожного елемента списку, перетворює одну вибірку на сотні окремих запитів. Кожен із них швидкий, тому в логах повільних запитів нічого не видно, а загальний час відповіді все одно величезний. Лікується жадібним завантаженням, тобто одним JOIN або одним запитом з IN по зібраних ідентифікаторах.',
      },
      {
        title: 'Keyset-пагінація замість OFFSET',
        text: 'OFFSET змушує базу прочитати й відкинути всі пропущені рядки, тому сторінка номер тисяча коштує в тисячу разів дорожче за першу. Keyset-пагінація тримає курсор на останньому побаченому значенні сортування і бере наступну порцію умовою більше або менше, працюючи по індексу за сталий час незалежно від глибини.',
      },
      {
        title: 'Партиціонування великих таблиць',
        text: 'Розбиття таблиці на секції за датою або за ключем дозволяє планувальнику взагалі не торкатись непотрібних секцій — це partition pruning. Додатковий виграш у тому, що видалення старих даних стає миттєвим DROP секції замість важкого DELETE, який лишає по собі роздуту таблицю й роботу для VACUUM.',
      },
    ],
    codeExamples: [
      {
        label: 'Діагностика планом',
        code: `-- реальний час і реальна кількість рядків по кожному вузлу
EXPLAIN (ANALYZE, BUFFERS)
SELECT o.id, o.total
FROM orders o
WHERE o.user_id = 42 AND o.created_at >= '2026-01-01'
ORDER BY o.created_at DESC
LIMIT 20;

-- Seq Scan на orders  (rows=8_000_000) ← індексу немає
-- після CREATE INDEX ON orders (user_id, created_at DESC):
-- Index Scan using idx_orders_user_created (rows=20)`,
      },
      {
        label: 'OFFSET проти keyset-пагінації',
        code: `-- ❌ сторінка 5000: база прочитає й викине 100 000 рядків
SELECT * FROM orders
ORDER BY created_at DESC
LIMIT 20 OFFSET 100000;

-- ✅ курсор по останньому побаченому значенню — сталий час
SELECT * FROM orders
WHERE created_at < $last_seen_created_at
ORDER BY created_at DESC
LIMIT 20;`,
      },
    ],
    seniorNotes: [
      'EXPLAIN показує лише оцінку планувальника, EXPLAIN ANALYZE реально виконує запит — на UPDATE чи DELETE його запускають усередині транзакції з подальшим ROLLBACK, інакше дані зміняться.',
      'Повільний запит часто взагалі не про запит: пул зʼєднань вичерпаний, довга транзакція тримає блокування, або autovacuum не встигає й таблиця роздута. У PostgreSQL це видно в pg_stat_activity за станом idle in transaction і в pg_locks.',
      'Розширення pg_stat_statements дає агрегований топ запитів за сумарним часом — саме воно показує, що вбиває базу насправді, бо запит на 20 мілісекунд, викликаний мільйон разів, шкідливіший за разовий звіт на 10 секунд.',
      'Додавати індекс під кожен повільний запит — типова помилка: кожен індекс сповільнює запис і займає місце. Перед створенням варто перевірити, чи не покриває потребу вже наявний складений індекс за правилом лівого префікса.',
      'Аналітичні запити по всій таблиці не оптимізуються індексами в принципі — для них правильна відповідь це окрема репліка для читання, колонкове сховище або попередньо порахований агрегат, а не чергова спроба підібрати індекс.',
    ],
  },

  {
    id: 'sql-transactions-acid',
    title: 'Що таке ACID, рівні ізоляції транзакцій і як працюють блокування?',
    definition:
      'ACID — це чотири гарантії транзакції: Atomicity, тобто застосовується або все, або нічого; Consistency, тобто база переходить з одного коректного стану в інший із дотриманням усіх обмежень; Isolation, тобто паралельні транзакції не бачать проміжних станів одна одної; Durability, тобто зафіксовані дані переживуть падіння процесу завдяки журналу попереднього запису. Ізоляція має рівні: READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ і SERIALIZABLE, і кожен наступний забороняє ще одну аномалію — брудне читання, неповторюване читання, фантомні рядки — ціною більшої кількості конфліктів і блокувань.',
    why: 'Коли з одними даними одночасно працюють сотні запитів, без гарантій ізоляції зʼявляються аномалії, що не відтворюються в тестах: два запити прочитали однаковий залишок і обидва його списали. Рівні ізоляції — це вибір, скільки саме паралельності ти готовий віддати за коректність у конкретному сценарії.',
    simple:
      'Транзакція — це пакет змін, який база застосовує цілком або не застосовує взагалі. Переказ грошей це два UPDATE, і якщо процес упаде між ними, гроші зникнуть; транзакція гарантує, що такого проміжного стану ззовні не існуватиме. Найцікавіше на співбесіді — ізоляція. READ COMMITTED, дефолт у PostgreSQL, означає: ти бачиш лише зафіксовані чужі зміни, але два однакові SELECT у межах твоєї транзакції можуть дати різний результат, бо між ними хтось встиг закомітити. REPEATABLE READ, дефолт у MySQL, фіксує знімок даних на початок транзакції, тому повторне читання завжди дає те саме. SERIALIZABLE поводиться так, ніби транзакції виконуються одна за одною, і в PostgreSQL реалізований через відстеження конфліктів: конфліктна транзакція просто відкочується з помилкою serialization failure, і застосунок мусить вміти її повторити. Блокування бувають двох видів: спільні на читання і виключні на запис, і саме через них паралельні оновлення одного рядка стають у чергу. Ключовий практичний прийом — SELECT ... FOR UPDATE: він бере виключне блокування на прочитані рядки до кінця транзакції, і саме так реалізують перевірку залишку перед списанням. Головне правило експлуатації: транзакція має бути короткою і не містити мережевих викликів, бо довга транзакція тримає блокування і в PostgreSQL ще й не дає vacuum прибрати старі версії рядків.',
    related: [
      {
        title: 'MVCC замість блокувань на читання',
        text: 'PostgreSQL і InnoDB не блокують рядки на читання: кожна транзакція бачить власну версію рядка за знімком, а старі версії зберігаються до тих пір, поки їх хтось бачить. Звідси наслідок, що дивує новачків: читання ніколи не чекає на запис, зате довга транзакція, яка нічого не робить, заважає прибирати мертві версії й роздуває таблицю.',
      },
      {
        title: 'Песимістичне й оптимістичне блокування',
        text: 'Песимістичний підхід бере блокування наперед через SELECT FOR UPDATE і змушує конкурентів чекати — доречно, коли конфлікти часті й дорогі, як зі списанням коштів. Оптимістичний підхід нічого не блокує, а тримає в рядку номер версії й перевіряє його в умові UPDATE: якщо версія змінилась, оновлення зачіпає нуль рядків і застосунок повторює операцію. Другий варіант масштабується краще при рідкісних конфліктах.',
      },
      {
        title: 'Дедлоки і порядок блокувань',
        text: 'Дедлок виникає, коли дві транзакції беруть ті самі блокування у зворотному порядку: перша тримає рядок A і чекає B, друга навпаки. База сама виявляє цикл і вбиває одну транзакцію з помилкою deadlock detected. Стандартна профілактика це завжди брати блокування в однаковому порядку, наприклад сортувати ідентифікатори перед оновленням, і тримати транзакції короткими.',
      },
      {
        title: 'Аномалії, які забороняє кожен рівень',
        text: 'Брудне читання, тобто читання незакомічених чужих змін, забороняється вже на READ COMMITTED, і в PostgreSQL воно недосяжне взагалі. Неповторюване читання, коли той самий рядок змінився між двома SELECT, забороняє REPEATABLE READ. Фантоми, коли між читаннями зʼявились нові рядки під ту саму умову, у стандарті знімає лише SERIALIZABLE, хоча знімкова ізоляція PostgreSQL прибирає їх і на REPEATABLE READ.',
      },
    ],
    codeExamples: [
      {
        label: 'Блокування рядка перед списанням',
        code: `BEGIN;

-- виключне блокування рядка до кінця транзакції:
-- паралельна транзакція на цьому ж рядку чекатиме
SELECT balance FROM accounts WHERE id = 42 FOR UPDATE;

-- перевірка залишку вже безпечна
UPDATE accounts SET balance = balance - 100 WHERE id = 42;
INSERT INTO transfers (account_id, amount) VALUES (42, -100);

COMMIT;`,
      },
      {
        label: 'Оптимістичне блокування по версії',
        code: `-- ❌ read-modify-write без захисту: втрачене оновлення
const { balance } = await db.one('SELECT balance FROM accounts WHERE id = $1', [id]);
await db.none('UPDATE accounts SET balance = $1 WHERE id = $2', [balance - 100, id]);

// ✅ умова на версію: нуль оновлених рядків означає конфлікт
const res = await db.result(
  'UPDATE accounts SET balance = balance - 100, version = version + 1 ' +
  'WHERE id = $1 AND version = $2',
  [id, version],
);
if (res.rowCount === 0) throw new ConflictError('спробуй ще раз');`,
      },
    ],
    seniorNotes: [
      'Рівень ізоляції за замовчуванням різний: PostgreSQL використовує READ COMMITTED, MySQL InnoDB — REPEATABLE READ. Один і той самий код дає різну поведінку при міграції між ними, і це часте джерело багів у звітах.',
      'SERIALIZABLE у PostgreSQL не блокує наперед, а відстежує конфлікти і відкочує транзакцію з кодом 40001 serialization failure — застосунок обовʼязково мусить мати логіку повтору, інакше рівень просто перетворюється на генератор помилок під навантаженням.',
      'SELECT FOR UPDATE блокує лише вже наявні рядки й не заважає вставити новий рядок під ту саму умову; від такого фантома рятує або унікальний індекс, або явне блокування діапазону, або advisory lock.',
      'Durability забезпечує журнал WAL із fsync на комміті; synchronous_commit = off підвищує пропускну здатність у рази, але при падінні втрачаються останні транзакції — це свідомий компроміс, а не оптимізація без наслідків.',
      'Довга транзакція в PostgreSQL блокує очищення мертвих версій рядків по всій базі, тому один забутий idle in transaction здатен роздути таблиці й обвалити продуктивність там, де з ним немає жодного звʼязку.',
    ],
  },
]
